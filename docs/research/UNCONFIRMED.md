# UNCONFIRMED Claims Registry (live)

Claims used in planning that were **not verified against a primary source**, or
that carry material uncertainty. The builder agent must not write code that
depends on an `unverified` row — verify first (vendored headers, upstream docs
matching the pinned version, or a passing test), then update `Status` and
`Verified by`. Add new rows whenever web-derived knowledge flows into code.

Statuses: `unverified` | `confirmed` | `refuted` (refuted rows get an addendum
in the source report).

| ID | Claim | Source | Date added | Status | Verified by | Impact if wrong |
|----|-------|--------|-----------|--------|-------------|-----------------|
| U-01 | Luau `class` syntax ships enabled/unflagged in 0.734 (implementation exists on master; flag state unknown) | luau-2026.md §5 | 2026-08-19 | unverified | — | None for v1 (ADR 0002 keeps `class` out of the idiom); matters for future adoption |
| U-02 | Luau 64-bit `integer` library is included in `luaL_openlibs` by default and is production-hardened | luau-2026.md §5 | 2026-08-19 | unverified | — | None for v1 (kept out of idiom); revisit for entity/network IDs later |
| U-03 | NCG performance figures (1.5–2.5×, 3.2× Mandelbrot) — newest official numbers are from 2023 | luau-2026.md §3 | 2026-08-19 | unverified | — | Perf planning only; re-benchmark on the pinned build (M2+) |
| U-04 | Android NCG scope (ABIs, W^X handling, version floor) beyond "production-tested" | luau-2026.md §3 | 2026-08-19 | unverified | — | Mobile phase planning |
| U-05 | iOS prohibits JIT ⇒ Luau NCG unavailable in App Store builds (well-established policy; no explicit 2026 Luau statement found) | ecosystem-2026.md notes | 2026-08-19 | unverified | — | Mobile perf budget = interpreter (already assumed; rule R16) |
| U-06 | SDL3 GPU Android support still "limited" as of Aug 2026 (FAQ wording may be stale) | ecosystem-2026.md §B.2 | 2026-08-19 | unverified | — | RHI backend strategy; mitigated by NDK compile job + device checkpoint + bgfx hedge (ADR 0005) |
| U-07 | SDL 3.4.x exact latest patch version (sources conflicted: 3.4.8 vs 3.4.14) | ecosystem-2026.md | 2026-08-19 | confirmed | M0 vendor step: upstream `releases/latest` is `release-3.4.14` (published 2026-08-03); pinned at `147a8ee32dbf` in `third_party/manifest.json` | Pin the actual tag at vendor time (M0) |
| U-08 | Jolt 5.6.0 release date year (2026-07-11 inferred) | ecosystem-2026.md §B.3 | 2026-08-19 | unverified | — | None; pin actual tag at vendor time |
| U-09 | `lute compile` does not support cross-compilation (docs silent) | lute-2026.md §7 | 2026-08-19 | unverified | — | CLI distribution plan (build per-OS in CI if true) |
| U-10 | `@lute/vm` child runtimes are OS-threaded (inferred from `runContinuously()` comment) | lute-2026.md §3 | 2026-08-19 | unverified | — | Tooling-only concern |
| U-11 | Roblox frame-phase ordering: `Heartbeat` is a separate, later resumption point than `PostSimulation` (from deferred-events doc, not the scheduler SVG) | ecosystem-2026.md §A.2 | 2026-08-19 | unverified | — | LuauG defines its own documented order (architecture §scheduler); familiarity nuance only |
| U-12 | Diligent Engine's Metal backend still requires a commercial license | ecosystem-2026.md §B.2 | 2026-08-19 | unverified | — | None (Diligent not chosen) |
| U-13 | FMOD/Wwise 2026 pricing tiers (secondary sources) | ecosystem-2026.md §B.4 | 2026-08-19 | unverified | — | Post-v1 plugin planning only |
| U-14 | luau-lsp custom-platform mode types the builtin `vector` with lowercase `x/y/z` correctly for our defs | api-design risk #1 | 2026-08-19 | unverified | — | Week-one spike in M3; affects Vector3 DX polish, not architecture |
| U-15 | "luauengine.org" claims to be an official Roblox Studio fork — no engine source found, no Roblox mention | ecosystem-2026.md §A.1 | 2026-08-19 | unverified (treat as false) | — | Ignore; never base anything on it |
| U-16 | meshoptimizer / assimp / Recast current exact versions (minor source conflicts) | ecosystem-2026.md §B | 2026-08-19 | unverified | — | Pin actual tags at vendor time (M0) |
| U-54 | The `Vector3` convenience members (`.Magnitude`, `.Unit`, `:Dot`, `:Cross`, `:Lerp`, `:Angle`) can be declared in the generated definitions, so `--!strict` code reaches them | api-design.md §2.3, ADR 0013 | 2026-08-20 | refuted | `vector` is a Luau builtin type and `declare extern type vector with …` does not augment it -- the builtin wins and every member reads as "Key not found in external type 'vector'". Verified against luau-lsp at the pin with a two-file reproduction | They work at RUNTIME and are not type-checkable. `--!strict` code must use the `vector.*` library form, which api-design.md §2.3 already recommends for speed; the conformance spec reaches the members through a widened alias to prove they exist |
| U-51 | A pure-Luau `@luaug/signal` can honour §3.1's deferred contract off-engine (under Lute or Roblox), so shared code keeps one signal semantics on every runtime | api-design.md §1.1 (@luaug tier); M2 spec-gap G41 | 2026-08-19 | unverified | — | If it cannot, the module is immediate off-engine and shared code silently changes ordering across runtimes — the convergence bet's one correctness hole. `@luaug/signal` is not M2 scope; answer before it ships, either with a portable definition in §3.1 or by scoping the module to the engine |
| U-52 | `v.X` on a vector raises `script.err.unknown_member`, so lowercase is the only spelling that works at runtime | api-design.md §2.3 (pre-2026-08-20 text), api/defs/datatypes.api.luau | 2026-08-20 | refuted | `LOP_GETTABLEKS` answers a single-character index on a vector inline and case-insensitively, before consulting `global->mt[LUA_TVECTOR]` (`VM/src/lvmexecute.cpp:619-635`). No metatable the engine installs is reachable for x/y/z in either case | The rule was unimplementable. api-design.md §2.3 now states the exception; lowercase stays the only declared spelling, so `v.X` remains a **type** error while returning the component at runtime |
| U-53 | `typeof` reports a metatable's `__type` for any value carrying one, so `Enum.PartShape` can be a frozen table | api-design.md §2.3 ("an enum object such as `Enum.PartShape` is `"Enum"`") | 2026-08-20 | refuted | `luaT_objtypenamestr` reads `__type` for **userdata** and then for `global->mt[ttype]`; the comment at `VM/src/ltm.cpp:167` says "for all types except userdata and table" — a table's own metatable is never consulted | `Enum` and every enum object are tagged userdata (tags 8 and 9) rather than tables. Two tags out of the 128 budget, spent to keep a documented `typeof` answer true |

Full per-report lists live in each report's final section. When the M0 vendor
step captures real tags/SHAs, update U-07/U-08/U-16 and
`third_party/manifest.json` together.

## Verified contradictions — the M3 dev loop vs the installed Lute (2026-08-20)

`docs/research/lute-2026.md` was captured from documentation and the GitHub API,
never from the binary. These five rows come from the typedefs `rokit` installed
at the pin (`~/.lute/typedefs/1.0.0/`) and from probe scripts run under
`lute 1.0.0`. Between them they decide the M3 control-channel design (ADR 0035).

| ID | Claim | Source | Date added | Status | Verified by | Impact if wrong |
|----|-------|--------|-----------|--------|-------------|-----------------|
| U-55 | A Lute dev server can supervise a long-running engine over its stdin/stdout, so the hot-reload control channel can be a pipe | M3 brief Decision 1 (first draft) | 2026-08-20 | refuted | `process.run(args, options) -> ProcessResult` returns **after the child exits** and hands back `stdout`/`stderr` as whole strings (`typedefs/1.0.0/lute/process.luau:33-62`). There is no child handle, no stdin, and no incremental read. `StdioKind` offers only `default` (capture) / `inherit` / `none` | A pipe protocol is not writable in Lute 1.0.0. The channel has to be a socket the engine opens, which is what api-design §3.2 said before the brief argued otherwise |
| U-56 | `process.run` blocks the whole runtime until the child exits, so a dev server cannot both run the engine and watch files | M3 brief entering risk 4 | 2026-08-20 | confirmed as **yields** | Probe: a `task.spawn`ed 150 ms heartbeat fired six times, and eleven `fs.watch` callbacks were delivered, while another coroutine sat inside a 600 ms / 2.5 s `process.run`. It yields the calling coroutine like the rest of the libuv-backed surface | The dev server's concurrency model is sound: one task runs the engine to completion, the rest keeps serving |
| U-57 | Lute exposes raw TCP, so the engine hop can use a line protocol without WebSocket framing | lute-2026.md §4 ("net: HTTP+WS client & server") read optimistically | 2026-08-20 | refuted | The entire net surface is `client.request`, `client.websocket`, `server.serve` (`typedefs/1.0.0/lute/net/{client,server}.luau`). No socket, listener, or stream type exists | WebSocket is the only bidirectional push channel Lute can speak, so the engine implements an RFC 6455 **client**. Framing is not optional |
| U-58 | `fs.watch(dir, cb)` reports the changed file, recursively | lute-2026.md §4 ("`watch(path, cb)` → `WatchHandle`") | 2026-08-20 | refuted | Probe on Windows: editing `root/sub/deep.luau` fires with `filename="sub"` — the top-level entry, not the file. One ordinary rewrite fires **2** events; a write-temp-then-rename save fires **6** across two names; a file creation fires **2** | The event payload cannot identify what changed. The watcher must watch every directory it cares about individually and rescan on a debounce, which is also what makes it behave the same on inotify. **Linux is worse than Windows and was verified in the Tier-2 container**: editing `sub/deep.luau` under a watch on the parent produces **no event at all**, where Windows at least named the directory. Watching every directory individually is therefore not an optimisation but the only thing that works on both |
| U-60 | `process.args` is the arguments passed to the script | `typedefs/1.0.0/lute/process.luau:45` ("Command-line arguments passed to the current script") | 2026-08-20 | refuted | Under `lute run script.luau -- a --b` it is `{script.luau, --, a, --b}`: the script path is entry 1 and the `--` that was meant to end Lute's own options survives into it. Upstream issue #852 is about the first half | Any CLI that reads `args[1]` as its command reads a path instead. `tools/cli/main.luau` drops a leading `*.luau` and a leading `--` before parsing |
| U-61 | A module `x.luau` and a sibling directory `x/` can coexist | assumed while laying out `tools/cli/` | 2026-08-20 | refuted | `require("../i18n")` with both `i18n.luau` and `i18n/` present fails with "could not resolve child component (ambiguous)" -- the resolver will not pick one | The module was renamed to `catalog.luau`; the directory keeps the name api-design.md §6 gives it. Worth knowing before laying out any module tree that also ships data |
| U-62 | `net.server.serve` accepts an HTTP `handler` and `websocket` handlers together, so one server can answer requests and take upgrades | `typedefs/1.0.0/lute/net/server.luau:46-53` (`Configuration` carries both) | 2026-08-20 | refuted | With a `handler` present the upgrade is refused -- the client reports "websocket connect failed: HTTP response code said error" -- and it is refused whatever the handler returns, `nil` included. Removing the field entirely makes the same connection succeed. Verified by reducing to a ten-line probe | A dev server that wants both has to choose. Ours takes upgrades only, which is all it ever needed |
| U-63 | `time.now()` returns something `string.format` can print | `@lute/time`; devserver's first token minting | 2026-08-20 | refuted | It returns an `instant` userdata: `string.format("%.9f", time.now())` raises "number expected, got instant". Related to U-59 from the other side -- the typedef says `Duration` where a number comes back, and here a userdata comes back where a number was assumed | Any timing or stamping code has to convert explicitly. The dev token is drawn from `math.random` instead, which is what it needed anyway |
| U-59 | `time.since(instant)` returns a `Duration` with `:milliseconds()` etc. | `typedefs/1.0.0/lute/time.luau` | 2026-08-20 | refuted | It returns a plain `number` of seconds; `:milliseconds()` raises "attempt to index number with 'milliseconds'". The installed typedef disagrees with the installed runtime | Any CLI timing code written from the typedef fails at runtime rather than at analysis. Treat `@lute/time` returns as numbers and convert explicitly |

## Verified contradictions — design docs vs vendored Luau (2026-08-19)

The rows below are the **inverse** of the ones above: they were verified against
a primary source (the vendored Luau tree at
`3fc82b1071ab387531175869afc4fb528464afa4`) and the source **disagrees with what
a design document, an ADR, or an earlier research report assumes**. `refuted`
here means "the claim as written is not what the code does", not "the design is
unworkable" — most have a supported alternative shape, named in the report.

Full context and the surrounding semantics for every row:
[`luau-c-api-2026.md`](luau-c-api-2026.md) §0 carries the index, and the section
column below points at the discussion. Nothing here has been actioned in the
design documents — that is a decision for the milestone owner, not for the
research pass.

| ID | Claim | Source | Date added | Status | Verified by | Impact if wrong |
|----|-------|--------|-----------|--------|-------------|-----------------|
| U-17 | One `TAG_INSTANCE` for all Instances **and** per-class metatables pre-registered so `lua_newuserdatataggedwithmetatable` applies | architecture.md:551-554 | 2026-08-19 | refuted | `lapi.cpp:1583` reads `global->udatamt[tag]` — one metatable per tag; `lapi.cpp:1884` `api_check`s `!udatamt[tag]`, so reassignment is impossible (luau-c-api-2026.md §1.3) | Binding-layer shape for every Instance and datatype. Two supported alternatives exist; the doc asserts both at once |
| U-18 | `LUA_UTAG_LIMIT = 128` means 128 host-usable tags | luau-2026.md:277 | 2026-08-19 | refuted | Tag 0 is the `lua_newuserdata` macro (`lua.h:496`), 128 = `UTAG_IDTOR` and 129 = `UTAG_PROXY` (`ludata.h:8,11`) — ~126 usable, shared between classes and value types (§1.1) | Caps a one-tag-per-class design; forces the shared-tag design past ~126 classes |
| U-19 | `useratom` is "string interning", called "when a string is created" | luau-2026.md:279, `lua.h:607` comment | 2026-08-19 | refuted | `luaS_updateatom` (`lstring.h:21-25`) is lazy and one-shot; `-1` is cached on the `TString` permanently (§1.4) | Any name interned before `useratom` is installed can never enter the fast path — install it immediately after `lua_newstate` |
| U-20 | Atom dispatch can be wired up at any point | architecture.md:561-563 (implied) | 2026-08-19 | refuted | Fast opcodes chosen once per `Proto` at `lvmload.cpp:637-674`; deopt is one-way (`lvmexecute.cpp:3501,3570,3661`); direct-access registration snapshots the metatable (`lapi.cpp:1940-1956`) (§1.5, §1.6) | All metatable + direct-access registration must complete at VM boot; hot reload needs a complete, stable atom table before first load |
| U-21 | A per-class slot numbering behind the atom switch | architecture.md:561-563 | 2026-08-19 | refuted | `cachedslot` is per-callsite and shared across tags (`lua.h:425-426`); the reference impl uses one global enum (`Conformance.test.cpp:946-1001`) (§1.6) | A per-class numbering mis-dispatches at any polymorphic callsite |
| U-22 | The direct-field-get and embedder-GC / weak-ref APIs are usable | architecture.md:554 (Instance-handle cache) | 2026-08-19 | refuted | `FFlag::LuauDirectFieldGet` (`lvmexecute.cpp:21`) and `FFlag::LuauGcTraceUdata` (`lgc.cpp:20`) both default false; the first **silently no-ops**, the second asserts (§1.7, §5) | Enabling either is a deliberate dependency on an unshipped feature — needs an ADR (R5) |
| U-23 | `luau-2026.md` §4 describes the userdata dispatch surface | luau-2026.md:272-279 | 2026-08-19 | refuted | It omits `lua_setuserdatametatable`, `lua_registeruserdatadirectaccess`, the `*UDATAKS` opcodes and the load-time atom upgrade, and names `lua_tolstringatom` + `useratom` — the *slow* route — as the primitive (§1.5) | Implementing from that report alone produces the generic-metamethod version and misses the fast path entirely |
| U-24 | `lua_newuserdatataggedwithmetatable` is "~3x faster" | architecture.md:553, luau-2026.md:273 | 2026-08-19 | unverified | Not measurable from source; no benchmark in `third_party/luau/bench`. Mechanical saving is one round trip, one push/pop, one barrier (`lapi.cpp:1572-1584`) (§1.3) | Perf planning only; re-measure on the engine's allocation pattern |
| U-25 | The vendored tree is the stock 0.734 VM surface | third_party/manifest.json, architecture.md | 2026-08-19 | refuted | The pin carries `LUA_TCLASS`/`LUA_TOBJECT`/`LUA_TINTEGER` (`lua.h:75-97`), a `class` library (`lualib.h:142`), embedder-GC (`lua.h:368-413`), direct-field APIs (`lua.h:438-463`) and an `Inliner/` tree (§5) | `lua_Type` ordinals differ from stock; published 0.734 documentation is not a safe reference for this checkout |
| U-26 | "`lua_callbacks->interrupt` armed every resumption" | architecture.md:570 | 2026-08-19 | refuted | `lua_callbacks(L)` returns `&L->global->cb` (`lapi.cpp:2030`) — one pointer per VM. `lua.h:596-598` warns re-assignment is racy (§2.4) | The watchdog must be one global callback recovering per-script budget via `lua_getthreaddata` |
| U-27 | A 1 s hard-kill bounds any script | architecture.md:570-574 | 2026-08-19 | refuted | Interrupt fires at 7 opcodes, inside `luaC_step`, and in pattern matching only; a long builtin is unpreemptible. It must return early on `gc >= 0`. The raised error is catchable by script `pcall` (§2.4) | No robust kill exists; the fallback is re-arm-and-raise, or abandon + `lua_resetthread` |
| U-28 | "the `userthread` callback maintains it [ScriptThreadCtx]" | architecture.md:531-532 | 2026-08-19 | refuted | Never fired for the main thread (`lstate.cpp:190`, `:107-130`), not fired by `lua_resetthread` (`:155`), and children do not inherit `userdata` (`:104`) (§2.2) | Main-thread context by hand; pooled threads carry stale context; children need an explicit copy |
| U-29 | R4 "sandbox always" is satisfied by `luaL_sandbox` | architecture.md:525-532, R4 | 2026-08-19 | refuted | `linit.cpp:65-92` freezes one level deep, freezes only the **string** metatable despite its comment, and removes nothing (§2.5) | Library curation is entirely the engine's job; tag metatables must be frozen separately |
| U-30 | Script code gets the `safeenv` fast paths and builtin folding | perf baselines, architecture.md:533-535 | 2026-08-19 | refuted | `getfenv`/`setfenv` exist (`lbaselib.cpp:367,376`) and clear `safeenv` permanently (`:131,140`); a *mention* of either name kills folding module-wide (`Compiler.cpp:4850-4870`, `:5232`) (§2.5, §4.4) | Remove both globals or qualify the baselines; add a `luaug check` lint for the `if getfenv then` idiom |
| U-31 | `lua_debugtrace` gives the watchdog a full traceback | architecture.md:519-523, 570-574, api-design.md:364 | 2026-08-19 | refuted | Static `char buf[4096]` shared process-wide (`ldebug.cpp:666`), middle elided to first-10/last-10. Frames die at `lua_resetthread` (`lstate.cpp:161-183`) (§2.6) | Use `luaL_traceback(schedulerL, co, …)`; capture before recycling, as `coroutine.close` does |
| U-32 | R3: every user-facing string is a key | R3, architecture.md error pipeline | 2026-08-19 | refuted | `luaD_seterrorobj` (`ldo.cpp:380-393`) replaces the error object with fixed English literals for `LUA_ERRMEM`/`LUA_ERRERR`; all stdlib messages are literals (§2.6) | Map on the status code, never on the message |
| U-33 | The version pin fixes runtime behaviour | MASTER_PROMPT §5 (R5), architecture.md | 2026-08-19 | refuted | FastFlags default false (`Common.h:139-143`) but the CLI turns nearly all on (`Flags.cpp:38-44`); several change require, resume and bytecode-version semantics (§5) | Determinism risk under R10 — the flag set must be pinned explicitly and treated as part of the version |
| U-34 | Engine-invoked script (signals, `BindToClose`, entry points) can yield | architecture.md:404, api-design.md | 2026-08-19 | refuted | `lua_pcall` (`lapi.cpp:1167`) makes everything under it non-yieldable and `api_check`s `status == 0`; Luau's `pcall` is `lua_pcallyieldable` (`lapi.cpp:1243`) (§2.3) | Those call sites need their own coroutine, or `lua_pcallyieldable` from a `lua_pushcclosurek` closure |
| U-35 | "a module that errors propagates to its requirer and the failure is cached" | api-design.md:361 | 2026-08-19 | refuted | `_MODULES` is written only in `lua_requirecont` (`RequireImpl.cpp:236-303`), skipped when `load` raises — the module re-runs every time (§3.4) | Failure caching must be implemented in the engine's own `load`; a failed cyclic load leaves a permanent locked placeholder that nothing cleans up |
| U-36 | "cyclic-require semantics" are available | architecture.md:542 | 2026-08-19 | refuted | Needs `LuauCyclicRequireShortCircuit` + `LuauExportValueSyntax` (both default false) **and** the `export` keyword **and** an embedder calling `createplaceholder`. Plain `return {}` cycles are unsupported at any flag combination (§3.4) | Without all four, a cycle recurses until the C stack is exhausted — there is no guard at all |
| U-37 | Resolution "identical across engine runtime / luau-analyze / luau-lsp / Lute tooling" | architecture.md:542 | 2026-08-19 | refuted | Runtime clears the config per step and never merges (`RequireNavigator.cpp:262-321`); the analyzer accumulates with `overwriteAliases=true` (`CLI/src/Analyze.cpp:252-318`). The analyzer's resolver lives in an executable, not a library (§3.2) | Different answers whenever an alias is redefined along a chain; the resolver must be reimplemented, not linked |
| U-38 | Require-by-string is available in a compiler-free shipping build (ADR 0002) | ADR 0002, cmake/luaug_luau.cmake | 2026-08-19 | refuted | `Luau.Require` → `Luau.Config` → `Luau.Ast` PUBLIC + `Luau.Compiler` PRIVATE (`third_party/luau/CMakeLists.txt:118-119,135`); static-lib PRIVATE deps still propagate as link requirements (§3.5) | Either ship the compiler, or build require on the `RequireNavigator` C++ interface with a context that never reports `PresentLuau` |
| U-39 | `is_require_allowed` gates access to engine modules | R4, api-design.md §7 | 2026-08-19 | refuted | `checkRegisteredModules` runs at `RequireImpl.cpp:318-321`, **before** `resolveRequire` — the only place the callback is consulted (§3.1) | Denied chunks can still `require('@luaug/…')`; the capability gate must live inside the modules |
| U-40 | `@std/…` and `@luaug/…` resolve via `luarequire_registermodule` | api-design.md:47 | 2026-08-19 | refuted | Exact whole-string equality against an ASCII-lowercased key (`RequireImpl.cpp:133-153`, `386-419`); no prefix matching, eager values not factories, never in `_MODULES` so cache-clearing cannot touch them (§3.1) | Every path registered individually; hot reload cannot invalidate them through the standard API; paths become case-insensitive, which sits badly with PascalCase |
| U-41 | A `.luaurc` reader is available to the runtime | architecture.md:542, api-design.md §1.3 | 2026-08-19 | refuted | `Luau.Require` has zero filesystem knowledge — only `Luau::parseConfig` is vendored. The reference `VfsNavigator` is CWD-dependent (`CLI/src/VfsNavigator.cpp:143`) and its path is the cache key (§3.3) | File discovery is the embedder's; deterministic/relocatable cache keys (R10) need a bespoke `get_cache_key` |
| U-42 | A user `.luaurc` may carry `languageMode` plus aliases (and by implication other keys) | api-design.md §1.3 | 2026-08-19 | refuted | Any unrecognised key is a hard error aborting the whole require (`Config.cpp:386`); numbers and `null` are syntax errors; `.config.luau` matches lowercase keys only and is wall-clock-timed (§3.3) | A `$schema` or LSP-specific key breaks `require` at runtime; `.config.luau` is non-deterministic (R10) — prefer `.luaurc` only |
| U-43 | `@self` cannot be overridden by user aliases | api-design.md:52, luau-2026.md §5 | 2026-08-19 | refuted | `DFFlag::LuauSelfIsSelfAndAlwaysSelf` defaults false (`RequireNavigator.cpp:16`); at the default a config alias named `self` is searched first (`:121-138`) (§3.2) | Set the flag at boot, or reject `self` as an alias key in generated projects |
| U-44 | `vectorLib` + `vectorCtor` + `vectorType` together "give constant folding + fastcalls" | ADR 0013:16-18, architecture.md:533-535 | 2026-08-19 | refuted | `vectorType` reaches exactly one line (`Types.cpp:76-77`) and only tags a type annotation; folding/fastcalls come from `vectorLib`+`vectorCtor` (`Builtins.cpp:372-384`) (§4.4) | Keep setting `vectorType` for native codegen, but do not diagnose a folding failure by looking at it |
| U-45 | GC step size derived from `lua_allocationrate` and remaining frame headroom | architecture.md:424-425, 589-591 | 2026-08-19 | refuted | `LUA_GCSTEP`/`LUA_GCSETSTEPSIZE` take **KB** (`lapi.cpp:1362,1439-1444`); `lua_allocationrate` returns **bytes/s** and `-1` whenever the window is under 1 ms (`lgc.cpp:1478-1494`); it reads the wall clock (`lperf.cpp:65`) (§4.3) | A missing `>> 10` is a 1024× GC overrun per frame; the `-1` case must be branched on; the value must never reach simulation state (R10) |
| U-46 | The script heap cap raises a graceful keyed error | architecture.md:581-587 | 2026-08-19 | refuted | Refusing an allocation means returning NULL, which every site turns into `luaD_throw(L, LUA_ERRMEM)` with the fixed message (`lmem.cpp:504-505` et al.); shrinks may never fail; the first allocation precedes any `lua_State` (§4.1) | Enforce with NULL, then translate `LUA_ERRMEM` into the keyed error at the pcall/resume boundary |
| U-47 | "a thin counting wrapper over the OS allocator" | architecture.md:581-583 | 2026-08-19 | refuted | The VM already keeps per-category and total byte accounting (`lua_totalbytes`, `lstate.h:241`), and 1..1024-byte objects come from pre-allocated pages (`lmem.cpp:137,197`) so a wrapper sees page churn (§4.1, §4.2) | Duplicated work and wrong granularity; use `lua_totalbytes`, or `lua_Callbacks::onallocate` (`lua.h:614`) for per-allocation visibility |
| U-48 | Memory categories 32–255 form a per-Script pool | architecture.md:577-580 | 2026-08-19 | refuted | 256 is an ABI ceiling (one byte in `CommonHeader`, `lobject.h:17-18`), so the pool caps at 224 concurrent scripts; and `lua_setmemcat`'s range check is `api_check`, compiled out in release (`lapi.cpp:2035-2039`) (§4.2) | "Coalesce oldest when exhausted" is mandatory, not a nicety; the host must validate the category itself |
| U-49 | Bytecode cache keyed on `blake3(source) + luauVersion + options`; `LBC_VERSION_TARGET` in the provenance header | architecture.md:536, ADR 0031, roadmap.md:96 | 2026-08-19 | refuted | `BytecodeBuilder::getVersion()` returns 11/12/13/100 instead of TARGET under four FastFlags (`BytecodeBuilder.cpp:1474-1488`), and compiler behaviour reads flags at compile time (§4.6, §5) | The effective FastFlag set belongs in both the cache key and the provenance header |
| U-50 | The counting/cap allocator can be installed on the existing VM | architecture.md:581-587 | 2026-08-19 | refuted | There is no `lua_setallocf` at this pin — only the getter (`lua.h:471`); the allocator is fixed at `lua_newstate`, and the host currently calls `luaL_newstate` (`engine/app/src/script_host.cpp:104`) (§4.1) | Switching to `lua_newstate` is a prerequisite for the memory-cap work; `ud` is the only per-VM channel |
