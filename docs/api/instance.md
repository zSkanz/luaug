<!-- GENERATED by api/generator/gen_reference.luau from api/defs/*.api.luau. Do not edit. -->

# Instance

- **Not creatable**: `Instance.new` refuses it
- **Abstract**: it exists to be inherited from

The root of the class hierarchy: a node in the tree, carrying a name, a parent, attributes and tags.

**Members below are the ones this class DECLARES.** Everything its base
offers is on the base's page, which is what keeps one added member on
`Instance` from being a change to every page here.

## Properties

| Name | Type | Default | Access | Description |
|---|---|---|---|---|
| `ClassName` | `string` | — | read-only | The name of this instance's class; typeof answers Instance whatever the class, so this is what tells two classes apart. |
| `Name` | `string` | — | read/write | This instance's name; siblings may share one, and renaming into an awaited name satisfies a WaitForChild waiter exactly as parenting a new child does. |
| `Parent` | `Instance?` | — | read/write | The instance this one hangs under, nil when unparented; assigning it appends this instance last among its new siblings, assigning the current parent again changes nothing, and assigning after Destroy raises. |

## Methods

### `AddTag(tag: string)`

Adds a tag to this instance, a no-op when it is already present; tags are pure instance state, so a nil-parented instance still carries and is found by them.

### `Clone(): Instance`

Deep-copies this subtree unparented -- children, properties, attributes and tags at every depth -- rewiring object references that point inside the copy and leaving those that point outside it on the originals.

### `Destroy()`

Removes this instance and its children from the tree synchronously, clears its tags, locks Parent to nil and defers Destroying; the handle stops resolving at the end of that drain.

### `FindFirstAncestor(name: string): Instance?`

The nearest ancestor with that name, searching upward from Parent; the instance itself is never a candidate.

### `FindFirstAncestorOfClass(className: string): Instance?`

The nearest ancestor whose ClassName is exactly className, searching upward from Parent; the instance itself is never a candidate.

### `FindFirstChild(name: string): Instance?`

The first direct child with that name in document order, or nil; there is no recursive form in v1, so depth is a GetDescendants loop.

### `FindFirstChildOfClass(className: string): Instance?`

The first direct child whose ClassName is exactly className, or nil; asking for BasePart therefore never finds a Part.

### `FindFirstChildWhichIsA(className: string): Instance?`

The first direct child that IsA className, or nil; matching runs through the class hierarchy, so abstract base names are accepted.

### `GetAttribute(name: string): (string | number | boolean | vector | CFrame | Color3 | Vector2 | UDim | UDim2 | Rect)?`

The value of that attribute, or nil when it has never been set; attribute names are case-sensitive.

### `GetAttributeChangedSignal(name: string): Signal<()>`

The signal fired when that attribute changes; any name is accepted, because an attribute that has never been set is a reasonable thing to wait for.

### `GetAttributes(): { [string]: string | number | boolean | vector | CFrame | Color3 | Vector2 | UDim | UDim2 | Rect }`

A fresh table of this instance's attributes, which the caller owns.

### `GetChildren(): {Instance}`

A fresh array of the direct children in child order, which is the order they were parented; the caller owns it, so destroying while iterating is safe.

### `GetDescendants(): {Instance}`

A fresh array of every descendant in document order -- child order taken depth-first, preorder -- which is the order the Find family tie-breaks on.

### `GetPropertyChangedSignal(name: string): Signal<()>`

The signal fired when that property changes -- the same object on every call, carrying no value so its type stays independent of the property's; a name the class does not have raises.

### `GetTags(): {string}`

A fresh array of this instance's tags, which the caller owns.

### `HasTag(tag: string): boolean`

Whether this instance currently carries that tag.

### `IsA(className: string): boolean`

Whether this instance is of that class or descends from it; a string naming no class answers false rather than raising, because the point is to test names you do not trust.

### `IsAncestorOf(descendant: Instance): boolean`

Whether descendant sits anywhere below this instance; read strictly, so an instance is not its own ancestor.

### `IsDescendantOf(ancestor: Instance): boolean`

Whether this instance sits anywhere below ancestor; read strictly, so an instance is not its own descendant.

### `RemoveTag(tag: string)`

Removes a tag from this instance, a no-op when it is absent.

### `SetAttribute(name: string, value: (string | number | boolean | vector | CFrame | Color3 | Vector2 | UDim | UDim2 | Rect)?)`

Sets that attribute, or removes it when value is nil; a table, an Instance or a function raises and leaves any previous value in place, and an empty name raises.

### `WaitForChild(name: string, timeout: number? = nil): Instance?`

**Yields.** The calling thread parks until it completes.

Parks the caller until a child of that name exists here and returns it, returning immediately when one already does; with a timeout, expiry returns nil, and without one the wait is unbounded and warns after five sim-seconds.

## Events

Every signal here is **deferred** (ADR 0015): a handler runs at the next
drain point, never inside the call that fired it.

### `AncestryChanged(instance: Instance, newParent: Instance?)`

Fired on a moved instance and then on each of its descendants in document order, carrying the instance whose ancestry changed and its new parent, nil when it was unparented.

### `AttributeChanged(name: string)`

Fired when any attribute of this instance changes, carrying the attribute name.

### `ChildAdded(child: Instance)`

Fired after an instance is parented to this one, carrying that child.

### `ChildRemoved(child: Instance)`

Fired after an instance stops being parented to this one, carrying that child.

### `DescendantAdded(descendant: Instance)`

Fired after an instance is added anywhere below this one, carrying that descendant.

### `DescendantRemoving(descendant: Instance)`

Fired as an instance leaves the subtree below this one, carrying that descendant; leaving is fully observed before arriving.

### `Destroying()`

Fired once when Destroy is called, carrying nothing; the tree mutation has already happened and only the signal is deferred.
