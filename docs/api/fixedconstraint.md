<!-- GENERATED by api/generator/gen_reference.luau from api/defs/*.api.luau. Do not edit. -->

# FixedConstraint

- Inherits [`Constraint`](constraint.md)
- Created with `Instance.new("FixedConstraint")`

A joint with no freedom at all: two bodies the solver treats as one rigid assembly.

**This is the one a `Weld` is not.** A weld moves its second part and asks nothing; this hands both to the solver, so a heavy crate fixed to a light one makes a heavy pair and a force applied to either moves both. Use a weld to attach something to a character, and this to build an object out of parts.

**Members below are the ones this class DECLARES.** Everything its base
offers is on the base's page, which is what keeps one added member on
`Instance` from being a change to every page here.
