<!-- GENERATED by api/generator/gen_reference.luau from api/defs/*.api.luau. Do not edit. -->

# ModuleScript

- Inherits [`BaseScript`](basescript.md)
- Created with `Instance.new("ModuleScript")`

Luau that is REQUIRED rather than run. It never starts on its own; `require(module)` evaluates it once and every later require of the same instance gives back the same value, which is what makes it the place shared code goes.

**Members below are the ones this class DECLARES.** Everything its base
offers is on the base's page, which is what keeps one added member on
`Instance` from being a change to every page here.
