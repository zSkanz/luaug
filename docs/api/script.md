<!-- GENERATED by api/generator/gen_reference.luau from api/defs/*.api.luau. Do not edit. -->

# Script

- Inherits [`BaseScript`](basescript.md)
- Created with `Instance.new("Script")`

Luau that RUNS. Every enabled Script in the world is started on its own coroutine when the world does, and one that is not in the world does not run -- which is the whole difference between storing a script and using it.

**In the editor, "when the world does" means when you press play** (ADR 0058). Opening a project mounts every script -- they are in the tree, `Source` is editable, a tab can open one -- and starts none of them, so a project shows what its scene holds and nothing a file scope built. Stop tears the VM down, which is what makes a second play the same as the first. Every other way of running this engine starts scripts at boot.

**Members below are the ones this class DECLARES.** Everything its base
offers is on the base's page, which is what keeps one added member on
`Instance` from being a change to every page here.

## Properties

| Name | Type | Default | Access | Description |
|---|---|---|---|---|
| `Enabled` | `boolean` | `true` | read/write | **Whether this script's threads are resumed.** It decides nothing else (ADR 0059).<br><br>False at the moment scripts start means this one never starts. Writing it afterwards takes effect at the next deferred drain: **false to true STARTS it**, on its own coroutine, file scope running now against the world as it is -- a start and not a resume, so a script that ran, was disabled and is enabled again runs its file scope a second time. **True to false stops resumption**, and that is all it does: a queued `task.defer`, a `task.delay` coming due and a signal fire are all discarded when they come up, and a thread already executing runs to its next yield, because a coroutine cannot be preempted.<br><br>**Connections are NOT disconnected**, nothing it made is undone and nothing it built is removed. They stop being invoked because nothing resumes the threads that would run them; disconnect them yourself if that is what you need. Anything larger would answer whether a connection dies with the thing its closure captured, which is a separate question and not one a property setter may decide.<br><br>Several scripts enabled by one tick start in document order. |
