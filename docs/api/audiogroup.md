<!-- GENERATED by api/generator/gen_reference.luau from api/defs/*.api.luau. Do not edit. -->

# AudioGroup

- Inherits [`Instance`](instance.md)
- Created with `Instance.new("AudioGroup")`

A mixing bus (§2.1, §2.2). A `Sound` whose `Group` names one has its volume multiplied by the group's, which is how "turn the music down" is one property rather than a loop over every music track.

Named `AudioGroup` rather than `SoundGroup` for the reason §2.5's divergence 19 gives: the service is `AudioService`, and one prefix across a family beats two.

**Members below are the ones this class DECLARES.** Everything its base
offers is on the base's page, which is what keeps one added member on
`Instance` from being a change to every page here.

## Properties

| Name | Type | Default | Access | Description |
|---|---|---|---|---|
| `Volume` | `number` | `1` | read/write | A multiplier, not a level in decibels: 0 is silent, 1 is unchanged, and above 1 is louder and will clip if the material was already near full scale. Nesting is not supported -- a group parented to a group is just an instance parented to an instance, and only a `Sound`'s own `Group` is read. |
