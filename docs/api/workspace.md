<!-- GENERATED by api/generator/gen_reference.luau from api/defs/*.api.luau. Do not edit. -->

# Workspace

- Inherits [`Instance`](instance.md)
- A **service**: reached with `game:GetService("Workspace")`
- **Not creatable**: `Instance.new` refuses it

The root of the 3D scene, reached through the `workspace` global as well as through `game:GetService`. Whatever is parented under it is in the world and whatever is not, is not.

**Members below are the ones this class DECLARES.** Everything its base
offers is on the base's page, which is what keeps one added member on
`Instance` from being a change to every page here.

## Properties

| Name | Type | Default | Access | Description |
|---|---|---|---|---|
| `CurrentCamera` | `Camera?` | — | read/write | The camera the world is rendered from. Nil renders nothing rather than falling back to a camera the engine invented, because a view nobody asked for is harder to debug than a black frame that says why. |
| `Gravity` | `vector` | `vector.create(0, -9.81, 0)` | read/write | The acceleration applied to every unanchored part, in metres per second squared. SI and signed, so the default points down; a game wanting moon gravity scales it rather than reaching for a separate multiplier. |

## Methods

### `GetBodiesInBox(cframe: CFrame, size: vector, params: RaycastParams? = nil): {BasePart}`

Every part overlapping an oriented box, as a fresh array in a stable order -- one entry per part however many of its surfaces are inside. `size` is the full extent, matching `BasePart.Size`.

### `Raycast(origin: vector, direction: vector, params: RaycastParams? = nil): RaycastResult?`

The nearest thing a ray from `origin` hits, or nil. The direction is NOT normalised: its length is how far the ray reaches, so `direction * 100` is a hundred-metre ray. A tie between two surfaces at the same distance resolves the same way on every run, because a query whose answer depends on traversal order is a replay divergence waiting for a body count to change (R10).

### `Spherecast(origin: vector, radius: number, direction: vector, params: RaycastParams? = nil): RaycastResult?`

The same question with a sphere swept along the ray instead of a point. This is what a camera boom or a character's ground check wants: a ray slips through a gap narrower than the thing it is standing in for.
