# Defect register

**Append-only. Numbered without gaps. Nothing is ever deleted from this file.**

It exists because three human-reported defects were removed from `PROGRESS.md`
while it was being rewritten to close M4 — not archived, removed — on the day
the human was being asked to sign that milestone off. A milestone close is
exactly when a ledger is least able to afford losing its open items and most
likely to: the file is rewritten wholesale, and a bullet that disappears leaves
nothing behind.

A row cannot disappear the same way. `scripts/gates/docs-lint.sh` checks that
the ids run `D001`, `D002`, … with no holes and no duplicates, so a deleted row
is a gap the gate names, and an edited row is a diff a reviewer sees. That is the
whole mechanism: not a database, a shape a check can hold.

## What goes here

Defects **observed** — by a human using the engine, or by the agent finding one
in something that already shipped. Not a backlog, not a design list, not
milestone scope: those live in `docs/roadmap.md`. If nobody has seen it go
wrong, it is not a defect yet.

`PROGRESS.md` still carries whatever is being worked on right now, in prose,
with the reasoning. This file carries the fact that it exists.

## Format

One row per defect. `State` is one of:

- `open` — reproducible and unfixed.
- `fixed` — corrected, with the commit that did it.
- `not-a-defect` — investigated, and the behaviour is correct. Kept, because a
  defect closed silently gets re-reported.
- `scheduled` — real, understood, and deliberately owned by a later milestone.
- `quarantined` — a CHECK that flaked twice (MASTER_PROMPT.md §12). It still
  runs, still measures and still reports; it no longer gates. The row says what
  has to replace the instrument, because a quarantine with no successor named is
  a deletion somebody has not admitted to yet.

| Id | Reported | What | State | Where |
|---|---|---|---|---|
| D001 | human, 2026-08-20 | `BasePart.Transparency` changes nothing | fixed | `e86f9b0b` — the value could not reach the renderer at all; `DrawItem` had no field for it. Sorted blended pass, M4.5 |
| D002 | human, 2026-08-20 | The sun never moves and shadows never lengthen | fixed | `4e4e3fc2` — `Lighting` was created on first `GetService`, after the host cached its id; a boot service now |
| D003 | human, 2026-08-20 | The sun's shadow flickers | fixed | `27c47549` — the shadow texel grid slid with the camera; snapped to whole texels. The rotational half is named and deliberately out of M4.5 |
| D004 | human, 2026-08-20 | A crash while dragging `Size`/`CFrame` in the inspector, no log | fixed | **Not reproduced, and two halves ruled out.** The write path was driven through zero, negative, 1e30 and infinity for 32 frames with a render extraction each time and did not fault (`world_host_tests`); the window was minimized and restored 25 times over 900 windowed frames and the host exited 0. What remains is the ImGui half. The handler and log file that would have captured it exist as of `7cc1c77c` **Closed 2026-08-23 on the reporter's word**: the person who filed it says it no longer happens. Nothing here reproduced it and nothing here fixed it on purpose, so what closed it is almost certainly one of the changes that rebuilt this panel between M4.5 and E2 -- the inspector's write path went through a gesture, the selection became a set, and the row layout was rebuilt around one height. The row stays because the register is append-only and a deleted id is a hole the docs gate names; what it no longer is, is OPEN. **If it returns, it returns with evidence**: the crash handler is in place and the report should carry `luaug-crash-<pid>.dmp` and `luaug.log` from beside whatever was being run |
| D005 | human, 2026-08-20 | The F3 panel is unreadable while running | fixed | `e7aa645f` — sampled at 4 Hz and held, worst frame beside the mean |
| D006 | human, 2026-08-20 | "go" on `RunService.Parent` crashes the host | fixed | `a0e41ac1` — an editor trusted the declared type over an absent value |
| D007 | human, 2026-08-20 | F5 in the editor launched an unrelated extension | fixed | `.vscode/launch.json`, four configurations against the real binaries |
| D008 | human, 2026-08-20 | Type errors on `engine.d.luau` in the editor | not-a-defect | `selene`, from an extension outside this project's toolchain. `.vscode/settings.json` did also point luau-lsp at a deleted file, and that is fixed |
| D009 | human, 2026-08-20 | The Android triangle is stretched in portrait | not-a-defect | The sample's vertices are in NDC, so it fills whatever aspect the device has. Recorded at the M4 device checkpoint |
| D010 | agent, 2026-08-20 | The capture backend recorded uniform blocks by SIZE, so the blocking render gate could not see a matrix, a light or a material colour | fixed | `4e4e3fc2` — a content digest, quantized onto the same grid the stream already uses |
| D011 | agent, 2026-08-20 | Debug wire boxes and `DebugService` lines were drawn in world space through a camera-relative view-projection, displacing every one by the camera's distance from the origin | fixed | `e86f9b0b` — the buffer is rebased once, after extraction |
| D012 | agent, 2026-08-20 | `Model:PivotTo` was `PrimaryPart.CFrame = cf` under a new name: `PivotOffset` did not exist, so nothing could hinge | fixed | `721f6194` — `PVInstance` with a real pivot offset |
| D013 | agent, 2026-08-20 | `modelPivot` said "the centre of the extents box" and averaged part positions, which is a different point whenever parts differ in size | fixed | `721f6194` |
| D014 | agent, 2026-08-20 | `schema.luau` documented `Property.Native` backwards — absent meant unbacked, where the generator derives a name and `Native = ""` means unbacked | fixed | `98a5a866` |
| D015 | agent, 2026-08-20 | `fopen_s`/`_wfopen_s` open for exclusive access, so nothing could read the engine's log while it ran | fixed | `7cc1c77c` — `_wfsopen` with `_SH_DENYWR` |
| D016 | agent, 2026-08-20 | `BindToClose` has no capped grace period: a close handler that yields is cut off at the next drain rather than waited for. `architecture.md` §app promises the cap | fixed | `c18f12f4` — the host keeps ticking while a handler is parked, up to a wall-clock cap, and warns when the cap runs out. Both halves tested: a handler that yields and finishes, and one that never does |
| D017 | agent, 2026-08-20 | The `DebugShell` has no memory-category table and no log/REPL pane, both named by `architecture.md` §app | fixed | **M6.** The memory table is §6's plan made real: every entry script gets a category from the 32..255 pool, assigned by CHUNK NAME so a hot reload reuses the row and the number stays comparable across one, and `lua_setmemcat` on the script's own thread is what attributes its allocations. The table shows only categories that hold something -- a table where seven rows are permanently zero is a table nobody reads.

The console pane is both halves. The log is a bounded ring the overlay fills from a sink that CHAINS to the console one, so the pane and the log file both get every line; a shell that ate the log would be the last place anybody looked for it. The REPL is `runSource` with a name and the reserved category 7 -- deliberately the same path into the VM an entry script takes, because a second path would be a second thing to keep honest. Four cases cover what is testable without a window: a chunk that runs, one that does not compile and leaves the VM alive, a per-script row with real bytes, and two scripts getting two categories with a reload reusing one |
| D018 | agent, 2026-08-20 | `luaug_net_tests` hung once on Windows, holding CTest until it was killed; a re-run passed | fixed | **Reproduced 2026-08-21 during M7 and root-caused rather than quarantined.** §12 would have allowed a quarantine on this second occurrence; the hung process answered the question before that was needed. It held exactly one socket -- `127.0.0.1:65533` in `LISTEN`, with nothing established -- and two threads, both waiting. `tests/loopback_server.h` called `::accept` with **no deadline**, and `~LoopbackServer` joined that thread BEFORE closing the listener. So the state a FAILING test leaves behind -- a client that never connected -- was being converted into a suite that hangs forever, which is why it presented as a flake: the underlying assertion failure was invisible. `Connection` has carried a ten-second read deadline since it was written, with a comment saying a suite that hangs tells you less than one that fails; the step BEFORE it had no such guard. **Fixed**: accept polls `select` with a fifty-millisecond tick against a ten-second deadline, and the destructor sets a stop flag before joining, so a server whose client never arrived reports "no client connected within ten seconds" instead of waiting for the CI runner to time out. Break-verified by a case that stands a server up and never connects: it reports the deadline rather than hanging, and with the deadline removed that case is the hang |
| D019 | CI, 2026-08-20 | `engine/app/src/engine.cpp` used `std::sort` with no `#include <algorithm>`: it compiled on Windows and in the Tier-2 container by reaching the header transitively, and failed on the CI runner's libstdc++ | fixed | `main` was red for one push. Found because the run the milestone push cancelled had already reported it. Neither local tier can see this class of defect — the transitive graph differs by libstdc++ version |
| D020 | CI, 2026-08-20 | `rokit install` resolves every pinned tool through api.github.com unauthenticated, which is 60 requests an hour shared across the runner's IP; it returned 403 and killed a job whose build and 25 tests had already passed | fixed | `rokit authenticate github --token` with the workflow's own `GITHUB_TOKEN`, before `install`. Subcommand read off the pinned 1.2.0 binary's `--help` rather than from documentation for another version |
| D021 | agent, 2026-08-20 | A property that refuses a value for being out of RANGE reports the key for its TYPE: `PhysicsService.FixedTimestep = 1/10` raises "it takes a number" about a number | fixed | M6 -- the IDL gains `Property.ErrorKey`, a per-property override for `errKeyOnInvalidSet`, and twenty ranged properties carry one. A setter still answers with a bool, so the key states the whole DOMAIN rather than the failure: "it takes a tick length from 1/240 to 1/30" is equally true of `"fast"`, which is why the override subsumes the type key rather than competing with it. Seven shared keys by shape, not one per property. Break-verified -- with the override ignored, five conformance cases and one seam case go red |
| D022 | agent, 2026-08-20 | A `Part` never reaches the solid renderer: only a `MeshPart` does, so every primitive in the world -- crate, ramp, floor, character -- draws as a debug wireframe box | fixed | **M6**, by human decision on 2026-08-20 — and this row said M7.5 until 2026-08-21, which is the correction. The roadmap and the M6 brief had carried it as M6 scope since `53e0af7c`, hours before this row was written, so the two sources disagreed: that is the state `Transparency` was in for a milestone, and it is worse than either answer. The reasoning for M7.5 was sound — it moves every render golden, and M7.5 owns how this engine renders — and it was outweighed by what deferring costs: `examples/04-obby` ships this milestone and M7's whole streamed world would be built and reviewed in wireframe. True since M2 and unchanged by M4, which shipped mesh rendering; found by looking at the M5 deliverable's first screenshot, where a whole physics playground is outlines. The fix is a built-in primitive mesh per `Enum.PartShape` scaled by `Size` **Closed in M6**, which is where the roadmap had scheduled it: every `Enum.PartShape` has a built-in primitive mesh scaled by `Size`, and `examples/04-obby` shipped on it. This row read `scheduled` until 2026-08-21 -- a ledger that says a defect is still coming after the milestone that closed it was signed off is worse than no ledger, and the ledger in `PROGRESS.md` had it right the whole time. |
| D023 | CI, 2026-08-20 | `scripts/gates/clang-format.sh` was committed without the execute bit, so the CI job that runs it directly failed with exit 126 while both local tiers were green | fixed | `4efa55a3` — `git update-index --chmod=+x`. `localgate.ps1` invokes every gate script as `bash <script>`, which does not care about the mode; `ci.yml` runs them the way a shell script is run. A `chmod +x` on a Windows checkout is not the same thing as a mode in the index, and the two look identical in `ls -l` |
| D024 | CI, 2026-08-20 | The determinism gate compared a PHYSICS scenario's committed trace against a build made by a different compiler, and went red on the CI Windows runner at tick 600 of 3,600 | fixed | ADR 0025's guarantee is same-BUILD, and every scenario before M5 was integer and tree state that happens to survive a compiler change. `scenario.json` gains `sameBuildOnly`: such a scenario carries no trace and is run three times, which is the roadmap's own wording for the M5 gate. The behaviour half is covered by seven tolerance-based assertions inside the scene. `churn` is anchored to keep its cross-build trace, since it was never a physics test |
| D025 | human, 2026-08-20 | A `CharacterBody` ignores `CollisionGroup`: a wall in a group its group is set never to collide with still stops it, and two characters in such a group still block each other | fixed | Reported as "two characters pass through each other", which did not reproduce — each character carries an inner rigid body and that is what the other one's sweep finds (Finding 15). The real defect was underneath: `CharacterVirtual::ExtendedUpdate` was handed a default-constructed `ObjectLayerFilter`, which accepts every layer, so the one thing a player controls was the one thing outside the collidability matrix. It now uses the world's own `GetDefaultLayerFilter`/`GetDefaultBroadPhaseLayerFilter` against the character's layer. Four seam cases and two conformance cases, verified by breaking both the filter and the inner body |
| D026 | agent, 2026-08-20 | The capture gate records an `upload`'s SIZE and not its contents, so every vertex a frame draws is invisible to the blocking render gate | fixed | Found while recording M6's UI goldens: the two resolutions differ in their scissors and viewports, and would have been byte-identical if only the quads had moved. **The same shape as M4.5's finding** -- that gate recorded the size of every uniform block and never its contents, and the fix reached `bindUniforms` and not `upload`, which was carrying debug-line geometry even then. A content digest is the obvious fix and is not free: raw float bytes can differ in the last bit between compilers, and a cross-BUILD comparison of them is exactly what D024 was. What holds the line meanwhile: `luaug_ui_tests` asserts exact rectangles, the two goldens catch anything that moves a scissor or a viewport, and the gate record carries a screenshot **Fixed 2026-08-21, and the row's own warning was right.** An `upload` and an `uploadTexture` now record what they carried. The obvious fix -- an FNV of the quantized words, the same digest `bindUniforms` has used since M4.5 -- was written first and the Tier-2 run refused it, on exactly the buffers whose contents are COMPUTED rather than read from a file: the primitive-shape meshes and the UI vertices. **A hash has no tolerance**, so a value one bit apart on either side of a quantization boundary hashes to two unrelated numbers, and over a few thousand floats something always lands on a boundary. Coarsening the grid moves the boundaries rather than removing them; it was tried and it made a third buffer fail. What ships is continuous instead: the MEAN of the float words and the mean step between consecutive ones, on the four-decimal grid the rest of the stream already uses, beside an exact hash of the words that were never floats. Means are what buy the margin -- the two compilers disagree by less than 1e-4 on a whole buffer, and moving one quad by four tenths of a pixel moves the mean by 1.4e-2, which is the break test. **And the bits cannot tell you which words are floats**: a `UiVertex` colour with alpha 127 reads as a perfectly ordinary normal float of 1.7e38, which overflowed the printed sum and made the two tiers disagree about what an out-of-range conversion produces. The split is by MAGNITUDE, and packed data is bit-identical on every compiler, so sending it down the exact path costs nothing. Four tests in `rhi_capture_tests.cpp` pin all of it. 
| D027 | human, 2026-08-21 | A character standing on a moving platform does not move with it: `examples/04-obby`'s waver slides out from under the player, who stays where they were | fixed | **Two halves, and the second is wider than the platform.** (1) `GetGroundVelocity` — Jolt's answer to "how fast is the body I am standing on moving" — appears **zero times** in this engine; `ExtendedUpdate` is handed only `mWalkStairsStepUp` and `mStickToFloorStepDown`, so a character never inherits its ground's motion. (2) And it would read zero if it were called: `jolt_physics.cpp:668` moves kinematic bodies with `SetPositionAndRotation`, which TELEPORTS. Jolt derives no velocity from a teleport; `MoveKinematic` is the call that takes a target and a delta and computes one. So **every script-moved kinematic body in the engine currently has zero velocity**: a closing door does not push, a piston does not launch, a conveyor does not carry. Scheduled M6, where it was found — the roadmap named the obby's moving platforms "a deliberate integration stressor" and this is the stress showing. The origin is M5's seam rather than M6's work | **Fixed 2026-08-21**, both halves. `setBodyTransform` records a kinematic body's target and `step` spends it through `MoveKinematic` against the tick the simulation is about to take -- the fixed timestep and never a wall clock (R10) -- so a moved kinematic body now HAS a velocity; and `moveCharacter` adds `GetGroundVelocity()` to the character's own, while grounded only, because carrying the last platform's velocity through a jump would launch it. Three seam cases: a kinematic body arrives with a velocity, a capsule rides a platform there and back without asking for a metre of it, and a capsule in mid-air inherits nothing. Break-verified -- with the inheritance removed the rider stays at zero while the platform reaches six
| D028 | agent, 2026-08-21 | `Touched` does not fire for a `CharacterBody`'s SIDE contacts: a character that walks into a wall, a flagpole or a trigger it does not stand on fires nothing | fixed | Found building `examples/04-obby`, whose finish was a pole the player pressed against and which therefore never fired. The ground half is fixed in the same commit -- `writeCharacters` diffs the surface under the character's feet against last tick's and fires `Touched`/`TouchEnded` on both parts, which covers checkpoint pads, hazards and pressure plates. The remainder needs the character's non-ground contacts, which are not on the `IPhysics3D` seam: `CharacterState` carries the ground body and nothing else, and `CharacterVirtual::GetActiveContacts` would have to reach it. That is a seam widening rather than a fix, so it is a row rather than a commit. What holds the line meanwhile: the obby's finish is a PAD you stand on, and that reads better than a pole anyway **Fixed 2026-08-21, and the row was wrong about what it would cost.** It said a seam widening; the seam did not move at all. `CharacterVirtual::GetActiveContacts` is read after the step, each contact turned into a pair keyed by our own handles, and the SAME diff `buildContactEvents` makes for rigid pairs decides Began and Ended. `ContactEvent` is unchanged, and so is the scene glue that consumes it -- `publishContacts` reads only the two user-data fields, which is exactly what a character has. The ground half MOVED here with it, and that is the point rather than a side effect: two mechanisms for one signal is how the two come to disagree, so `writeCharacters` no longer diffs the surface under the feet and keeps only `Landed`, which is about ground STATE. Two details that are not obvious: a predictive contact -- one the sweep found ahead of the character and never reached -- is filtered on `mHadCollision`, and the character diff makes NO sleep exception where the rigid one must, because a `CharacterVirtual` is never put to sleep by the solver and its inner body is created with `mAllowSleeping = false`. Three seam tests and one conformance case, each verified by breaking: with the collection removed, exactly those three go red. |
| D029 | human, 2026-08-21 | A `UIListLayout` with `HorizontalAlignment.Center` does not centre its column: every child in `examples/04-obby`'s menu sits 16 px left of centre | fixed | The cross-axis metric is the wrong one. `layout.cpp:275` computes `lineCross` as the WIDEST CHILD in the line, and `:294` then centres each child against that -- so a column whose children are all the same width gets `lineCross - cross == 0` and the whole column is placed at `content.min.x`, the left edge. The menu's children are `UDim2.new(1, -32, ...)`, 32 px narrower than the panel, which puts their centre exactly 16 px left of the panel's: the observed number is half the inset. The main axis is correct and uses `lineLimit`, the container's own extent (`:282`), which is what the cross axis should use when the layout does not wrap. **`Wraps = true` needs deciding rather than discovering**: whether wrapped lines centre within their own band or within the container is a real choice and this fix has to state which | **Fixed 2026-08-21**: the cross axis uses the container's own extent when the layout does not wrap, exactly as the main axis already used `lineLimit`. **`Wraps = true` is decided rather than discovered** -- a wrapped line centres within its own BAND, because wrapped lines stack along the cross axis and centring one against the whole container would put every line on top of every other. Four cases, including the wrap one and a horizontal row, whose cross axis is the other one and which a half-fix would have missed. Break-verified -- with `lineCross` back as the metric, three go red
| D030 | human, 2026-08-21 | `UICorner` does nothing: every frame and button draws square, and `CornerRadius` has no consumer anywhere | fixed | `components.h:452` declares `cornerRadius` and that declaration is its ONLY occurrence outside the generated accessors; neither `engine/ui` nor `shaders/src/ui2d.hlsl` mentions a radius, a corner or a rounding. The class doc promises the opposite -- "rounds its parent's corners; it changes the DRAWING and not the layout or the hit test". **And the marker built to make this state impossible was not applied**: the property is not `Inert`, so nothing in the inspector or the api-dump says it is stored-and-unread. That is the finding worth more than the fix -- `Inert` (M4.5) still depends on somebody remembering, which is the one thing it exists not to depend on. The drawing fix is a rounded-rectangle signed distance in `ui2d.hlsl`; the process fix is a check that a property with no reader is either consumed or marked | **Fixed 2026-08-21**, both halves. The drawing: `DrawQuad` carries a radius, `UICorner` is read where the background quad is built, and `ui2d.hlsl` measures a rounded-rectangle signed distance and antialiases it over one pixel of its own gradient. The geometry stays four vertices and a radius of zero costs one compare, so a rounded panel is the same draw as a square one -- and the hit test stays rectangular, which `UICorner`'s own doc promises. **The process half is `tools/repo/inertcheck.luau`**, in the Luau gate: every component field has a reader or its property is marked `Inert`. Run against the whole tree it found five more -- `CollisionFidelity` (which claimed a reader in prose that did not exist), `ScaleType`, `SliceCenter`, `ScrollBarThickness`, all now marked with M7 named -- and one field, `TextInputComponent::caret`, that named a feature nobody had written and is deleted
| D031 | human, 2026-08-21 | D027's fix is never reached: a tweened `Anchored` platform is a STATIC body, and `MoveKinematic` on a static body does nothing, so the character still does not ride it | fixed | Confirmed by playing a build newer than D027's fix. `physics_sync.cpp:122` reads `isDriven(id) ? Kinematic : anchored ? Static : Dynamic` — `Kinematic` is chosen only for a part an active `Weld` drives, so an `Anchored` part a TWEEN moves is classified `Static`. Jolt neither moves a static body nor derives velocity from one, so `MoveKinematic` receives a target and nothing happens. The example states the intent that the classification does not implement: "anchored parts whose `CFrame` a tween writes. That IS the kinematic case." **Fix, chosen by the human 2026-08-21 (option A of two):** an anchored part becomes `Kinematic` when its `CFrame` is written and returns to `Static` after N ticks without a write. Option B — `Anchored` meaning "kinematic always" — was rejected on cost: `jolt_physics.cpp:89` splits the broadphase into `NonMoving` and `Moving`, and the static layer exists precisely so it is never re-fitted; making every floor and wall kinematic would put a world of never-moving bodies into the layer that is updated each tick. Option A moves only what is written, which is the count that must be moving anyway. **Two things to get right rather than discover:** hysteresis, because a part written every other tick would otherwise oscillate between broadphase layers; and the transition cost, which this project measures rather than estimates — record it in `perf-baselines.md` with N platforms transitioning, beside the physics rows | **Fixed 2026-08-21** as the human chose: an anchored part is `Kinematic` while something is writing its `CFrame` and returns to `Static` twelve ticks after the last write. Twelve covers any driver writing at five hertz or faster and costs at most a fifth of a second of a stopped body in the moving layer; it is a TICK count, because a body's broadphase layer must not depend on how fast the machine is running (R10). **Two costs the measurement found and reasoning did not**, both fixed before the row was written: writeback went 0.03 -> 9.9 ms because Jolt reports every kinematic body as active and the mirror was copying six thousand solver transforms a tick back into components the script owns (a kinematic body is not written back now); and apply went 1.60 -> 6.96 ms because the pending-move list was deduplicated by scanning it, which is quadratic. `churn10k` settles at 7.32 ms against 4.96 before, and that increase is honest -- two thirds of its anchored parts are written every tick and are now simulated as the moving bodies they always were. `tests/bench/platforms200` and two rows in `perf-baselines.md` carry the numbers. Break-verified -- with the classification back to `Static` the conformance case reads zero
| D032 | agent, 2026-08-21 | The audio underrun counter counted the NORMAL case: a sixty-second soak on a real device reported 1,348 underruns and every one of them was the mixer working correctly | fixed | Found by running M6's own audio gate on a machine with speakers, which is the half CI cannot do. The counter incremented whenever a device callback arrived with no NEW voice frame published since the last one -- and that is most callbacks: the device asks for buffers at about 82 Hz and the simulation publishes at 60, so roughly a fifth of them legitimately mix the same voices again. **A gate that reports a catastrophe for the ordinary case is worse than no gate**, because the first thing anybody does with it is stop reading it.
| D033 | human, 2026-08-21 | `examples/05-streaming` loses frame rate steadily: 100 fps at the start, 35 fps after five minutes of flying | fixed | Reproduced before anything was touched, headless, counting instances rather than trusting the frame rate: `workspace` goes 2804 -> 4784 -> 8186 -> 13538 over 165 s of sim and never falls, across whole laps of a 60 s circuit. So it is not a hitch and not the renderer -- it is a world that only grows, and the smooth decay is the renderer iterating it. **`StreamingHost::setWorld` rebuilt the `StreamingGlue` EVERY FRAME.** The frame loop calls it at the safe point so a hot reload's new world is picked up; the glue is where the record of which instances belong to which resident chunk lives. So each frame threw that record away. The manager went on believing chunks were resident and called `evict(id)`; the newborn glue looked the id up, found nothing, and returned. Nothing was ever destroyed, and every re-entry into a chunk built a fresh folder of instances beside the last one. The fix is an early return when the world and root are unchanged -- and, on a real change, `StreamingManager::forgetResidency()`, because otherwise the first frame after a reload asks a new glue to evict chunks it never materialised: the same silent no-op, one frame wide | **Fixed 2026-08-21.** After: 1737 / 1528 / 1531 / 1685 instances, repeating exactly lap after lap -- the world became a function of the circuit position, which is what a streamed world is. Median frame 9.26 -> 2.31 ms, worst 36.08 -> 7.85 ms. **The gate that would have caught it now exists**: `ctest -R streaming_soak` flies the five-minute circuit and asserts a frame-time histogram, a declared memory ceiling, and -- the one the roadmap did not name -- that the INSTANCE COUNT FLATTENS. D033 broke neither of the first two: no frame passed 33 ms and no ceiling anyone would have set was crossed. Break-verified both ways: with the early return removed the gate is red, with it restored green
| D034 | agent, 2026-08-21 | With `--rhi=null` the streamed world never loads: 289 chunks become eleven instances, and the soak gate passes in 0.17 s | fixed | Found while wiring D033's gate, and it is the worse of the two even though nobody could see it. Content mounting, the pack mount and `streaming.load` all lived inside `ensureDebugPass`, which returns early when `device->caps().shaderFormat == Unknown`. Nothing in that block reads a shader, a format or a device: content addressing and streaming residency are decisions about files and distances. The consequence was not a broken picture but a VACUOUS PASS -- flat frame times over an empty world are exactly what a leak detector wants to see, so the gate reported a clean bill of health on a run that had done nothing | **Fixed 2026-08-21**: the block moved out of the shader gate into the boot path. The same coupling would have silently emptied the world on any machine whose shader load failed. `--soak-min-instances` landed in the same change, so the gate has to be TOLD what "the world loaded" means for a scene and cannot pass on an empty one again
| D035 | agent, 2026-08-21 | `TcpStream::connect` waits out its whole timeout on Windows when the connection is REFUSED: ten seconds to discover that nothing is listening on a loopback port that answered instantly | fixed | Found because `@std/net`'s first request took ten seconds to fail against a dead port, and the ten was suspicious -- it is the request timeout to the millisecond, which means nothing was ever detected and the deadline simply expired. `tcp.cpp:145`'s `waitFor` passes only `writefds` to `select`. That is correct on POSIX, where a resolved connect is writable whichever way it went. **Winsock reports a FAILED non-blocking connect in `exceptfds` and NEVER in `writefds`**, so the one case the code was written to detect -- ask `SO_ERROR` which way it went -- could not be reached at all. Present since M3 and invisible until now: the one caller was the dev-server connect (ADR 0035), where a slow failure looks like a server that has not started yet. The header even states the intent it was failing -- "the OS default for a refused-then-retried connect is tens of seconds of nothing" | **Fixed 2026-08-21**: `waitFor` passes an except set as well when waiting to write. Harmless on POSIX, where a descriptor turning up in both sets is still one `ready > 0` and `SO_ERROR` still says which. Break-verified against a LONG timeout and a loose bound -- twenty seconds against eight -- because how fast a refusal arrives is the OS's business (about two seconds on the machine this was found on, where the firewall drops the SYN rather than answering it) while the BUG makes the answer exactly the timeout on every machine
| D036 | agent, 2026-08-21 | A `@std/net` request that completed on the socket never came back to Luau: the worker filed nothing, forever | fixed | A fairness bug rather than a logic one, and only a frame loop could have produced it. `AsyncClient::take` is called once per frame for every outstanding ticket, and a headless run on the null backend has no frame pacing at all -- a hundred thousand lock/unlock pairs on the same mutex the worker needs for the single instant it takes to file a finished result. `std::mutex` is not fair: a thread reacquiring in a tight loop can hold it essentially continuously. The worker printed "done" and then never got back in; the run ended, `shutdown()` set the stop flag, and it returned having filed nothing. Worth noting what made it hard to see: every part in isolation was correct, and the trace showed the request completing | **Fixed 2026-08-21**: an atomic completion counter read WITHOUT the mutex, so a poll that finds nothing takes no lock at all and there is nothing left to starve. Release/acquire pairs the counter with the insert that precedes it. Break-verified by a test that is that caller -- a tight `take` loop around a real request -- which does not finish without the early out
| D037 | agent, 2026-08-21 | `luaug_jobs_tests` hangs intermittently: the whole local gate stops with the process alive, at zero CPU, and never finishes | fixed | Caught in the act rather than reasoned about. The stuck process had exactly TWO threads, both `Wait`, both at zero CPU -- and a jobs test with a four-worker pool should have five. Two means the main thread plus ONE worker, which points at `join` and not at the work. A minidump confirmed it before anything was touched. **`shutdown` stored `running = false` -- half of the worker's wait predicate -- without `p.mutex`, the very mutex its `condition_variable` waits on.** A worker that had evaluated the predicate (still running, nothing ready) and had not yet atomically released the lock and registered on the variable missed the `notify_all` entirely, slept through the shutdown, and left the main thread in `join` forever. One instruction wide, and it had been there since M7's Phase 0. **`enqueue` had the same bug and is fixed in the same commit**: `readyCount` is the predicate's other half and it too was an atomic incremented outside the graph lock. That one was found by inspection while chasing this, and honesty requires the distinction -- the shutdown race reproduces on demand and the enqueue race did not, in ten thousand attempts. It is the same defect by construction, and a condition variable whose predicate reads state changed outside its mutex is wrong whether or not a test has caught it yet | **Fixed 2026-08-21**: both stores move under `p.mutex`. The cost is one uncontended lock acquisition per enqueue and one per shutdown; the graph lock was already taken twice per job. Break-verified, and this is the strong kind: with the shutdown fix reverted the new regression case hangs FIVE runs out of five, and with it restored it passes in 0.29 s. **The comment that was there made this harder to find.** It argued that an increment before a notify meant a job "cannot be missed", which is true of the window it was thinking about -- between a failed steal and the wait -- and says nothing about the window between evaluating the predicate and registering on the variable. A confident comment about the wrong window reads as a settled question
| D038 | agent, 2026-08-21 | The M7 soak gate fails on the Tier-2 container: twenty frames spend more than 33 ms INSIDE STREAMING, the worst 130 ms | fixed | Found by the gate itself on its first honest run, and only because the hitch check had just been changed to measure time attributed to streaming rather than whole frames -- a whole-frame check on a shared runner would have been dismissed as noise. Measured rather than guessed, in four passes: the cost was not IO completion (0.02 ms), not eviction (0.00 ms), not the rebase, but the LOAD path; inside that, `beginRead`; inside that, two separate things. **The first is `SDL_LoadFileAsync`, which opens the file SYNCHRONOUSLY and only reads asynchronously.** On a local disk that open is microseconds; on the container's bind mount it measured 5 to 141 ms, on the frame thread, inside the streaming pump. An async IO service whose submit path can block for a tenth of a second is not one, and the machines where that bites -- a network share, a phone's storage, a cold cache -- are exactly the ones a streaming world exists for | **Fixed 2026-08-21**: admission moved to a dedicated submitter thread. `readFileAsync` queues and returns; the submitter picks the most urgent request, RELEASES the mutex, and does the blocking open; `pumpIo` only harvests. `platform::shutdown` now stops the service -- nothing did, and a joinable `std::thread` in a static is `std::terminate` at exit rather than a leak. Three async-IO tests had to say what they had been assuming: with admission on its own thread, "submitted" and "in flight" are two moments with a scheduler between them, so a case reasoning about QUEUE ORDER now waits for the first read to be admitted before queueing the rest. Worst streaming pump went 130 ms to 68 ms; D039 is the other half
| D039 | agent, 2026-08-21 | Every streamed chunk is read from disk TWICE, and the first read is synchronous on the frame thread | fixed | The remainder of D038's measurement, and the worse of the two. `ContentMounts::resolve` decided whether a loose file existed by calling `platform::readFile` -- **reading the entire file** and throwing the bytes away -- immediately before the caller started an asynchronous read of the same file. The comment above it was right about why it could not use `std::filesystem::exists` (inside an APK the content directory is zip entries and no path a C runtime can stat) and drew the wrong conclusion from it: existence has to mean "can it be OPENED", not "can it be read". Twice the IO, the first half of it synchronous, in the pump | **Fixed 2026-08-21**: `platform::fileExists` opens and closes through SDL, which keeps the APK case working at a fraction of the cost. And the pump does not resolve at all any more -- `StreamingHost` resolves every chunk in the index ONCE when the index is read, because whether a file exists was settled when the project was built and is not a question for a frame. A missing chunk is now reported at boot rather than three minutes into a fly-through. After both fixes the Tier-2 soak passes: no frame spends more than 33 ms inside streaming, p99 whole-frame 4.6 ms. **And the crash after the fix was M6 Finding 17 for the THIRD time**: `StreamingHost` gained members, some object file did not get rebuilt, and an access violation landed in code that had nothing wrong with it. `--clean-first` was the whole answer, again
| D040 | agent, 2026-08-21 | Incremental Windows builds silently reuse objects compiled against an OLD header: a header change rebuilds nothing at all | fixed | **This is what M6 Finding 17 always was.** Four times across two milestones an access violation landed in code that had nothing wrong with it, immediately after a struct gained members, and four times `--clean-first` was the answer -- recorded each time as a pattern to remember rather than a defect to fix. The fifth time, somebody touched `physics_sync.h` and ran the build: `ninja: no work to do`. Ninja learns which headers an object depends on by matching a PREFIX against `/showIncludes` output, and CMake writes that prefix into `rules.ninja`. On this machine it reads `msvc_deps_prefix = Observacao: incluindo arquivo:` -- a LOCALISED MSVC -- written as UTF-8 (`43 C3 A7`), while the compiler emits it in the console codepage. The two never match, so every `/showIncludes` line is treated as ordinary output and **not one header dependency is ever recorded**. Every incremental build on a localised toolchain has been unreliable for the whole project. `VSLANG=1033` does not help: forcing English needs the English language pack, and the installed one is Portuguese | **Fixed 2026-08-21**: the build runs under `chcp 65001`, which makes the compiler emit the same UTF-8 CMake wrote. Verified directly -- with the codepage set, recompiling and then touching `physics_sync.h` rebuilds `physics_sync.cpp.obj`; without it, `ninja: no work to do`. `scripts/localgate.ps1` sets it, `bootstrap.ps1` prints it beside the vcvars line, and CLAUDE.md carries it in the list of things that waste an hour. **The lesson is not about codepages.** A recurring symptom recorded four times as a pattern is a defect nobody has looked for, and "remember to use --clean-first" is what a workaround sounds like when it has been mistaken for an explanation
| D041 | agent, 2026-08-21 | A clustered light is clipped into hard horizontal bands: the pool of light on a floor stops at a straight line and resumes at the next one | fixed | Found by LOOKING at the frame, and it could not have been found any other way: the cluster assignment numbered clusters slice-major while the shader read the grid texture row-major, so every fragment looked up a cluster belonging to some other part of the screen. Both sides were internally consistent, so every unit test passed -- the ninth light was assigned, the runs were contiguous and ascending, the tile-Y flip was right. What was wrong was that two correct pieces disagreed about a layout neither of them named. `clusterIndexOf` is now that name, it is `constexpr`, and a test asserts it against the exact expression the shader computes. The general shape: **when two sides derive the same index independently, the index is the thing to write down, not the derivation.** |
| D042 | human, 2026-08-21 | The obby's animated `Waver` renders as a dark square from behind and a downward-pointing triangle from the front, instead of as a bending bar | fixed | **Every skinned mesh in the engine had every joint but index 0 reading past the end of its palette, since M6.** The skin stream stores joint indices as `VertexFormat::Float4`, because `rhi::VertexFormat` has no integer format and the enumeration is frozen (ADR 0037) -- and both skinned shaders declared their input `uint4`. That does not CONVERT the value, it reinterprets the bits: `1.0f` arrives as 1,065,353,216. Zero was the only index that survived, because zero has the same bits either way. Found by a human looking at the picture, then cornered by differentials rather than by reading: the palette was logged and proved to be a correct affine matrix; a step rig with no blended vertices collapsed exactly the half bound to joint 1; and forcing every vertex onto `JointMatrices[1]` in the shader produced a perfect rigid rotation, which put the fault in the INDEX and nowhere else. Fixed by declaring the input `float4` -- which is what the stream holds -- and converting explicitly at the call. **`capture_gate_skinned` could not have caught it**: it watches the joint palette, and the palette was right; what was wrong was the index a vertex used to read it. A gate that watches an input cannot see a defect in its consumer. The asset was under-specified too and is rebuilt by `examples/04-obby/tools/make_waver.py`: it was a zero-thickness plane, so back-face culling made it vanish from behind, and it declared no material, so it took glTF's own default of a perfectly rough metal. |
| D043 | human, 2026-08-21 | The instanced path draws nothing: `tests/perf/horde` renders 2,000 enemies as an empty field with only the player in it, at 22 draw calls and 3.7 ms | fixed | **The frame M7.5 measured its headline number on was drawing almost none of the scene.** `core::Mat4` stores `m[column][row]`, so the four per-instance attributes are the model matrix's four COLUMNS -- and HLSL's `float4x4(a, b, c, d)` constructor fills ROWS. Both instanced shaders handed it four columns and got the transpose, so `mul(M^T, p)` wrote a dot product of the translation into `w` and every instanced vertex left the frustum. Fixed by transposing, and by reading the cofactor basis straight off the attributes instead of out of a matrix whose indexing had just changed meaning. **Every gate the milestone had was satisfied**: draw calls fell from 15,250 to 22, which is exactly the gate the roadmap asked for; the capture goldens matched, because the command stream was correct; and the frame got eight times faster, because it was drawing nothing. Found by rendering a NEW scene of eight identical spheres and seeing an empty floor -- then confirmed on the horde, whose picture nobody had looked at. Proved by differential: with the fix, the instanced frame is BYTE-IDENTICAL to the same frame with instancing disabled; before it, 87% of pixels differed. `screenshot_gate_instanced` is now that check, standing. |
| D044 | human, 2026-08-21 | The sun's shadows break into hatched dashes as they stretch, and the pattern crawls frame to frame instead of sliding | fixed | Reported twice, with a screenshot, and reproduced exactly: `examples/02-meshes` at frame 2100, a sun 24 degrees up -- nowhere near the horizon. **It is the shadow term and nothing else**: forcing it to 1 removes the pattern completely. The flicker and the dashes are one defect, because the pattern crawls as the light moves. **What it is NOT, each ruled out by a build and a measurement**, against the shadow term isolated from the frame (0.548 as shipped): the constant depth bias at 0.3 m (no change) and at 5 m (0.410, and by then it is detaching every shadow in the scene); a slope- and texel-scaled bias, which is the correct FORM and is not the cause (0.399 at twelve texels, 0.306 at twenty-four -- where the shadow itself is gone); receiver plane depth bias, Isidoro 2006, the textbook answer (0.549, identical with and without); twice the shadow-map resolution (0.549); `kShadowDistance` halved (no change); the split lambda at 0.70 and 0.60 (0.543); ten times the caster margin (no change); the cascade blend band from 0.12 to 0.90 (0.546); a slope-adaptive filter radius (0.527, and it costs contrast); bypassing the `Gather` bilinear reconstruction (worse); switching the shadow pass off front-face culling (0.604, much worse -- that cull is earning its keep); and the content-fitted cascade committed alongside this row (0.549); and the texel-sized filter that followed it. **What the filter change DID fix is a second thing the same human reported** -- that the gradient between shadow and lit grows as the shadow stretches, and loses its resolution doing it. That one was real and is closed: the far cascade was filtering 0.8 texels, nine taps inside a single texel, so its gradient was a raw texel step 21 cm wide beside a 3.4 cm soft one in the near cascade. **The instrument that made all of that possible is worth more than the list**: binding the shadow atlas to the resolve pass and looking at it. That is what showed the cascade was spending 500 by 90 texels of a 1024 by 1024 tile on the scene -- a real defect, fixed -- and it is what proves the map itself is clean, so whatever produces the dashes happens at sampling time. What has not been tried: reading the stored depth and the receiver's depth for one dashed texel and printing the difference in metres. **The measurement has since been made and it reframes the row.** The probe reports, per pixel, how far the receiver sits behind what the shadow map stored for it, in metres. The answer is not small and it does not oscillate: the streaks are regions where the receiver is more than a METRE behind the stored depth, the lit floor beside them is more than a metre in front, and the boundary between the two is one pixel wide. **Acne oscillates around zero; this does not.** The streaks are real shadows with a real depth step, and what reads as broken is their EDGE -- a silhouette quantised onto the shadow map's texel grid, seen at a grazing angle, with no penumbra to hide it. That also explains why every bias failed: there was never a comparison to correct. **What is left is a penumbra wide enough to cover a texel step**, and the two commits either side of this row made that harder rather than easier -- the content-fitted cascade shrank the texel to about a millimetre and a half, so the texel-sized kernel that followed it spans a few millimetres of world. Widening it to four and eight texels was measured and does not close the gap; a 3x3 kernel cannot span the thirty-odd texels a five-centimetre penumbra now costs, which is the clamp M7.5 had for a reason. The next change is the KERNEL -- more taps, or a rotated pattern -- not another constant. **And the reason shader-side visualisation kept failing was not the automatic exposure**, which this row said twice and both times wrongly. Replacing the forward pass's colour outright leaves its uniforms and textures unused, dead-code elimination drops them, the reflection then reports different resource counts and the pipeline stops being creatable -- so the frame that came out flat was a frame with no pipeline, not a frame with a bad exposure. The M7.5 brief already records that exact mechanism from the per-feature sweep. Keeping the original expression live (`color = probe + color * 1e-8f`) and pointing the resolve at the HDR target rather than the tonemapped one gives a working probe, and that is how the measurement above was taken **Fixed 2026-08-22 by changing the KERNEL, which is what the probe pointed at.** Nine taps cannot draw a penumbra: spread a 3x3 far enough to cover a texel step and its own three samples show as bands, which is why the radius was clamped to three texels and why the edge stayed hard however the bias was set. Twenty-five taps cover the same reach with the spacing filled in, so the radius can go back to being a number of METRES -- clamped to [2, 4] texels, which is now a floor that guarantees the step is always covered rather than a ceiling that stopped it ever being. **It costs 0.54 ms at 1080p** and that is recorded in `perf-baselines.md` rather than buried: 1.51 to 2.01 on `examples/02-meshes`, a third of that frame, and the tap count is the dial if it is judged too much. **Three designs and each one failed for a reason worth keeping**: world-constant radius with a clamp the chain defeated at both ends; a texel-sized kernel, which fixed the far cascade and made the near one razor-sharp once the fitted cascade shrank its texel to a millimetre and a half; and this one, which works because the kernel finally spans what the radius asks for. The unit test was rewritten with each and says so. |

Redefined to what starvation actually is here: the simulation holding the voice list when the device needs it. The callback `try_lock`s and, if it loses, counts one and emits the silence the buffer was already cleared to -- which is what an underrun sounds like. **It also fixes a real hazard**: the callback used to take a blocking `lock_guard`, so a stall in the simulation would have stalled the audio device with it, and an audio callback must never wait on a game thread. The same soak now reports **0** in two of three sixty-second runs on a real device and 1 in the third, and that one is a single contention in five thousand callbacks -- inaudible, and structurally removable with a lock-free publish when M7 gives audio real buffers to stream |
| D045 | agent, 2026-08-22 | `luaug new` cannot find its own template: it fails inside `listDirectory` on a path with `../..` in the middle of it, and has done since the CLI's commands moved into `commands/` | fixed | Found by USING it -- M8 needed a scaffolded project to package, and the first `luaug new` of the milestone failed outright. `cliRoot` counted two directories up from its own source file, which was right while the command lived at `tools/cli/new.luau` and stopped being right the moment it moved into `tools/cli/commands/`: two levels up from there is `tools/`, so it looked for `tools/templates/starter`. Nothing caught it. The CLI's own suite covers `toml` and `version`; `luaug new` had no test at all, and the M3 gate that signed it off ran it by hand before the move. **A count is a fact about a directory layout that no check holds still.** The fix walks UP from the command's own source looking for `templates/starter`, which is self-correcting under any layout, and `tests/packaging` now scaffolds with the real `new.run` on every Windows gate run -- so the next move breaks a gate instead of a user's first command. |
| D046 | agent, 2026-08-22 | Two gates depend on a generated world that nothing generates: on a fresh clone `streaming_soak` and `asset_determinism` compile an empty content directory | fixed | Found while wiring M8's own soak gate: the flagship's world was deleted to prove the new gate would rebuild it, and the M7 gates beside it turned out never to have rebuilt anything. The chunk SOURCES are generated and gitignored on purpose -- 4.8 MB of JSON from a hundred committed lines -- and `run.bat` regenerates them for a HUMAN. Nothing regenerated them for a gate, so both tests passed here because a hand-run had left the world on disk. **Neither had ever run in CI to say so**: the last all-green run across all three tiers is at M5, before either gate existed, and Actions has executed zero steps since 2026-08-21. Fixed by `tests/support/ensure_generated_world.cmake`, included by both drivers: absent world, `lute <generator>` from the repository root, and a named failure when lute is not on PATH. Break-verified by deleting the world and running the gate, which rebuilt 289 chunks and passed. **The general shape is the one this project keeps meeting**: a gate whose input is produced by a step nobody automated is a gate that reports on whatever the last person left behind. |
| D047 | human, 2026-08-22 | `examples/10-open-world` shakes: with the character walking, the whole world vibrates | fixed | **The engine has no render interpolation, and the scheduler computes the factor for it that nothing reads.** Measured on the reference machine rather than guessed: a windowed run of the flagship reports about 100 render frames for every 60 simulation ticks (`PROBE render=608 sim=6.100`), so a camera advanced on `PreRender` moves on every frame while everything it looks at moves on two frames out of three. At a walking pace of 7 m/s that is up to 0.12 m of alternating lag between the camera and the world, which is what "it vibrates" looks like. `Frame::alpha` is computed by `frame_scheduler.cpp` and its own header says it is "passed to `render::extract` so a 144 Hz display shows smooth motion from a 60 Hz simulation" -- `architecture.md` §3 says the same -- and grepping the tree finds no consumer: every other `alpha` in `render` is transparency. So the promise is documented, the value exists, and the interpolation was never built. **The scene half is fixed**: the flagship advances its camera on `Heartbeat` now, with the world, which is what every earlier example already did and what removes the symptom. **And the engine half is fixed too.** `render::TransformHistory` captures every part's and camera's transform at the START of each tick, so once the tick has run it holds the state the frame interpolates FROM and the world holds the state it interpolates TO; `render::extract` evaluates the whole frame -- camera included -- at one fractional time. It lives in `render` rather than in `scene` deliberately: a previous transform is not world state, must not enter the world hash and must not travel in a replay, so `scene`'s components are untouched and every recorded determinism trace stays valid. Something that streamed in has no previous and is drawn where it is; a reused ECS slot is caught by the generation. **Headless passes alpha zero by construction** -- a headless run drives exactly one fixed step per frame, so its frames ARE the ticks -- which is what makes every golden in the repository still match byte for byte, and all 43 tests did. The remaining caveat is written down rather than left to be found: a game that drives its camera on `PreRender` now has that camera blended slightly towards the last tick, which is why every example drives it on `Heartbeat`. |
| D048 | human, 2026-08-22 | Looking down in `examples/10-open-world`, something dark on the ground keeps changing shape from second to second | fixed | **Reproduced exactly as reported**: ten screenshots a second apart with the camera pointing straight down, then differenced. With the character HELD STILL and the sun FROZEN, and the scene proved identical between the two frames -- 58 draws, 12 visible objects, 1,320 triangles in both -- 10,658 pixels of a 960x540 frame still differ, and the diff traces every tile-seam shadow and the outline of a boulder's shadow. So it is the shadow path, not the content and not the streaming. **Three contributors, each isolated by a flag or a build:** the caster-fitted cascade (M7.5) accounts for about 3,000 pixels -- disabling the fit and falling back to the sphere leaves 7,617; automatic exposure accounts for about 5,000 more -- `--no-auto-exposure` on top leaves 2,540; and the remaining 2,540 is a one-pixel dotted line along every shadow edge, which is the cascade stepping by ONE TEXEL as the camera crosses a half-texel boundary. That last one is inherent to a snapped cascade and is the technique working, not failing. **And the biggest contributor was not in the frame at all**: the demo ran a full day in four minutes, ninety times faster than the real sun, so every shadow visibly swings while a player stands still. That is the part a person notices, and the day is ten minutes now. **Fixed in three places, and the third one is the root.** (1) The day, above. (2) **The cascade fit remembers its box**: `ShadowCascades` carries the box it chose, in WORLD space, and a cascade keeps that exact centre and extent -- and therefore that exact texel lattice -- for as long as it still covers what casts into the slice. A fresh fit is grown 25% past its contents so there is something to keep. It refits when the content leaves the box or has shrunk below half of it, and a settings change or a resize drops the memory because a new atlas is a new texel size. (3) **The character was standing on a four-tile corner and sliding off it forever**: the flagship spawned at the world origin, where four 32 m ground tiles of slightly different heights meet, so it crept about three centimetres a second -- which moved the CAMERA, which moved the whole shadow lattice, which dithered every shadow edge in the frame. Spawned on a tile centre it is still to nine decimal places, and the `CharacterBody` itself is not at fault: the same probe drifts at a corner and does not on a flat tile. **The chain, measured end to end on a scene proved identical between two frames: 10,658 differing pixels of 518,400 became 43 -- and 3 with automatic exposure frozen, which is floating-point noise.** The forty that remain are the exposure still converging by a hair, which is the stabiliser doing its job. Three goldens moved and were re-recorded: the change is a few texels of shadow edge on the two most distant spheres, which is what the diff shows and what a kept box should cost. |
| D049 | human, 2026-08-22 | `InputService.PointerLocked` does nothing: the cursor is never locked, never centred and never hidden, so a mouse look control stops at the edge of the screen | fixed | Found by asking for mouse look in the flagship and discovering there was nothing to switch on. **The property was stored and read by nothing** -- `setInputServicePointerLocked` wrote `engineState().pointerLocked` and no platform code ever consumed it; `PointerVisible` was the same. It is the shape `Inert` exists to make visible (D030), and `inertcheck` could not see it: that check sweeps COMPONENT fields, and these two live in `EngineState`, which is the world's own struct rather than a component pool. Fixed with `platform::setPointerLocked` over `SDL_SetWindowRelativeMouseMode` -- which centres and hides the cursor and reports motion instead of position, the only arrangement that lets a look control keep turning past the edge of the screen -- applied by the frame loop on CHANGE, windowed only. **And the camera turned the wrong way**, reported in the same breath: the rig's forward is `(-sin y, 0, -cos y)`, so increasing the yaw rotates towards -X, which is left. A positive input has to subtract, and both `Turn` and `Look` now do. |
| D050 | human, 2026-08-22 | Shadows flicker continuously as the sun moves: the edges of the tree's and the character's shadows jump a few pixels between frames instead of sliding | fixed | **A different defect from D048 and with a different cause**, which is why it survived it: D048 was about a STILL sun, and this is about a moving one. Isolated by holding the character still, letting only the sun turn, and freezing the HUD -- the last of which mattered more than it should have, because the clock digit ticking over was two thirds of what the first measurement counted. **The shape of the numbers is the diagnosis**, not their size. With the old snap, the difference between frames 600 and 601 is 201 pixels and between 600 and 602 it is 1,312 -- a jump of six and a half times for one extra frame, then flat at 1,553 and 1,809. That is a discrete jump, not motion. With the fix: 224, 570, 1,029, 1,571 -- near-linear, which is what sliding looks like. **The cause**: the texel snap rounded `R(t) * centreWorld`, an ABSOLUTE position in a light space that turns with the sun. With the box half a kilometre from the world origin and a ten-minute day, that coordinate sweeps several texels per frame on its own, so the rounding flipped constantly and the whole shadow map jumped a texel at a time in a direction nothing in the scene explained. **The fix is to round the box's MOVEMENT rather than its position**: the delta from where the box was last frame is quantised, so the lattice is carried forward frame to frame instead of re-derived from an absolute that is sliding underneath it. That is also the stronger statement -- temporal stability asks that this frame's grid line up with the last frame's, and a grid fixed in the world stops being fixed the moment the light turns. **And the fit's memory (D048) had never engaged in this scene**: the ground fills the slice, so the content's light-space bounds ARE the slice's own, and the coverage test failed by one hundredth of a metre -- the snap's own displacement -- every single frame. One texel of slack fixes that, which is invisible: it is one texel at the very edge of a cascade, inside the blend band to the next. Two goldens re-recorded; the diff is a scatter of dots on shadow edges. |
| D051 | human, 2026-08-22 | Objects resting on the ground have a band of lit floor between their base and the start of their shadow, so a capsule and a boulder both look like they are hovering | fixed | Reported with a close-up, which is what made it findable: **the same objects at `tests/screenshots/specular`'s framing show that gap as about one pixel**, so a milestone whose shadow gate looked at that scene could not have seen it. `tests/screenshots/contact` exists now and is a standing golden -- three metres from a capsule, a ball and a box, with the frame number sweeping the hour so both ends of the range are one flag away. **The first hypothesis was wrong and a picture said so.** The shadow pass culls FRONT faces, which stores the far side of a solid object and detaches the contact by its thickness, so the obvious fix is to cull back faces instead. That was built and measured: the contact attaches, and a regular hatched acne appears across every lit floor in `examples/02-meshes` -- D044's own signature, and the confirmation of its note that the cull "is earning its keep". **What was actually wrong is that the two biases were paying for that acne a second time.** With front faces culled there is very little for a normal offset or a depth bias to correct, and what they do instead is push every shadow away from the thing casting it. `kShadowNormalOffsetFilters` was `sqrt(2)` -- a square kernel's diagonal, which is the right scale for a bias that has work to do -- and `kShadowDepthBiasMetres` was two centimetres, which under a character IS a visible band of bright floor. **Measured at three sun elevations**: at a quarter of a filter the contact is perfect and the capsule's own base begins to speckle; at half it is attached at every hour with no speckle anywhere, and the acne frame is clean. Shipped at half and eight millimetres. Three goldens moved. |
| D052 | human, 2026-08-22 | Shadows in `examples/10-open-world` lose resolution at a moderate distance: a tree's shadow stretching towards the camera is drawn in visible blocks, and the tile seams across the ground break into dashes | fixed | Asked as a question -- "is this normal?" -- and the honest half of the answer is yes: a cascaded shadow map picks its cascade from where the shadow LANDS, not from where its caster stands, so everything past the third split is drawn at that cascade's texel size and nothing about a distant caster makes its shadow coarse on nearby ground. The other half is that ours was much coarser than it needed to be, and the flagship's own project file is why. **Measured** by dumping each cascade's fitted box: at the file's `shadow_distance = 180` on the preset's 1024 tile, the far cascade covers everything from 44 m out with a half-extent of 266 m -- **0.52 m per texel**, which is a half-metre grid for a tree's shadow at fifty metres and is exactly what the report described. A caster fit cannot help there: in an open field the ground casts everywhere, so the box is the slice's own sphere to the centimetre. The file now asks for `shadow_resolution = 2048` and `shadow_distance = 140`, which measures **0.20 m per texel** in the same cascade, and the cost is nothing -- 2.81 ms median against 2.89, over 2,400 frames at 1080p, run twice each in alternating order because a sequence of GPU runs is not a sequence of measurements (M8 Finding 5). **Two things found while measuring it.** The Ultra preset spent its four-times atlas on range rather than density: 220 m gave 0.32 m per texel against High's 0.35, so a preset called Ultra was as blocky as the one below it for anything past thirty metres. It is 160 m now, which measures 0.23. And `--quality=low` did not fully win over a project file: the preset was replaced but the file's per-key entries were then applied on top, so a machine that asked for less would have been handed this file's 4096-pixel atlas. A file's per-key graphics entries refine the level it names and are dropped with it; `engine/app/tests/project_config_tests.cpp` is new and says so, the layering having had no test of its own before. |
| D053 | human, 2026-08-22 | The world is temporally unstable from about eleven in the morning of the in-game clock: with the camera and every object still, the ground pulses and shadow edges tremble and jump between frames, where earlier in the cycle the same shadows slide smoothly | fixed | **Two defects under one symptom, and the report's own detail -- that it starts at an HOUR rather than after a duration -- is what separated them.** Reproduced on a still probe scene under the flagship's clock rate, one screenshot per frame, subtracting consecutive frames. **The first and larger one is not a shadow at all: it is the diffuse ambient, and it was arriving in steps.** The irradiance SH rode on the specular chain's level zero, which is rebaked only when the sky drifts past `kEnvironmentRebuildCosine` -- about one frame in a hundred under a ten-minute day -- so the light every matte surface in the world receives held perfectly still for a hundred frames and then jumped by about a per cent. Measured: on the bake frame **68% of the ground changed** while the sky beside it did not, at an energy of 297,810 against 17,000 for its neighbours. The specular threshold is right for what it guards (a degree of staleness in a mirror is a step nothing rougher than a mirror can show) and wrong for the diffuse half, which costs one projection onto nine numbers rather than six texture bakes. The diffuse half now has its own threshold four times tighter, and what the shader reads walks towards it a twelfth per frame -- **peak-to-mean 6.99 became 1.27**, at +0.2 ms. A sun that jumps further in one frame than the specular threshold allows is treated as a CUT and taken whole, which is what keeps a clock-scrubbing fixture (and `tests/screenshots/contact`, which steps a quarter of an hour per frame) from fading into every shot. **The second is the shadow edge, and it is inherent rather than broken**: a cascade's lattice is defined in the light's own rotating frame, so a fixed world point drifts across it at a rate set by the light's angular speed -- measured at 0.02 texels per frame at dawn against 0.50 near noon, which is why the report names an hour. What that drift produces is a shadow edge that steps one texel at a time, and what hides it is a penumbra wider than the step: `kShadowFilterMinTexels` is 3 rather than 2, which halved the amplitude of the worst step (channel delta 20 to 9) and leaves the near cascade -- where D051's contact lives -- untouched, because its filter was already above three texels. The rest is texel size, which is D052. |
| D054 | human, 2026-08-22 | The shadow flicker under a moving sun is reduced but not gone, and it scales with CAMERA DISTANCE: close to a caster the shadow slides smoothly, far from it the edges go back to jumping between frames | fixed | The third report about this edge, and the distance relation in it is the diagnosis. A cascade's lattice lives in the LIGHT's rotating frame, so a shadow edge cannot slide continuously -- it advances one texel at a time. What changes with distance is the frequency, not the size: measured on a still probe, the near cascade drifts about half a texel per frame near noon and steps every other frame, while the far cascade drifts a twentieth and holds still for twenty-three frames before moving. **Frequent small steps read as motion; one step every four hundred milliseconds reads as a jump**, which is exactly what the report describes and why the near field looked fine. The step cannot be removed without a temporal filter this renderer does not have, so it is hidden instead: the penumbra has to be several texels wide, and in the far cascades the authored 5 cm penumbra is a fraction of one, so the texel-band FLOOR is the only thing speaking out there. It was two texels, then three; it is six now. What made six affordable is the second half: **the 5x5 kernel is rotated per pixel** by Jimenez's interleaved gradient noise, which is a function of the pixel's coordinates alone and therefore perfectly stable in time. A fixed grid spread that wide puts its twenty-five taps a texel and a half apart and every pixel in a region samples the same five rings, which is the banding the old ceiling of four existed to avoid; rotating each pixel's grid spends the same taps on different rings and the banding becomes fine noise. **Measured on a still scene with the casters at forty-five metres, consecutive frames subtracted**: pixels changing by more than four levels fell from 62 per frame to 14, those changing by more than two from 227 to 80, and the worst single change from 37 levels to 14. The near cascade is untouched -- its texel is millimetres, so the world radius lands inside the band on its own -- and contact was re-measured at three sun elevations on `tests/screenshots/contact` because a wider filter scales the normal offset with it, which is what D051 was about. Seven goldens moved: five capture streams, whose diff is one line each recording that the PBR fragment shader grew by 2,520 bytes, and two screenshots. **The instrument for the next one is `tests/screenshots/contact`'s sibling trick, and it came from the report too**: a camera looking straight down at the ground turns a shadow edge into a shape whose motion is unmistakable. **Second round, from the same reporter**: the worst hour moved from noon to nine in the morning, which is a low sun and therefore long shadows over the flagship's terraced ground. Measured properly this time -- a burst of 120 consecutive frames from ONE run, and the shadow edge's sub-pixel position tracked along a scanline by where the profile crosses the midpoint between its lit and dark ends, which is invariant to the ambient drifting: **the edge held perfectly still for five to twenty-five frames and then jumped 3.6 pixels, twice reversing direction.** The reversal is the tell: the sun turns one way, so a backwards jump is the rasterisation flip-flopping rather than tracking. The cause is the filter's own arithmetic -- it returns a COUNT of taps, so the smallest change it can express is one tap, and with twenty-five of them one texel of the map flipping moves the edge by a twenty-fifth of a six-texel penumbra. **Seven by seven rather than five by five over the same radius quantises four times as finely: the worst single step fell from 3.58 pixels to 0.75**, and the flagship's median frame did not move (2.98 ms against 3.09), because at 1080p this scene is not bound by shadow taps. Seven goldens moved again, and the diff is again one shader-size line each plus a hair of edge. **One thing ruled out while looking**: the dark dashed lines along the flagship's tile seams are not a shadow artifact at all -- with `--shadow-cascades=0` they are still there, because they are the vertical faces of the steps between tiles seen nearly edge-on. |
| D055 | agent, 2026-08-22 | `inertcheck` sweeps component pools only, so every property belonging to a service with one instance per world -- half the API surface -- was never checked for being stored and unread | fixed | Named in M8's own gate record as the thing the milestone measured and did not fix, then asked for. The blind spot was the size of a service: a knob like `FixedTimestep` or `PointerLocked` lives in `scene::EngineState` rather than in a component pool, because a service with one instance and no hierarchy has nothing a component would buy. That is how D049 survived from M6 to M8. The lint now runs the same sweep twice -- once over `components.h` for `*Component` structs, once over `world.h` for `EngineState` -- with each sweep excluding its own declaring header, and an explicit table maps the fields whose property name is not just their name in PascalCase (`streamingLoadRadius` is `StreamingService.LoadRadius`). **The first run found three, and none of them was marked Inert.** `DebugService.OverlayVisible` was stored and nothing applied it: the overlay was toggled by F3 alone, so the property was a lie in both directions -- it is synced both ways now, because a game that draws its own hint from it would otherwise go stale the moment somebody pressed the key. `UIService.DisplayScale` says in its own comment that the host writes it every frame and the host never did, so it was 1 on a doubled display and `SafeAreaInsets` was zero on a device with a notch -- both are written from the window now, which is what `platform::windowDisplayScale` and `windowSafeAreaInsets` are for. And `StreamingService.PauseOutsideLoadedArea` had a reader **waiting** for it: `StreamingManager::minimumRingResident()` exists, its comment says it "is what `StreamingService.PauseOutsideLoadedArea` reads", and nothing had ever called it -- so a game that turned the property on got no pause and a character walking into unloaded ground fell through it. **Break-verified**: with a minimum ring nothing can satisfy, 240 frames produce zero ticks with the property on and ticks with it off. It does not weaken R10 -- what it changes is WHEN a tick runs, never what a tick computes. |
| D056 | agent, 2026-08-22 | The `shipping` profile has not compiled for an unknown length of time, and nothing anywhere builds it | fixed | Found by acting on a reconnaissance claim rather than by a gate, which is the point: `cmake --build --preset win-msvc-shipping` fails with two `fatal error C1083: cannot open include file 'luacode.h'`, at `engine/script/src/modules.cpp:9` and `engine/script/src/runtime.cpp:18`. The cause is not a missing file -- it is that `shipping` is the one profile that sets `LUAUG_LUAU_COMPILER=OFF` (`cmake/luaug_options.cmake:16-18`, "loads precompiled bytecode only", ADR 0002) and no `#if` was ever written around the two sites that need the compiler's header. **Nothing exercises it**: `win-msvc-shipping` is in `configurePresets` and `buildPresets` and NOT in `testPresets` (`CMakePresets.json:57-62`), the string does not appear in any workflow under `.github/`, and `scripts/localgate.ps1` builds `win-msvc-dev` only. So the profile that the option file describes as the one that "cannot accidentally carry" the compiler cannot accidentally be built either. **A second failure is stacked behind this one and is still untested**: `engine/app/src/debug_overlay.cpp` closes its `#if LUAUG_DEBUG_UI` at line 784 and then defines `DebugOverlay::captureLog()` at 786 using `ConsoleLog`, `console()`, `core::setLogSink` and `std::lock_guard`, all of which exist only inside the block -- so that translation unit should fail too, and the build never reached it. **This is what D057 is made of**, and it is why a broken profile nobody builds is not a harmless one. **Fixed 2026-08-22, and it took three fixes rather than the two that were visible.** The `luacode.h` includes are guarded, and the absence is made HONEST at runtime rather than silent: `loadChunk` is the single point where source would have become bytecode, so a shipping host boots, mounts the scripts and reports `script.err.no_compiler` per file with a non-zero failure count -- which means `luaug test` on such a build fails loudly instead of passing over nothing. `captureLog` was real and failed exactly as predicted once the first errors cleared. The third was new: `LUAUG_BUILD_TESTS` defaulted ON in `shipping` and `sandbox_tests.cpp` calls `luau_compile` directly, so the option is dependent now -- a suite that drives the engine through Luau SOURCE cannot run in a profile with no compiler, and `CMakePresets.json` already agreed by leaving `win-msvc-shipping` out of `testPresets`. **The gate builds and LINKS the profile every run** (`scripts/gates/shipping-build.sh`, Tier-2, ~30 s warm), linking rather than compiling because half of ADR 0011's claim is a link-time one: a call site that survives an `#ifdef` sweep is an unresolved symbol, not a compile error. It caught its first defect on its first real use, in the editor work landing beside it -- three private fields nothing reads when ImGui is compiled out, one of them dead. **What it does not fix is D057**, and the two are cleanly separable now: the profile compiles and still cannot run a packaged game, because ADR 0002 gives it no compiler and ADR 0045 ships source |
| D057 | agent, 2026-08-22 | `luaug build` packages a `dev`-profile host, so the released v1.0.0 game binary carries the ImGui debug overlay, the inspector and a Luau REPL against its own VM | fixed | Found while investigating D056, an hour after the v1.0.0 release was published with this binary attached. `projectlib.findEngine` searches `LUAUG_BUILD_ROOT` for `win-msvc-dev`, `linux-clang-dev`, `macos-clang-dev`, `win-msvc-debug` (`tools/cli/project.luau:126-143`) -- **`shipping` is not in the list**, so `luaug build` cannot select it even when it builds. Verified against the shipped artifact rather than inferred: reading `examples/10-open-world/dist/win64/LuauG-Open-World.exe` as bytes finds the literals `debug-overlay`, `ImGui` and `stored and read back faithfully` -- the last of those being the inspector's own inert-property tooltip from `engine/app/src/debug_overlay.cpp:436-445`. So F3 in the released game opens the engine's debug shell, and its console evaluates arbitrary Luau in the game's VM through `ScriptRuntime::evaluate`. **Impact is a promise broken rather than a machine harmed**: this is the player's own copy of a single-player game, so the REPL is a cheat console and not an escalation -- the sandbox (R4) is intact and `luaL_sandbox` is on in every profile. What it contradicts is ADR 0011:14, "dev-facing only, compiled out of shipping builds", and the release notes, which describe a player and do not mention a debug shell. **The two candidate fixes are not equal and the choice is a human's.** Making `shipping` build and packaging it would ship a profile that has never run a test in this repository's history (D056), which is worse than shipping the profile with 34 passing ctest targets behind it. Making the promise true instead -- a packaged game built from a profile that compiles the overlay out -- needs the bytecode packaging ADR 0045 declined, because `shipping` has no compiler and a packaged game ships source **Fixed 2026-08-23 by the third exit this row named.** `LUAUG_DEBUG_UI` and `LUAUG_LUAU_COMPILER` were independent options that only ever shared a profile STRING, so a profile that takes one and not the other costs a condition. `player` is Release, links the Luau compiler -- which a packaged game needs, because `luaug build` ships its Luau as SOURCE (ADR 0045) -- and declares no ImGui target at all, which is what makes "this artifact contains no overlay" a link-time fact rather than a promise (ADR 0011's own argument, applied to the profile that actually ships). **`luaug build` now REFUSES a host that is not one**, rather than warning: a warning in a build log is a warning somebody scrolls past, and what it would be describing is an artifact already written to disk and about to be sent to people. The message names the two commands that fix it. `--dev-host` is the door, because handing a tester a package they can open the inspector in is a real thing to want, and a rule with no door gets worked around by setting an environment variable instead -- which says nothing. So `LUAUG_HOST` is deliberately NOT consulted when choosing what to package (it is what `luaug dev` uses, and "I pointed the dev server somewhere" must not decide what a release contains); `LUAUG_PLAYER_HOST` is its own name for its own job. **And the gate builds and links it**, beside `shipping`, in the stage that exists because a profile nothing builds is a profile nobody knows is broken (D056) -- which was this row's own objection to simply shipping the `shipping` profile, and would have been a poor thing to reintroduce. The packaging suite asserts the refusal where no player host exists and says so out loud where one does |
| D058 | human, 2026-08-22 | The editor opens with the game already running and has no play, pause or stop, so there is nothing to edit against -- whatever you change is overwritten by the next tick | fixed | Reported at E1's review, in the sentence that decides whether the milestone is an editor: "só não gostei que o jogo já começa em execução e não tem onde dar pause play stop". It is right and it is not a missing feature so much as a wrong default -- a world that is simulating is a world whose properties are being written by scripts every sixtieth of a second, so a person typing into the properties grid is arguing with the game and losing. **The fix is cheap and the precedent is already in the loop**: `waitingForGround` (D055) gates `frame.simTicks` to zero for streaming, and its comment already makes the R10 argument this needs -- what a gate changes is WHEN a tick runs, never what a tick computes, so the same ticks happen in the same order with the same inputs and the hash after n ticks is the hash after n ticks. **And pausing at tick zero shows a built world rather than an empty one**, because `WorldHost::boot`'s drain runs every entry script before the first frame and advances no clock, which `world_host.cpp:318-327` says in its own words. **What cannot honestly ship with it is STOP.** In an editor, stop returns the world to what it was before play -- to the EDITED state, not the scripted one -- and this engine cannot remember an edited state yet, because nothing can serialize a world (E3). A button labelled Stop that silently discarded somebody's edits would be worse than no button **Closed 2026-08-23**, and by E1 rather than by anything here: the editor opens in EDIT mode, the toolbar has play, pause, resume, stop and step, and stop is honest because `World::snapshot` and `World::restore` arrived with it -- what it puts back is the world as it was before play, which is exactly what this row said could not ship yet. Left open in the register while it was being read as a scope note rather than as a defect; the state is what the register is FOR |
| D059 | human, 2026-08-22 | The mouse cursor disappears while the editor is open, so there is nothing to click the panels with | fixed | The same wrong-owner mistake D058 was, one device over: `examples/10-open-world` sets `InputService.PointerLocked = true` and `PointerVisible = false` at file scope for its mouse look (`init.luau:386-387`), and `WorldHost::boot`'s drain runs file scope before the first frame -- so an editor opened on that project had no cursor from the very first frame it drew, and every panel in it was unclickable. **The rule is the one Unity and Unreal use and the one the transport already applies to time**: while the world is paused the editor owns the device; pressing play hands it back. **Fixed 2026-08-22** by overriding rather than overwriting -- `engineState.pointerLocked` still holds what the script wrote, so a script reading the property back sees its own value and a replay of a game that locks its pointer stays legal; only what reaches the WINDOW is the editor's while paused. Reported by a person opening it, which is the third defect in this milestone found that way and the third that no test in this repository could have found |
| D060 | human, 2026-08-22 | A sound plays while the editor is open and the world is not running | fixed | Reported alongside D061 in one sentence, and it is the same wrong-owner mistake as D058 and D059 one device further out: `examples/10-open-world` starts its ambient wind at file scope, `WorldHost::boot` runs file scope before the first frame, and `AudioSystem::update` pushes voices on the RENDER clock rather than the tick -- so a paused world was inaudible in no sense at all. **Fixed 2026-08-22** with `AudioSystem::setSuspended`, a HOST control no script can reach: while suspended the mixer is handed an empty voice list and **not one `Sound` is touched**, so `Playing` and `TimePosition` are exactly where they were and pressing play resumes rather than restarts. That distinction is the difference between pausing and stopping, and getting it wrong would make every ambient loop jump back to its start every time somebody tested a change. `SoundService.Volume` stays the game's control; this is the tool's |
| D061 | human, 2026-08-22 | After pressing play and then stop, the editor camera flickers continuously | fixed | Two authors for one transform, and the pause did not separate them because it only gated the TICK. `RunService.PreRender` fires on the render clock, `examples/10-open-world` drives its follow camera from there, and the editor writes `CurrentCamera` at the frame's safe point -- so on every frame the script put the camera behind the character and the editor put it back, and the view alternated between two answers sixty times a second. It appeared only after a play because the editor camera is not adopted until then. **Fixed 2026-08-22 by making pause mean what it says**: a paused world runs no script phases, not even the render-rate one. Arbitrating who wins the camera was the other candidate and it is the wrong one -- a phase that still fires is the game still running, and no amount of deciding who wins would make that untrue. The same reasoning already governs the tick (D058), the cursor (D059) and the audio (D060), and the four of them are one rule: **while the editor is paused the tool owns the machine, and pressing play hands it back** |
| D062 | agent, 2026-08-22 | The game's input still reaches it while the editor is editing, so WASD would drive a character and the fly camera at once | fixed | Found by looking for the shape of D058, D059, D060 and D061 rather than by hitting it: those four are one architectural mistake appearing four times -- the editor inheriting the game's decisions instead of taking them -- and input was the fifth device with no owner declared. It had not been REPORTED because a paused world runs no ticks, so no action resolved; the moment somebody pressed play on a project with keyboard shortcuts, WASD would have gone to two places. **Fixed 2026-08-22** by the rule the other four now share: while the editor is editing the game receives no input at all, and `InputSystem::releaseAll` runs once on the way in so a key held when play stopped is not still held when play starts. That is the alt-tab case `releaseAll` was written for, arriving through a different door. ImGui is unaffected -- it takes the untranslated SDL stream of its own (`debug_overlay.cpp`), so the shell stays fully live while the game is not |
| D063 | human, 2026-08-22 | Holding the right mouse button to turn the editor camera hides the cursor but does not hold it: it moves, and reappears somewhere else | fixed | Reported twice, the second time with the detail that decided it -- "I do not see it but it moves". That separates two failures that look alike: hiding and holding are different calls, and only the first was working. **The log said the second one was working too**: `Pointer held.` and `Pointer released.` with no refusal, so `SDL_SetWindowRelativeMouseMode` was accepting. **The cause is SDL doing exactly what it documents.** In relative mode `SDL_mouse.c` accumulates a LOGICAL position from the motion -- `mouse->x += xrel; mouse->y += yrel;` clamped to the window -- and on the way out `SDL_SetRelativeMouseMode(false)` warps the real cursor to it: "Put the cursor back to where the application expects it". For a game whose camera is the mouse that is right; for an editor where the pointer is a tool it is not, because a look that turns the camera around leaves the cursor against a window edge instead of under the hand that was holding the button. **Fixed 2026-08-22 by anchoring**: the position is remembered while the pointer is FREE -- recording it during the hold would anchor to wherever the camera took it -- and `platform::setPointerPosition` puts it back when the hold ends. **And the diagnostic that found it stays**: `setPointerLocked`'s result was being discarded with a `(void)`, so a refused lock and a granted one looked identical from outside, and the difference only shows as a camera that stops turning at the edge of the screen -- a thing somebody reports and nobody can explain |
| D064 | human, 2026-08-22 | Right-clicking anything in the editor does nothing: no context menu appears | fixed | Reported twice, and the two reports had different causes. **The first was a condition I wrote wrong.** The camera's look test read "over the viewport OR dragging", and ImGui's `IsMouseDragging` becomes true a pixel after a press ANYWHERE -- so a right-click in the explorer with a tremor of the hand became a camera turn: the pointer locked, motion stopped reaching ImGui (D063's fix), and the menu that click was asking for never appeared. Latching on the PRESS fixes it, and the press is the only moment the question can be answered -- once the pointer is held it stops reporting a position. **The second was worse and was a process failure.** The patch that added the two item menus asserted mid-way and never wrote the file, because it writes at the end; only the folder menu -- applied separately -- had landed. It compiled and every test passed, because the signature it would have changed had not changed either, so nothing could disagree. I reported the menus as done on the strength of having RUN the patch rather than having read the result. The scripts that edit this repository now re-read the file from disk after writing and fail if what they added is not there: **an edit that cannot be verified is an edit that did not happen** |
| D065 | human, 2026-08-22 | The editor lets you delete, duplicate and rename the engine's own services | fixed | Asked as three questions in a row -- "I should not be able to delete the basic services", "the Workspace counts too, right?", "renaming as well, I think" -- and the three have three different reasons, which is why each is stated rather than covered by one sentence. **Deleting** leaves a world that cannot answer a call every script makes. **Duplicating** makes "one per world" false, so every `GetService` becomes a question with two answers. **Renaming** breaks `game.Workspace`, which is a lookup BY NAME and not a keyword, in files the editor cannot see. **Fixed 2026-08-22 by asking the IDL**: a class tagged `Service` carries `ClassFlags::Service` into the generated descriptors, so `Workspace`, `Lighting`, `UIService`, `AudioService`, `RunService`, `TagService` and `ScriptService` are covered with no special case and a service added later joins the rule on its own. A list of names would have gone stale the first time somebody added one. **The rule lives in `Editor`, where the world changes, and the menu only reflects it** -- greyed rather than refused afterwards; a rule that lived in the menu would be bypassed by every other path to the same action. **And the first version was wrong in a way the tests caught**: it read "has no parent" as "is the world's root", which would have refused to delete anything somebody had detached, because an instance parented to nil is a loose instance and not a root. The root is passed in now rather than inferred |
| D066 | agent, 2026-08-22 | `openworld_soak` fails its instance-growth check under machine load and passes immediately afterwards | quarantined | Failed once in E1's closing gate -- "the world grew from 4269 to 5385 instances between the second and the fourth quarter of the run; a streamed world flattens" -- and passed twice in a row when re-run alone, minutes later, on the same binary and the same tree. **Not quarantined**: `MASTER_PROMPT.md` §12 sets that bar at flaking twice, and this is once. Recorded so the second time is recognised as a second time rather than as a first. **The hypothesis the evidence supports, and it is labelled as one**: streaming's materialisation budget is denominated in MILLISECONDS rather than in chunks (`architecture.md` §10, M7). Under load fewer chunks materialise per frame, the resident set lags the focus, and the balance between materialising and evicting shifts across the quarters -- which is exactly the quantity this check compares. The failing run shared the machine with a parallel container build and an interactive editor; the passing runs had it to themselves. **If that is right the test is measuring the machine as well as the engine**, and the fix is for the check to be denominated in something load cannot move -- not for the threshold to be widened until it stops complaining, which would remove the only instrument that watches a streamed world for a leak. **Second flake 2026-08-22, and it was recognised as one because this row existed.** 4,269 to 5,306 instances in a gate run, then 4,269 to 4,222 on the very next run with the machine to itself -- same binary, same tree, and 5,939 frames both times, which is the part that settles it: the frame COUNT is deterministic and the resident set is not, so what differs is how far behind materialisation fell. **Quarantined per MASTER_PROMPT.md §12**, and quarantined rather than weakened: `SoakVerdict` gained a `quarantined` list beside `failures`, the check still runs, still measures, still logs at warn level and is still in the JSON report -- it no longer fails the gate. `soak_tests.cpp`'s D032 case now asserts exactly that distinction, so the complaint cannot go quiet without a test saying so. **What replaces it has to be denominated in something load cannot move**, and the honest candidate is a focus path that RETURNS to where it started: a resident set that does not come back with it is a leak whatever the millisecond budget did. |
| D067 | human, 2026-08-22 | Pressing play in the editor on `examples/10-open-world` prints `init.luau:566: [script.err.instance_dead]` once a frame and nothing happens -- the viewport only changes colour | fixed | **Reproduced byte for byte before it was touched, and the reproduction is the diagnosis.** With the project's scene named in `luaug.toml`, 120 headless frames of the flagship produce 89 copies of the reported line, from the reported file, at the reported line number; with the fix, zero. The chain is one ordering mistake. `WorldHost::boot` runs every entry script's file scope in its own drain BEFORE the first frame, so `init.luau` had already built the walker, the lantern post, the welds, the follow camera and the input contexts. `engine.cpp` then applied the boot scene AFTER that drain -- and a scene load REPLACES the contents of `Workspace` (`scene_file.cpp:559`, and it has to, or a second load would double everything), so `clearScene` destroyed every one of them. **The Luau VM is not restored by any of this**: it went on holding the references and its `Heartbeat` connection stayed connected, so the first tick that ran raised `instance_dead` on `lantern`, aborted the handler before the camera, the movement and the streaming focus, and left only the two lines above it -- `ClockTime` and `FogColor` -- still landing. That is precisely "the background changes colour and nothing else happens". The streamed world could not arrive either: `StreamingService:AddFocus(character)` had registered an instance the clear destroyed, so `collectFoci` dropped it and the resident set had nothing to follow. **The fix is the order ADR 0047 already specifies and `scene_file.h` already states in its opening paragraph** -- "boot reads it and then starts the scripts". `WorldHostOptions` carries `bootScene` and `WorldHost::boot` applies it after the project mount and the preserved restore and BEFORE `startScripts`, so there is nothing alive to invalidate and a script that builds its world builds it on top of the file. E1 shipped the inversion knowingly and said so; what it did not have was a project where the two orders disagree. **Two things it fixes that were never reported**: a hot reload used to drop the scene entirely, because the load lived outside the host the reload rebuilds, and it now travels in the options `reloadWorld` copies; and an entry script can finally see the world it was given, which is what makes a scene worth having. Break-verified: with the block moved back after the drain, three of the four new cases in `world_host_tests.cpp` fail |
| D068 | human, 2026-08-22 | Save Scene As wrote the flagship's scene into `content/content/scenes/`, where nothing will ever look for it | fixed | Found in the wreckage of D067 and it is the reason that project had a scene at all. The dialog labels its box `content/` and then resolved what was typed against the content root, so the natural thing to type -- `content/scenes/main` -- became `<project>/content/content/scenes/main.scene.json`, and `createDirectories` made the folder without a word. The forensics are on disk: a 5,093-byte full world at the doubled path at 10:52, and 32 seconds later a 170-byte empty one at the right path, which is exactly what `New Scene` followed by a second `Save As` writes. **Nothing validated the string at all** -- not a leading `/`, not a drive letter, not `..`, none of which `ContentTree::isUsableName` would have accepted from the New Folder box three hundred lines away. `Editor::normalizeScenePath` and `Editor::sceneNameIsUsable` are static and pure so the dialog previews the RESOLVED path while it is being typed and disables Save on a path that leaves `content/`, which is the half that makes the rule visible rather than surprising |
| D069 | human, 2026-08-22 | In play mode with the pointer locked, the mouse still reaches the Explorer and the Properties panel -- hovering rows and clicking things nobody is pointing at | fixed | **The other half of D063, and the worse half.** A locked pointer is SDL's relative mode: the cursor is hidden and does not move, and SDL goes on posting motion carrying a logical position it accumulates from the deltas. That position walks across the panels, and ImGui hovers and clicks whatever it walks over -- so a player turning their head is dragging through the explorer at the same time. D063 fixed this for the EDITOR's own right-drag and `handleEvents` has withheld motion while `lookInput().active` ever since; nothing covered the case where the GAME holds the lock, which lasts as long as somebody is playing rather than as long as a button is held. **Fixed 2026-08-22**: the frame loop already answers the question -- `wantLocked && !editorOwnsPointer` -- so it tells the overlay rather than leaving it to infer, and it tells it BEFORE `handleEvents` runs later in the same frame, so there is no lag on either edge. While the game holds it, motion, wheel and button PRESSES are withheld and the cursor is pushed off-screen with ImGui's own `-FLT_MAX`, because withholding motion alone would leave whichever row was under the cursor when play was pressed lit for the whole session. A RELEASE is always delivered, in both the editor's case and the game's: ImGui must never be left believing a button it was never told about is held. **And Escape stops play now**, which is what Unreal does and what this change made necessary rather than merely nice -- a project that never hands the cursor back would otherwise have had no way to reach the stop button, and the keyboard still arrives |
| D070 | human, 2026-08-22 | After play then stop in the editor, the flagship's character capsule flickers continuously in a world that is not running | fixed | **The code had already written down the obligation nobody met.** `world.h:424-428` lists `render::TransformHistory` among the caches that are "rebuilt from the tree rather than restored" and gives the order in so many words -- "safe order: restore, then rebuild". `TransformHistory::clear()` had no caller anywhere in production. So `Editor::stop`'s `world.restore` put the capsule back at its spawn while the history still held where it had walked to, and a snapshot preserves generations PRECISELY so an id means the same thing afterwards -- which is exactly what let the stale entry go on answering `previous()`. **The second half is the alpha**: `FrameScheduler` drains its accumulator every frame whether or not the editor let a tick through, so `frame.alpha` goes on sweeping the whole of [0, 1) at render rate. Interpolating between two poses metres apart with a factor that changes every frame is the flicker, and it was only ever the three instances that MOVE during play -- the walker, its capsule and the lantern post -- because `render_world.cpp`'s early-out skips a part whose two ends are identical, which is the whole anchored terrain. **Fixed 2026-08-22 in both halves.** A world that is not advancing has nothing to interpolate, so `renderAlpha` is zero unless `advancing(runState())` -- which also settles the smaller tremor a paused frame had against a history one tick old. And the history is dropped when `Inspector::worldGeneration` moves, which is the signal every world-replacing path already raises: stop, undo, redo, load, new scene and hot reload, all at once and without inventing a second notion of "this is not the world it was" for the two to disagree about. `editor_tests` holds that counter as a contract, because a path added later that restored without announcing it would bring this straight back |
| D071 | human, 2026-08-23 | Ctrl+Z collapses the whole Explorer | fixed | The tree drops its expanded set whenever `Inspector::worldGeneration` moves, and undo moves it -- so every undo also closed every row somebody had opened, on a scene where finding the thing again is the expensive part. **The counter was answering two questions with one number.** A reload or a scene load builds a DIFFERENT world: slot indices restart from zero, so a set keyed by instance index would hand a dead row's expansion to whatever moved into its slot, and dropping it is right. An undo, a redo and a stop are `World::restore`, which carries generations and the free list *precisely* so that an id means the same thing afterwards -- `world.h` says so where the snapshot is defined -- so every row the set names is still that row. **Fixed 2026-08-23** by splitting the two: `worldGeneration()` still counts every wholesale change of the world's VALUES and is what the frame loop's `TransformHistory` reads (D070 is what happens when that one is missed), and `worldIdentity()` counts only the worlds that are actually different, which is what anything keyed by an id reads. `onWorldRestored()` bumps the first and not the second, and unlike `onWorldChanged` it leaves the selection to `pruneDead` rather than clearing it -- so an undo now keeps what you had selected too, which was the same defect one panel over and had not been reported yet. `editor_tests` holds the distinction, because a path added later that restores through the wrong one would collapse the tree again and nothing else would notice |
| D072 | human, 2026-08-23 | Opening the content browser fills the log with `Calling PopStyleColor() too many times!` | fixed | Mine, and one edit old: the open scene's green name was pushed once and popped twice. When the browser's rows were restructured so the name is DRAWN on the row rather than carried as the selectable's label, the colour had to be popped after the name instead of after the selectable -- and the new pop was added without the old one being removed. ImGui caught it in the open, once per row per frame, which is the best version of this mistake. **Fixed 2026-08-23**; the pop that coloured nothing is gone |
| D073 | human, 2026-08-23 | Selecting one instance in `examples/10-open-world` makes others disappear -- every boulder and every tree canopy leaves the frame | fixed | Mine, and one commit old. The first cut of the selection silhouette excluded an outlined draw from instance batching, on the argument that a batch is drawn or skipped whole and selecting one of five identical crates does not select the other four. That argument is right and the place was wrong: a run of identical meshes is how a whole forest is drawn, and splitting one -- in `buildInstanceBatches`, which every pass of the frame depends on -- took the split mesh out of the frame entirely. Reported with two screenshots a second apart, which is what made it unmistakable. **Fixed 2026-08-23 by not touching the batching at all**: `instanceable` is back to transparent-and-skinned, and the OUTLINE pass ignores batching instead -- it treats every draw as its own, which costs a handful of calls because only what is selected is drawn there. The cost belongs to the tool's pass and not to the one the whole frame is built on. `outline_mask_instanced` went with it, and the capture goldens are re-recorded: the only lines that changed are the shader and pipeline creations |
| D074 | human, 2026-08-23 | Saving a scene in `examples/10-open-world`, closing and reopening leaves the world with two of everything -- two characters | fixed | **Not a regression: the honest cost of D067's fix, arriving where it always would have.** Once the scene loads BEFORE the entry scripts -- which is ADR 0047's lifecycle and what D067 corrected -- a project whose CODE builds the world has two sources for one world. The scene puts back the character it saved and then `init.luau` makes another, and the engine cannot merge them: it has no way to know that a `CharacterBody` the file names and a `CharacterBody` the script makes are meant to be the same one. **The engine now says so, with both numbers**, at boot and once (`scene.warn.two_sources`), because "the file gave you 6 and the scripts added 5" is actionable and "something may be duplicated" is not. **What it does NOT yet have is the mechanism a project needs to resolve it**, and that is the open half: a script has no way to ask whether the world it is about to build has already been authored, so the only resolutions today are to stop building that world in code or to give the project no scene. `examples/06-scene` is the shape ADR 0047 asks projects to become and it has neither problem. **Fixed 2026-08-23, the way Unity and Unreal have it** (human decision): the world is the FILE and the script is what it does with it. `examples/10-open-world`'s character, its capsule, its lantern, the two welds, its camera and its whole input map are now `content/scenes/main.scene.json`, `luaug.toml` names it, and `init.luau` FINDS them by the names it agreed to instead of making them -- ninety lines of construction became six lookups and an assert per name, so a scene missing something says which. Three things had to move with it. `luaug-host --save-scene=PATH` is the migration itself: a project whose world is in its code had no way to get that world into a file, and retyping it into JSON is not a migration anybody performs -- it boots, lets the scripts build, and writes through the same `writeScene` the editor's Save calls. `@luaug/camera` ADOPTS `workspace.CurrentCamera` when the scene already holds one, or every scene-first project would get a second camera that silently wins. And the cold-boot teleport to the spawn is gone: it was right while the script owned the world and is a save that did not save now that a file does. **The warning stays** -- it is what proves the fix, and it is what the next project to grow two sources will see |
| D075 | human, 2026-08-23 | A child added to a `Folder` in the Workspace never appears -- "porque em folder na workspace eu não consigo adicionar filho??" | fixed | **The plus worked; what did not was SEEING what it made, and the reported symptom is indistinguishable from the reported cause.** `Editor::createInstance` accepts a `Folder` parent -- it refuses only an unknown or uncreatable class and a `generated` parent -- and it selected the new instance, so the properties grid was showing it. The Explorer was not. The expanded set is only ever written by two things: the chevron somebody clicks, and the root at depth zero. **A row that has never been opened is not opened by gaining a child**, and an EMPTY row has no chevron at all to open it with -- so a `Part` created inside a fresh `Folder` was invisible EVERY time rather than intermittently, which is what made this read as "the button does not work". Diagnosed by reading, and the mechanism is deterministic enough that reading settles it: there is no state in which an empty folder could have been open. **Fixed 2026-08-23** the way every editor with a tree fixes it -- making a thing reveals it. `Inspector::reveal` is a one-shot request and `collectAncestors` is the way down; the panel drains it BEFORE it decides visibility, so the row appears on the frame the plus was pressed rather than one later. One-shot because a request that stayed set would re-open, every frame, a row somebody had deliberately closed; and dropped by `onWorldChanged` because a different world recycles slot indices and the id would name something else. **`createInstance`, `duplicateInstances` and `reparent` all raise it**: dropping something into a collapsed folder and watching it vanish is the same defect arriving through the other verb, and it shipped in the same session as the drag that makes it reachable. Break-verified by removing the one call and watching `editor_tests` fail |
| D076 | human, 2026-08-23 | A `UDim` or `UDim2` in the properties grid is two or four unlabelled boxes -- there is no way to tell scale from offset | fixed | **A box is for a value, and nothing on the screen said which value.** `x y z` has a convention to fall back on and a scale-and-offset pair does not, so `UIPadding.PaddingTop` drawn as two stacked boxes holding `0.000` and `10.000` is a widget nobody can read. The first fix put the word INSIDE the box -- `scale 0.000` -- and the human rejected it in one sentence, correctly: that is a box with a caption in it, and it spends the width the number needs. **Fixed 2026-08-23 the way every properties grid does it**: the name goes in the name column where every other name in this panel already is, and a composite value expands into a row per number -- `Scale` and `Offset` for a `UDim`, `X Scale` through `Y Offset` for a `UDim2` -- with the collapsed line carrying the same summary `formatValue` has printed since M6, so the two agree by construction. Open by default, because starting closed puts a click between a person and the property they opened the panel for. **What this found is worth more than the fix.** `inspector_fixture.h` has said "one property of **every** `ValueType`" since M4, with a `static_assert` on `std::variant_size_v<Value>` under it -- and counting is not covering. M6 appended `Vector2`, `UDim`, `UDim2` and `Rect` to the variant and no property in the fixture ever named them, so **four editor branches had been executed by no test at all**, including the one being rewritten. That is the M4 brief's entering risk 6 word for word, found exactly the way it predicted. All four are in the fixture now and the real-device overlay test draws every one of them |
| D077 | human, 2026-08-23 | The Explorer offers a plus INSIDE a streamed chunk -- `Chunk_-3_0_0/Ground` takes a child that the save skips and the next eviction destroys | fixed | **Reported as the opposite of what it is**: "the plus does not appear for a folder", then corrected on the next line -- it does not appear on the `Chunk_*` rows, and a folder made inside one had it. The chunk rows are RIGHT: nothing authored may live in what streaming materialised. What was wrong is everything under them. `Editor::authorable` walks the ANCESTRY and its own comment says why -- "a chunk marks its FOLDER and not its contents, which is exactly the economy that makes checking the instance alone wrong" -- and both the plus and `createInstance` asked `world.generated(id)` of the INSTANCE. So `Ground`, which streaming did not mark, wore a plus, accepted the create, and the thing somebody made was skipped by the serializer and destroyed by the next eviction without a word. The one row that looks dangerous was refused and the ones that do not were not. **Fixed 2026-08-23** by naming the rule once: `Editor::canParentInto` is the ancestry walk with no opinion about services -- a service IS a legal parent, `Workspace` holding a `Part` is what it is for -- and `authorable` is now that plus `!isEngineOwned`, so the two cannot drift. The plus, `createInstance` and `reparent` all read it. **And the chunks are hidden from the Explorer now** (`Window > Streamed Content`, off by default), which answers the question the human asked next: streaming pumps in EDIT mode as well as in play, deliberately -- a world you cannot see is a world you cannot edit, which is what Unreal's World Partition does in its own editor -- but a chunk is not part of the authored scene, the serializer skips it whole, and sixty `Chunk_x_y_z` folders standing between a person and the four things they wrote is the complaint the root's own hidden row already answered. A switch rather than a rule, because "what did streaming actually materialise" is a real question with no other way to ask it |
| D078 | human, 2026-08-23 | A part dragged by a manipulator arm does not stay on the arm -- "não segue exatamente a reta da movimentação, vai todo estranho para o sentido da seta" | fixed | **A comment that said the opposite of its own code.** `Editor::driveGizmo`'s translate branch read "Snapped in the GIZMO's frame rather than the world's, so a local drag lands on the part's own grid" and then called `snapTo(delta, step)`, which quantised each WORLD component independently. That is right for a world-axis drag -- the delta is axis-aligned, so rounding the three apart keeps it on the line -- and wrong for every other one: an arm in LOCAL space points diagonally through the world, so the three roundings pull the motion off the arm and the part wanders. Snapping is on by default at a quarter metre, so this was reachable the first time anybody turned a crate and dragged it. **Reproduced before it was touched**, and the reproduction is the shape of the test: a drag made DIAGONALLY on screen, because that is what a hand does and an arm is supposed to hold the motion to its line anyway, with the movement decomposed into "along the arm" and "across it". Across must be zero; it was zero in world space with and without snapping and non-zero in the part's own. **Fixed 2026-08-23** by doing what the comment promised: the delta is expressed in the gizmo's own basis, snapped there and turned back. An axis drag stays on its axis and a plane drag stays in its plane, because a component that was zero rounds to zero; in world mode the basis is the identity and the code is exactly what it was. **And the same question asked of the other two modes found a second defect** (D079). Rotate is right and was checked: the angle is a scalar, so snapping it cannot move anything off anything, and the turn is about the gizmo's own axis around the gizmo's own pivot |
| D079 | agent, 2026-08-23 | Resizing a rotated part with the world-space gizmo grows it along the wrong axis | fixed | Found by asking D078's question of the other two modes rather than by anybody hitting it, which is the cheaper way round. **A `Size` is three numbers in the part's OWN frame** -- there is no world-space size to change -- so `driveGizmo`'s scale branch projected the drag onto whatever axis the gizmo was showing and then wrote it into `size.x`, `size.y` or `size.z`, which are the part's. On an unrotated part the two agree and nothing looks wrong; on a turned one the arm points one way and the part grows another, and the handle is lying about what it does. **Fixed 2026-08-23**: `gizmoFrame` uses the part's own rotation whenever the mode is `Scale`, whatever the world/local toggle says -- which is what Unity's scale tool does and for this reason. The toggle greys out while resizing rather than reading "world" over local handles: a control that stayed live while changing nothing would lie twice, once by looking usable and once by naming the wrong space. Break-verified: three assertions fail with the rule removed |
| D080 | human, 2026-08-24 | Editing a stamp on its stage and saving does not move the linked instances already in the world -- "editei a stamp do content e não atualizou a stamp da workspace" | fixed | **Half of ADR 0051 was only true across a save and a LOAD.** "Changing a stamp changes every instance of it" was implemented where the format could see both trees -- the scene reader builds a stamped node from the file every time it opens one -- and nowhere else. A linked instance sitting in a running editor is an ordinary subtree carrying a mark, and nothing re-read the file while it sat there; `scene_file_tests` proved the rule on a round trip and the claim read as though it held generally. **Fixed 2026-08-24** with `scene::restamp`, which is the same arithmetic done live: what each instance has of its own is measured against the file it was built from, its children are rebuilt from the file as it stands now, and the overrides go back on top. **`before` is why the session carries the file's previous text** -- an instance that differs from the new file is either overridden or merely out of date, and only the text it was built from tells those apart. Rebuilt IN PLACE rather than destroyed and re-read: the instance keeps its id, its parent and its place among its siblings, so a reference held to it survives and an Explorer's rows do not jump on every save. A subtree whose shape has moved on is left alone and said out loud in the status line, which is the rule the writer already applied, from the other side. **Two limits, stated rather than found later**: a refresh is not an undo step (it is a change to a file, and the stage's history is what an undo here belongs to), and a reference held to something INSIDE a refreshed instance goes with the children that were replaced |
| D081 | human, 2026-08-24 | The content browser shows `charactertesting.stamp.json`, which reads as a JSON file rather than as a stamp | fixed | **`.json` is how the file is stored and `.stamp` is what it is**, and the browser is the one place that has to say the second. Reported alongside D080 and D082 in one message, all three about the same row. **Fixed 2026-08-24**: `ContentTree::displayNameOf` drops the trailing `.json` of a COMPOUND suffix -- asked of `extensionFor`, the one answer to "what part of this name says what it is", so a `.json` that is somebody's own data file keeps it. The name on disk is untouched, and the grid view's tooltip still shows it whole: this is a label, and every path, rename and load spells the whole thing |
| D082 | human, 2026-08-24 | A stamp in the content browser wears a generic icon; the same thing in the Explorer wears its class's, badged | fixed | Reported twice in one exchange and the second report is the better statement of it: not "the badge is missing" but **"o icone da stamp deveria ser o icone da instancia raiz"** -- a file of a character should draw as a character. The panel drew `class.Model` for every stamp, which was written as an honest stand-in for a `content.Stamp` drawing the set owes; the drawing is not what was missing. **Fixed 2026-08-24**: a stamp row wears `class.<its root's class>`, read from the head of the file at refresh and cached on the entry, with the kind's own icon as the fallback and the stamp badge drawn over it. **A prefix rather than a parse**, and the size is the argument: a stamp of a whole character is hundreds of kilobytes and a folder can hold forty of them. The badge is what now carries "this is a file one comes from" -- the same mark the Explorer puts on an instance, read the other way round -- so `content.Stamp` is no longer owed at all |
| D083 | human, 2026-08-24 | The editor opens on `stats` rather than `properties`, and forgets the manipulator's space and the window's geometry between launches | fixed | Asked for as a standing rule rather than as a bug -- "o layout em si do editor deve ser lembrado ... por padrão" -- and it is three things wearing one sentence. **The docking, the panel sizes and which tab is open were already remembered**: ImGui writes `.luaug/layout.ini` for `Shell::Editor` and that includes a dock node's selected tab, so this half was working and only its DEFAULT was wrong -- a tab node opens on whichever window was docked last, and `stats` was docked after `properties` by accident of call order. **The manipulator's mode and space, snapping and the browser's layout were remembered nowhere**, which is the half that was actually missing: they are somebody's answer to "how do I work", and an editor that asks it again every launch is one they answer by hand every morning. **The OS window's own geometry was remembered nowhere either**, and the layout file cannot hold it -- that one knows about the panels INSIDE a window and nothing about the window. **Fixed 2026-08-24**: the tab is chosen explicitly rather than falling out of dock order; the toolbar state and the browser's view go in `.luaug/editor.json` as WORDS a person can read (`"space": "local"`, not a flag); the window's placement goes there too, read by a static before the window is created so it OPENS at the right size rather than opening small and jumping. Three details that are the difference between it working and nearly working: the preferences are drained by one flag once a frame rather than written from inside each keystroke handler, the placement is asked for once a frame and compared rather than subscribed to (a move and a resize are several events each and none of them is the last), and a maximised window keeps the geometry it will have when it is NOT maximised -- SDL reports the screen it fills, and storing that hands somebody who un-maximises it a window the size of their display. The ini is also written on the way out now, because ImGui saves it on a five-second timer and an arrangement made just before quitting is one made twice |
| D084 | agent, 2026-08-24 | The partitioner leaves in the boot scene everything it moved into cells, so a partitioned world is loaded TWICE | fixed | Found while proving that `examples/10-open-world` really streams after its terrain moved into `Workspace` (ADR 0053). The boot log is right and the file is not: **"Partitioned the scene into 115 cell(s) holding 15363 instance(s); 204 stayed in the scene"**, and the scene it then writes and boots -- `.luaug/partition/<hash>/scene.json`, written at `partition_cache.cpp:229` and returned as `outcome.scenePath` -- still holds **15,568** instances, of which 15,541 are the `Scenery` folder the cells were made from. Both halves are live: a 3,000-frame soak reports `earlyInstances 25311 -> peakInstances 26498`, which is the whole scene plus a resident ring of cells materialised on top of it. **Streaming itself is healthy** and that is what makes this easy to miss -- `worstStreamingMs 1.712` against a 2 ms budget, `hitches 0`, and the resident count rises and falls as the focus moves. What it costs is the frame: median 16.1 ms with the duplicate against 10.3 ms measured on the same world before the autopilot's long leg, and roughly twice the instance memory. The duplicate is invisible because the copies are coincident -- a duplicated `Ground` tile is exactly under the streamed one, so the picture is right and only the counters say otherwise. **Mine, and the walk asked one question where it needed two.** `visitChildren` tracked whether a direct child had been REMOVED and nothing else, so a child that stayed and was itself rebuilt -- because something under IT left -- left the parent believing nothing had happened, and the parent then copied its own original text over the top of that rebuild. Every part of `Scenery` was correctly emitted into a cell, correctly spliced out of `Scenery`, and then the spliced `Scenery` was discarded when `Workspace` wrote itself out verbatim. A node now reports which of three things it did -- removed, verbatim, or rewritten -- and a parent rebuilds for either of the last two reasons. **Every test in the suite had its streamable parts as direct children of `Workspace`**, which is the shape the flagship stopped having when its terrain moved into a folder; three tests now put a part one, two and three folders down, and a fourth asserts the property this defect really wants -- partitioning an already partitioned scene moves nothing, byte for byte. Measured after: the boot scene holds 204 instances against 15,568, and the flagship runs at a median 4.80 ms against the 16.1 ms recorded here. **Confirmed independently by the reporter** on a cold partition and a second 3,000-frame soak of the same world: median **16.1 -> 5.90 ms**, p99 **23.6 -> 9.18**, worst **64.9 -> 11.07**, peak resident 181 -> 168 MiB. The instance count still rises and falls with the focus (11,102 -> 7,807), which is the streaming this defect was found while trying to prove |
| D085 | human, 2026-08-24 | Pressing F3 in `examples/10-open-world` turns the whole window black | fixed | Reported while trying to watch streaming happen. **Not reproduced here and it cannot be**: the ImGui shell does not render headlessly and SDL does not accept injected input (E1's brief), so there is no way from this side to press F3. What follows is what reading ruled out and what it did not. **Three obvious causes are eliminated.** A saved layout with a huge window: `io.IniFilename` is null for every shell but the editor (`debug_overlay.cpp:3499`), so the panel is 420x520 on every launch. The overlay clearing the frame under itself: its pass is `loadOp = LoadOp::Load` with a comment saying why (`:3641`). A dockspace covering the viewport: `DockSpaceOverViewport` is inside `drawEditorShell`, and a game shell calls `drawShell`, which opens one window. **What is left is the instance count, and it is very likely self-inflicted.** `drawShell` calls `drawExplorer` with `showGenerated = true` -- deliberately, to show the world as it IS rather than as it was authored -- and the Explorer's row pass is `collectTree(world, root, g_rows)`, the WHOLE tree every frame, with `g_visible` filtered from it afterwards. The flagship's terrain moved into `Workspace` the same day (ADR 0053) and D084 duplicates it, so that world now runs at roughly 24,000 instances against the few hundred this panel was built against. A frame that stops being presented is a black window. **The discriminating test is thirty seconds**: press F3 in `examples/06-scene`. Working there and not here is the instance count; black in both is the overlay and predates this  **The discriminating test was in the log, and it says EDITOR.** `luaug.log` from the session carries `81 icon(s) in a 1024-square atlas`, and the icon atlas is built behind `options.editor` and nowhere else (`engine.cpp:2019`); it also carries `Loaded a scene of 15567 instance(s)` with no `Partitioned the scene into...` line, which is the editor skipping the partition on purpose. So `drawShell` was never running and the instance count is not this -- the reporter pressed play in the EDITOR and then F3. **And in the editor F3 hides the only thing that draws the world.** The editor renders into an offscreen TEXTURE sized to the viewport panel, the screen itself is only cleared to `{0.06, 0.06, 0.07}` as a backdrop (`engine.cpp:2116`), and `DebugOverlay::render` returned early on `!visible_` -- so hiding the panels left that clear as the entire picture. A black window, exactly as reported, and true since E1 rather than anything ADR 0053 did. The overlay's own header calls F3 "the cheapest way to look at the world without the furniture", which is the one thing it could not do. **Fixed 2026-08-24**: the editor shell now draws while hidden, and what it draws is the viewport alone -- a borderless window over the whole screen, sharing `drawViewportBody` with the docked panel so the aspect ratio, the picking ray and the image agree whichever is up. A window of its own rather than the docked one undocked, because the layout somebody arranged is theirs and a keystroke that rearranged it would cost more than it showed. **What this rules out is worth keeping**: the Explorer walking the whole tree every frame in `drawShell` is a real cost at twenty-four thousand instances and it is not what happened here; it gets its own row when somebody hits it. |
| D086 | agent, 2026-08-24 | The released v1.0.0 binary reports its version as 0.0.1: `project(LuauG VERSION ...)` was never bumped | fixed | Found while writing the packager (ADR 0054), which names the folder after that declaration and produced `LuauG-0.0.1-win64` for an engine tagged `v1.0.0` and released on 2026-08-22. It is not a stale copy -- ADR 0031's rule held perfectly, and everything downstream reported the number faithfully: `LUAUG_VERSION_STRING` in the generated `build_info.h`, `luaug --version`, `api/api-dump.json`, and the engine's own boot line. **What had no owner was the declaration itself.** A version that only ever moves when somebody remembers to move it is exactly the shape this project keeps designing against, and the reason nobody noticed for a whole release is that the one artifact that would have put the number in front of a person -- a folder named after it -- did not exist until now. **Fixed**: bumped to 1.0.0 with the api-dump regenerated beside it. What is NOT fixed is the mechanism: nothing checks that a `vX.Y.Z` tag and the declaration agree, and the honest next step is a docs-gate rule comparing the newest tag against `CMakeLists.txt` |
| D087 | human, 2026-08-24 | `luaug edit examples/10-open-world` opens a black window | fixed | Reported the day the packaging milestone landed, and caused by it: `scripts/package.ps1` builds the `player` profile, and `findEngine` had been trying player presets FIRST since D057 -- which cost nothing for as long as no machine in the world had a player build. The moment one did, every `luaug edit` and `luaug dev` launched a binary with `LUAUG_DEBUG_UI=OFF`, so there is no ImGui target linked into it at all and `DebugOverlay` degrades to a class whose methods do nothing. In edit mode the world renders into an offscreen texture and the screen receives only the backdrop clear, so the panels are the only thing that draws anything: no panels, no picture. It ran, it exited zero, and it was black. **Reproduced before it was diagnosed**: the CLI's own resolution reported `win-msvc-player/engine/app/luaug-host.exe`, which is D085's window with a different cause -- the same symptom, the second time, from the opposite direction. **Fixed** in two parts, because ordering alone would only move the luck: the player presets are now the LAST thing `findEngine` will take, after dev and after editor; and `luaug edit` asks the binary what it is rather than inferring it from where it was found -- `hostProfile` parses the profile out of `--version`, and a host with no debug UI is refused by name with the two commands that fix it. `luaug dev` warns instead of refusing, because a player host does run a project; what it cannot do is F3, the inspector and the REPL. Break-verified with `LUAUG_HOST` pointed at the player build, which is now refused rather than launched. **The general shape**: a preference that is right only because nothing has exercised the alternative is a preference nothing is holding. |
| D088 | human, 2026-08-24 | A blank project opened in the editor shows a background cycling through colours instead of a world | fixed | Reported minutes after E6 shipped the launcher, and reachable for the first time BECAUSE of it: until there was a New Project button, nobody opened a project that had no scene and no camera in it. The rainbow is `pulseColor` -- the M1 "the engine is alive" clear, which `engine.cpp` falls back to whenever `snapshot.camera.valid` is false, and which every example since M2 has hidden by having a camera. **The chain is four links and each one is reasonable.** `editor.adoptCamera` is seeded from `Workspace.CurrentCamera` so that opening a project does not teleport the view (D061's design); a project with no scene has no camera to adopt, so `cameraAdopted()` stayed false forever; `useEditorView` requires it, so `extract` received no `ViewOverride`; and `extract` with neither a world camera nor an override leaves `out.camera.valid` false, which is the pulse. **Fixed**: with nothing to adopt, the editor adopts its OWN default frame, so the editor always has a camera -- which is what ADR 0046's "the tool owns the machine while it is editing" already implied, and the adopt-if-present path is untouched. **The empty world itself is not the defect**: a project whose world is built in script is empty in the editor until play, which is ADR 0047 working as designed. What was wrong is that emptiness rendered as a rainbow. |
| D089 | agent, 2026-08-24 | `core::JsonDocument::parse` refuses a UTF-8 byte-order mark, so an engine JSON edited in Notepad is ignored in silence | fixed | Recorded as a finding by the other session while building the appearance file and fixed here, because it is `core`'s rather than the shell's: **every** JSON this engine reads goes through one parser -- the scene format, the chunk index, the partition manifest, the project list, `appearance.json` -- and all of them are files a person may open in an editor. Notepad writes a BOM by default, so does Windows PowerShell's `Out-File` for UTF-8, and so does Visual Studio; the parser saw three bytes where a value belongs and answered `expected a value at byte 0`, which every caller here turns into "there is no such file" and carries on. **RFC 8259 leaves this to the implementation** -- it says a parser MAY ignore a BOM -- so the old behaviour was not wrong, it was useless against the files people actually have. **Fixed**: the prefix is skipped at the start of `parse` and nowhere else. A BOM in the middle of a document is a zero-width no-break space in somebody's data and is left alone, and a document that is nothing but a mark is still not a document. Break-verified: with the skip removed the first case reports `expected a value at byte 0`, which is the symptom itself. |
| D090 | human, 2026-08-24 | Double-clicking the packaged `luaug-host.exe` opens a console that closes immediately | fixed | Reported against a folder that was HALF DELETED, and the packager did that: `removeTree` let `removeDirectory` walk the tree and fail when it reached `luaug-host.exe`, which Windows will not unlink while it is running. What was left had the binary and none of the content -- so the host started, found no icon theme and no fonts, could not bring the shell up, and exited. **A half-deleted installation is worse than one that was not cleared at all**, because it behaves like a defect in the engine rather than like a step that failed. **Fixed**: the binaries that can be locked are removed FIRST, one at a time, so a failure there has deleted nothing and says which file is in use and what to do. The earlier half of this -- the stack trace from inside `fs.remove` -- was already replaced by a sentence in the same session; this is the half that mattered. |
| D091 | human, 2026-08-24 | Pressing play in a project with no camera shows a background cycling through colours | fixed | The other half of D088, and it survived that fix because the fix was scoped to editing. `useEditorView` required `editing(runState)` -- deliberately, because pressing play must show the GAME through the game's own camera rather than a tool's idea of it -- so play mode dropped the override, a project whose scripts never made a `Camera` had nothing to render through, and the frame fell back to the M1 "the engine is alive" pulse. **Fixed**: the editor's camera is the fallback and never the winner. A game with a camera is still shown through its own; a game with none is shown through the editor's, which is what every editor in this shape does and what was asked for in those words. The breadcrumb rectangle reported in the same message is the same shape as the frameless icon buttons: `SmallButton` draws a filled, bordered box that was invisible until a theme drew borders, and a path wants words you can click -- so the steps lost the frame and gained the underline every file manager uses instead. |
| D092 | human, 2026-08-24 | A two-minute sound with `Playing` true plays for about a second and stops -- "o som em si tem 2 minutos e pouco mas na nossa engine ele só toca poucos segundo e para" | fixed | **`AudioSystem::tick` measured every sound in the world against a one-second constant.** `kPlaceholderDuration` was right for the whole of M6 and its name says why: nothing decoded a file then, so every sound WAS a one-second generated tone, and the timeline that `Ended` fires from had exactly one length to know about. M7 made `Content` real -- `clipFor` decodes WAV, MP3, FLAC and Ogg through miniaudio and the mixer plays the samples -- and nothing went back to the timeline. So a decoded sound was mixed from its own file and ENDED on the tone's clock: at one second `TimePosition` reached the constant, `Ended` fired, `Playing` went false, and the voice retired with a hundred and nineteen seconds of audio left in the buffer. The `Ended` it raised while doing so was a lie about the file, and any script awarding something when a jingle finishes got it early. **Fixed 2026-08-24**: the length is `clipDuration(sound.content)`, which is the decoded clip's frame count over the sample rate and answers from the same cache `update` reads -- a lookup per playing sound per tick, not a decode. A content that names nothing is still one second, because the tone is still what such a sound plays, and that is now the only thing the constant means. **Break-verified**: with the constant back, `Ended fires at the end of the FILE` reports the sound stopped at exactly one second, which is the symptom itself. The fixture grew a three-second file for it -- every case here is a comparison against one second, and a fixture that was only ever shorter than that could not tell a sound measured by its file from one measured by the constant |
| D093 | human, 2026-08-24 | The same sound is audibly distorted while it plays -- "enquanto toca ele fica meio que estourado" | fixed | Reported in the same sentence as D092 and it is a second, independent defect: that one is when the sound stops, this is what it sounds like until it does. **The mixer re-read its cursor from `TimePosition` every frame.** That was a deliberate design and its reasoning is sound -- the timeline is the SimClock's (M6 brief, Decision 9), and a mixer whose position the simulation could read would be the wall clock coming in through a side door (R10). What it missed is that the two clocks are quantised differently and only one of them is smooth: `TimePosition` advances in whole fixed steps inside the tick loop, the cursor advances in whole device buffers, and `update` runs once per RENDERED frame. A frame in which no tick ran therefore dragged the cursor BACKWARDS by a frame's worth of samples and the mixer replayed sixteen milliseconds it had just played; a frame in which two ticks ran skipped forward. At 144 Hz against a 60 Hz sim that is most frames, and a buffer that jumps back and forth sixty times a second is exactly what "estourado" sounds like. **Fixed 2026-08-24**: the mixer keeps its cursor and SEEDS it from the timeline, taking the timeline only when the two have genuinely parted company -- `detail::shouldTakeTimeline`, a quarter of a second, which is far larger than a tick plus a buffer and far smaller than any seek anybody performs on purpose. A looped sound is compared the short way round the loop, or the frame in which the mixer has wrapped and the timeline has not would be read as a seek and click at the top of every loop. **The invariant that mattered is untouched, because it was never about this number**: nothing in the simulation reads the cursor, `TimePosition` still advances by `FixedTimestep * PlaybackSpeed` per tick, `Ended` still fires from it, and a replay still reproduces both exactly. What changed is only what the speakers do between two ticks, which the API's own words already called downstream of the simulation and never an input to it. Those words were also wrong in one place and are corrected: the `Content` doc said the position "comes from `TimePosition` every frame" |
| D094 | agent, 2026-08-24 | An Ogg Vorbis file cannot be decoded at all, so a format the API documents as supported plays the placeholder tone | fixed | Found while fixing D092, in the line above the one being read: `clipFor` required `ma_decoder_get_length_in_pcm_frames` to succeed before it would allocate anything, and miniaudio's own header says that call returns `MA_NOT_IMPLEMENTED` "such as with Vorbis decoders". `Sound.Content`'s documentation has listed "WAV, MP3, FLAC and Ogg Vorbis" since M7. So an `.ogg` resolved, read, initialised a decoder, failed the length probe, was cached as a null clip and counted in `AudioClipsMissing` -- which reads as a missing file rather than as a format the engine will not decode. **Not caught by anything, and could not have been**: the audio fixture writes its own WAV rather than checking a binary into the repository (ADR 0032), and a test that writes an Ogg would have to link an encoder to do it. **Fixed 2026-08-24**: the declared length is a HINT and reading until the decoder stops is the answer -- one second at a time when there is no hint, and the buffer trimmed to what actually arrived. It also removes a smaller error in the same place: with a resampler in the path the estimate is a frame or two out either way, which was a clipped tail or a silent one on every file whose rate is not 48 kHz, which is most of them. The duration cases assert the exact length of a 44.1 kHz file through that resampler, so a read that stops short now fails a test rather than shortening a sound |
| D095 | human, 2026-08-24 | Escape with the add-child menu open closes the menu AND clears the selection, in one press | fixed | "o esc está fechando a caixa de adição de instancia mas também está removendo a seleção da instancia" -- and the two happened on the same keystroke, so the instance somebody had opened the menu for was gone by the time the menu was. **Both halves are correct on their own and neither could see the other.** The shell's Escape handler runs last, after every panel, and refuses while a popup is open -- deliberately, because a modal is closed by Escape and taking the key from one would make the shell's own dialogs unclosable. The add menu asks for Escape itself, for the same reason every transient thing on a screen does. What is not true is the assumption joining them: `ImGui::CloseCurrentPopup` takes effect IMMEDIATELY, so by the time the shell's guard asks whether a popup is open the popup it is guarding against has already gone, and the press arrives as though there had never been a menu. **Fixed 2026-08-24**: the popup says it took the key and the shell's handler consumes that flag beside its own guard. One Escape, one thing -- close the menu, then press it again to drop the selection, which is what was asked for and what every editor in this shape does. Not covered by a test and this says so plainly: the ImGui shell does not render headlessly and SDL does not accept injected input (E1's brief), so there is no way from this side to press a key |
| D096 | human, 2026-08-24 | Opening a project in the editor RUNS every entry script's file scope, so a world nobody authored appears before anything is played | fixed | Found with the starter template in about a minute: `templates/starter`'s `main.luau` calls `Instance.new("Part")` at file scope, so a project that has never been played has a `Spinner` in its Workspace beside the authored one -- and a `RunService.Heartbeat` connection made in the same file scope already holds it, so deleting it while STOPPED leaves a live connection writing to a destroyed instance. `WorldHost::boot` calls `script::startScripts` with no condition on it. **The tick is already gated and that is what hid this**: `worldAdvancing` stops `Heartbeat` from firing while editing, so everything per-frame is correctly quiet; what is not gated is the one-shot, and a file scope is not a tick. **Sixth instance of ADR 0046's shape** -- the editor inheriting the game's decisions instead of taking them -- and the first found after that rule was written, which is worth saying: the rule existed and this call was not checked against it. D067 is the nearest neighbour. Settled by **ADR 0058**: boot mounts, play starts, stop tears the VM down. **Fixed 2026-08-24**, in two halves that only work together. Boot MOUNTS and the editor does not start -- the `Script` instances are in the tree, the Explorer shows them, `Source` is editable and a tab can open one, and a game, `luaug dev`, a headless run, the conformance runner and a replay all start them at boot exactly as before. And stop TEARS THE VM DOWN: `WorldHost::restartRuntime` throws the runtime away and builds another on the same world, adopting the `DataModel` the world already holds -- `registerServices` was the only thing that unconditionally CREATED one, everything under it was already found -- and carrying the mount table across, because which file a `Script` came from is a fact about the project rather than about the VM. **The second half needed a better test than the obvious one.** The first attempt asserted through a `Heartbeat` connection and the world restore alone killed it: `RunService` is created by a script's first `GetService`, so restoring a pre-play snapshot removes the service the signal belongs to, and the case passed with the teardown deleted. What isolates the VM is a `require`d module's cached result and a `task.delay` due after the stop -- break-verified, and with the teardown removed the second play sees the counter already at one and a world nobody is playing grows a part on its own. **The starter template changed with it**: the scene holds the `Spinner` and the script only spins it, which is what `examples/10-open-world` already does and what the template should have been teaching. D097 is what the conformance spec found while this was being written |
| D097 | agent, 2026-08-24 | Destroying a `Script` does not stop it: its connections go on being invoked, forever | fixed | Found by the conformance spec written for ADR 0059, and found the way these are worth finding -- as a leak between cases. A `Script` whose `Heartbeat` handler makes a part was destroyed by `afterEach`, and it went on making parts inside every LATER case's sandbox, interleaved with a second copy of itself from a re-enable. The spec now leaves its own target behind rather than depending on the answer, and the case is written so it cannot mask the question. **Why it is not fixed here**: `Enabled` suppresses resumption for a live, disabled `Script` (ADR 0059 rule 2), and `resumptionSuppressed` deliberately answers false for an id that is no longer alive -- so a destroyed script falls through every check and its threads are resumed like anybody else's. Making it answer true would be easy and would be a decision, not a fix: ADR 0058 declines whether a connection dies with the instance its closure captured and ADR 0059 declines it again, and this is the same family one step over. **What makes it worth its own row rather than a comment**: the `Script` class's own documentation already says "one that is not in the world does not run -- which is the whole difference between storing a script and using it", and that sentence is currently false in one direction. A destroyed script is not in the world and it runs. The question for whoever decides it is narrower than connection lifetime and probably answerable on its own: should `Destroy` on a `Script` stop its threads being resumed, the way `Enabled = false` does, and does anything it already built stay -- which rule 2 says it does **Decided and fixed 2026-08-25, autonomously**, because the row itself says the question is narrower than connection lifetime and answerable on its own -- and because the alternative was leaving a sentence in the `Script` class documentation that is false in one direction. **A destroyed script is stopped.** `Enabled = false` already suppresses resumption, and it would be incoherent for the weaker operation to stop a script and the stronger one not to; nothing can ever re-enable a destroyed one, so its threads were a leak with no way back. What it already built stays, exactly as rule 2 says for `Enabled`: a part a script made before it died is a part, not a script. `resumptionSuppressed` asks about liveness BEFORE class, because a retired instance has no class to ask -- and destroyed-but-not-retired counts, since `World::destroy` unlinks and marks and a paused world may not drain for many frames. Three conformance cases, break-verified: with the rule removed the connection case fails, the `task.delay` case fails, and `CharacterBody > falls, lands` fails as well -- which is the original symptom, the destroyed `Ticker` leaking into a later case's sandbox, reproducing exactly as first reported |
| D098 | agent, 2026-08-24 | A scene with two parts in it is partitioned into a streaming grid, and a project that never registers a focus then shows an empty world | fixed | Measured while giving `templates/starter` the scene ADR 0058 requires it to have. A scene holding a camera, a ground and a spinner logged **"Partitioned the scene into 2 cell(s) holding 2 instance(s); 1 stayed in the scene"**, and a headless run of it printed the script's greeting and then nothing: `workspace:WaitForChild("Spinner")` was still waiting sixty frames later. **Two separate things make it happen and each is defensible alone.** `partitionProject` runs for every non-editor run with a scene and has no floor -- `outcome.active = !outcome.index.chunks.empty()`, so two loose `Part`s are enough -- and `StreamingHost::collectFoci` walks `World::streamingFoci()`, which nothing populates but `StreamingService:AddFocus`. So a world with no registered focus loads no cell, ever, and every streamable instance in it is in a cell. The flagship registers its character and is fine; a project somebody just made is not, and the failure is silent in both directions -- the log says the world was partitioned, which reads as success, and the world says nothing at all. **Worked around rather than fixed**: the starter's world is a `Model` with `StreamingMode = "Persistent"`, which keeps it out of the grid entirely, and the template says why in a comment. That is the right answer for a template and the wrong answer for the general case, because it requires knowing about streaming to place two parts. **What it needs is a decision, and there are three shapes.** Partition only past a floor -- a scene of five parts is not a world that streams. Or make the camera a focus by default, which is what a person means by "show me the world" and what the editor already assumes. Or refuse to partition a project that registers no focus and say so, which is the cheapest and the most honest. The first two change what existing projects do; the third only changes what they are told **Decided and fixed 2026-08-25, autonomously.** Of the three shapes the row offers, the second is the one that cannot silently produce an empty world -- but making the camera a focus ALWAYS would change what every existing project does, which is the objection the row raises against it. So it is narrowed to exactly the broken case: **a world whose focus set is empty is watched from `Workspace.CurrentCamera`.** A game that registers its own foci never reaches the fallback, so nothing that works today changes; the only worlds that gain a focus are the ones that had none and were therefore showing nothing. A world with no camera either still collects nothing, which is the honest answer -- a focus at the origin that nobody asked for would load whichever cell happens to sit there, and one arbitrary cell is harder to diagnose than none. The deleted-camera case is covered too, because `CurrentCamera` is a reference and an id stays structurally valid after the instance is gone -- D100's shape, arriving where it would have been a focus reading a component that is not there. `collectFoci` became public to be asserted directly: it IS the rule, and a frame is not something a test can run. Four cases in a new `engine/app/tests/streaming_host_tests.cpp`, break-verified. **The floor is deliberately NOT added**: partitioning a two-part scene is wasted work and a misleading log, but it is no longer a way to lose a world, and a floor changes what existing projects compile to |
| D099 | human, 2026-08-24 | `RunService` and `HotReloadService` appear in the Explorer only after you press play -- "todos os serviços deveriam aparecer no explorer tanto no play quanto no edit" | fixed | Reported the same day ADR 0058 landed and caused by it in the way a change surfaces something rather than breaks it: services were created by their first `GetService`, entry scripts called `GetService` at file scope, and file scope ran at boot -- so a project opened in the editor showed a full set of services because a script had just made them. Scripts start on play now, so a project nobody has played shows only the five that had earned their way into boot one at a time, and stop restores a world that has them again -- a tree whose shape depends on whether a script has run. **The five were never five decisions.** `Workspace` and `ScriptService` because a script reaches one through a global and the other is the mount point; `Lighting` because the renderer reads the environment every frame whether or not anybody asked, and M4 spent four milestones lighting every scene with struct defaults before it got its turn; `UIService` because the frame lays out every `ScreenGui` under it; `AudioService` because the mixer reads `MasterVolume`. One decision found five times, each time after something had already read a service nobody had created. **Fixed 2026-08-24**: every class carrying the `Service` flag exists from boot, found by the flag rather than by a list, so a service added next year is there without anybody remembering. **The order is a second decision and it is about reading**: `Workspace`, then `Lighting`, then every other service by name. The two at the front are what a scene IS and the two an editor opens into; past those a dozen names is a list scanned by name, and sorting by name is also the only ordering that is the same on every machine -- registration order is a fact about which module was compiled in and in what sequence, which R10 forbids reaching observable output. **What it narrows**: `FindService` used to answer "is this service in use" and now answers the same as `GetService` for every real service, which the IDL says. Both traces re-recorded on both tiers: the services are world state and the hash walks it |
| D100 | human, 2026-08-24 | Deleting the scene's camera and pressing play shows the background cycling through colours and a bare grid | fixed | The third arrival of the same rainbow, from a direction neither D088 nor D091 covered, and the first that is not about a world with no camera at all. **`Workspace.CurrentCamera` is a REFERENCE**, and deleting the camera in the editor leaves the property naming a dead id -- an `InstanceId` stays structurally valid after the instance it named is gone. `engine.cpp` asked `host->currentCamera().valid()`, which is "is the field set"; `render_world.cpp`'s `extract` asks `world.alive(id) && !world.destroyed(id)` and then whether there is a `CameraComponent`, which is "can I draw through it". The two disagreed for exactly one world -- one whose camera had been deleted -- so the editor's override was withheld on the grounds that the game had a camera, and `extract` then found it could not use the one it was pointed at. No camera, no view, and the frame falls back to the M1 "the engine is alive" pulse. **Fixed 2026-08-24**: the question is asked the same way in both places. A predicate that two files spell differently is a predicate that will disagree, and this is the third time this particular disagreement has produced this particular rainbow |
| D101 | human, 2026-08-24 | The editor still opens on `stats` rather than `properties`, which D083 recorded as fixed | fixed | Reported in the same words as the first time, which is the tell. D083 chose the tab explicitly instead of letting it fall out of dock order, and the call it used -- `ImGui::SetWindowFocus` -- does not select a tab in a dock node. **That is not a guess**: ImGui's `FocusWindow` carries the line that would do it and the line is commented out, with its own note (`// Select in dock node ... for #2304 we avoid applying focus immediately before the tabbar is visible`). So the focus moved and the tab did not, and the node went on opening on whichever window was docked last, which is what it had always done. **Fixed 2026-08-24** through what ImGui does read: the node's `SelectedTabId` when a tab bar is built, and its `NextSelectedTabId` when one already exists. Setting both is the same request asked of whichever state the node is in. **And once for a layout that predates this**, which is the half that decides whether anybody sees the fix: every project arranged before now has `stats` written into its `layout.ini` -- not because somebody chose it, but because it was docked last -- and Reset Layout would fix that while throwing away every split and panel size they had built, which is a bad trade for a tab. So `.luaug/editor.json` carries a `layoutRevision`, the new default is applied once against a layout that predates it, and a person who chooses `stats` afterwards keeps it. **The general shape**: a fix verified by reading the call site and not by watching the pixel is a fix that can be recorded as done while doing nothing |
| D102 | human, 2026-08-24 | The audition button on a `Content` row is bigger than the field beside it, so that row is taller than every other row | fixed | Reported as "o tamanho das box checkbox play etc.. tao meio zuado" against a `Sound`'s properties, with a screenshot showing the green triangle pushing past the column. Mine, from the day before: `iconButton` sizes the PICTURE and the button then adds `FramePadding` around it, so asking for `GetFrameHeight()` worth of icon produces a button taller and wider than a frame -- the one thing every other widget on a property row is. **Fixed 2026-08-24**: the glyph is what is left of a frame after its padding, which is the font size, and the width the row reserves is what the button will actually measure. One row, one height. The checkboxes reported alongside it are `GetFrameHeight()` squares already and are left alone: a checkbox is small because a checkbox is small, and stretching one to the width of a text field would be a control that looks like it takes a value |
| D103 | human, 2026-08-25 | Remove does nothing on a launcher row whose folder was deleted: the click reports "is not there any more" instead | fixed | Reported with a screenshot of six orange rows and the message the ROW produces sitting under them, which is the tell -- the button's click was landing on the row underneath it. **Two independent halves, and either alone would have looked like the same nothing.** The button was submitted after a `Selectable` covering the whole row and without `SetNextItemAllowOverlap`, so ImGui gave every pixel to the selectable: Remove was drawn, never hovered, and the press went to the row, which for a missing project says exactly what the reporter saw. It was also emitted only while the row was hovered, which is the Explorer's plus-sign defect from the other side -- an item that comes into being on the frame a row becomes hovered is an item whose first press the thing underneath has already taken. **And the list was only ever written when a project was OPENED**, so even a working button would have removed the row until the launcher closed and every one of them came back. **Fixed 2026-08-25**: the overlap is allowed, the button is emitted on every row every frame and drawn at zero alpha when the pointer is elsewhere -- alpha is a drawing property and not a hit-testing one, and the rows where it is invisible are the rows nobody can press it on -- and the hover is asked of the row's RECTANGLE rather than of the selectable, because with the overlap allowed the selectable stops reporting hover the moment the pointer reaches the button, and a button that vanishes when you point at it cannot be clicked by anybody. The save moved into `applyProjectDecisions`, drained by the loop like every other decision this shell takes. **That function exists because of D101's own lesson**, which was written the day before: the loop needs a window, a GPU and somebody clicking, so "the loop calls save" was a claim nobody could check -- break-verified now, and with the drain returning false the row is back in the file. The click half is not covered and this says so plainly: the shell does not render headlessly and SDL does not accept injected input (E1's brief). A message says what happened, including that the folder was not touched |
| D104 | human, 2026-08-25 | Ctrl+S in the script editor flashes the whole screen, and before that lost the panels, the tabs, the caret, the Explorer's expansion and the selection | fixed | **Six reports, six patches, and one wrong decision underneath all of them.** Ctrl+S wrote the file and then rebuilt the world, so that a running VM would pick the change up. Each symptom was fixed as it arrived: the panels were put back (`overlayVisible` is world state and a fresh `EngineState` says false), the tabs were re-opened by chunk name, the caret and scroll were carried across, the Explorer's expansion was preserved, the selection was re-applied. **The patches were the tell.** A fix that has to put six unrelated things back is not a fix, it is a receipt for the wrong operation -- and the sixth report, a full-screen flash, is what a world rebuild looks like when there is nothing left to lose. Saving a text file must not destroy a world, and it never needed to: the pane writes `Source` on the instance as somebody types, so the world already holds the new text, and ADR 0058 makes PLAY what compiles `Source`. In the editor there is no running chunk to refresh. **Fixed 2026-08-25**: Ctrl+S is a `writeTextFile` and a `markSaved`, and five of the six patches were deleted with the reload that made them necessary. The sixth stays, because it was real on its own -- the dev server has had it since M3 and nobody noticed, since `luaug dev` starts with the overlay hidden anyway. Breakpoints moved to the play/stop cycle, which is what owns the VM they are patched into. Verified by driving the real window twice: the editor after Ctrl+S is pixel-identical to the editor before it |
| D105 | human, 2026-08-25 | Autocomplete in the script editor never offers anything from the project: `Workspace.` lists class members and no instances | fixed | Reported together with four others found by using the editor, and this is the one that matters. **Completion knew every class the engine ships and nothing at all about the project open in front of it**, so the one name somebody types most -- something in their own Explorer -- was the one name it could not produce. That is not a limit of the design (ADR 0057 chose the engine's reflection over `Luau.Analysis` for compile-time reasons that still hold); it is a source that was never read. The tree is a pointer away. **Fixed 2026-08-25**: a dotted path is walked instance by instance from the DataModel and offers that instance's children beside its class's members, a name inside `WaitForChild("` or `FindFirstChild("` is completed from the same walk, and `GetService("` from the service flag. There is **no list of service names in the completion code at all** -- a service is a child of the root -- so one registered by a module this build has never heard of resolves for free. One row per name, because six parts called `Ground` insert the same six characters; and a string that is nobody's argument now offers nothing, where before it popped every keyword in the language over whatever somebody was writing. The four found beside it: the completion popup was **measured in Inter and drawn in Cousine**, so its text spilled out of its own highlight -- every ASCII string hides that by a different amount, and the rest of the pane multiplies cells by one advance for exactly this reason; a pane holding ImGui's active id made **every other item in the frame unhoverable** (`ItemHoverable`, `imgui.cpp`), so the first click on the Explorer, the Viewport, the Properties grid or another tab did nothing and only the second landed -- released at the top of the frame, because by the time the pane draws, whichever panel came earlier has already answered that it was not clicked; the Properties grid **drew every line of `Source`**, making the panel as tall as the file, fixed with a `Code` flag in the IDL rather than by testing whether the string has a newline in it, because which properties are code is a fact about the CLASS -- the argument `contentKind` had already made in its own comment; and the central node's tabs now carry their class icons, so a `ModuleScript` tab and a `Script` tab are told apart at a glance |
| D106 | human, 2026-08-25 | Double-clicking in the middle of a word in the script editor selects only the half before the pointer | fixed | Reported as "double-clicking in the middle of `require` selects up to the `u`", which names the cause precisely: the selection ends where the pointer is. `wordAt` was right and the frame after it was wrong -- **`IsMouseDragging` with a zero threshold answers true for as long as the button is held**, so the double-click set the word and the next frame's drag put the caret back under the pointer, leaving whatever was left of it. The zero threshold is deliberate and stays: a selection drag that has to travel five pixels before it starts is a selection drag people fight. **Fixed 2026-08-25**: a drag knows what it is extending BY, and a word-wise drag grows a word at a time in either direction -- which is what every editor does and what makes the gesture worth having rather than merely not-broken |
| D107 | human, 2026-08-25 | `game:GetService("AudioService").` offers nothing: a path through a call is not walked | fixed | The first line of most Luau files ever written, and the completion walk stopped dead at the closing bracket -- so a file that assigns its services at the top completed nothing from the line after it. Two halves. **A call that NAMES something is a step in a path**: `GetService("X")`, `WaitForChild("X")` and `FindFirstChild("X")` each answer the instance their own argument names, so `X` is the step. Only a bare string literal counts -- `game:GetService(name)` is an expression, and reading one would be the type inference ADR 0057 says this does not do, so it stops rather than guesses. **And a step may be a local somebody assigned earlier**, read the way a person scrolling up would read it: one shape, `local NAME = <path>`, spliced in front of the path being resolved, three hops deep. Nothing is evaluated and no function is followed. **A third thing fell out of it**: a dot with nothing readable behind it used to fall through to the keyword list, so `t:GetChildren().` offered every reserved word in the language -- it now offers nothing, which is a state of its own |
| D108 | human, 2026-08-25 | The script editor has no zoom, and no way to move a line | fixed | Two gestures a code pane is expected to have, asked for together. **Ctrl and the wheel** size the text, read from the HOVER rather than the focus because a wheel belongs to whatever is under the pointer -- and the percentage appears in the pane's own corner with `Ctrl+0` beside it for as long as it has just changed, because **a zoom with no readout is a state somebody can get into and not out of**. **Alt+Up and Alt+Down** move the lines the selection covers, and the selection travels with them, which is what makes pressing the chord four times mean anything. The text half lives in `ScriptDocument::moveLines` rather than in the panel -- it is an edit and not a drawing, which is the split that file exists for and the reason it has tests -- and it is ONE `replace` over both blocks rather than a delete and an insert, so Ctrl+Z takes the whole move back. A move somebody has to undo twice is a move that will eat the line underneath it the first time nobody is watching. **One found while testing it**: the join asked `rebuilt.empty()` whether to write a separator, which cannot tell "nothing yet" from "a blank line" -- so moving a line into the empty last line of a file swallowed the final newline, and every file ends in one |
| D109 | human, 2026-08-25 | With a script tab focused, the first click on the Explorer, the Viewport, a property or another tab does nothing: everything takes two clicks | fixed | **Reported twice, because the first fix was a reading of ImGui rather than a measurement of it.** The code pane holds ImGui's active id for as long as somebody is typing -- that is what a caret is, and what makes the shell's `!IsAnyItemActive()` guards keep its shortcuts off the letters being typed. `ItemHoverable` refuses to hover any item while another is active (`imgui.cpp`), so nothing else in the frame could be clicked. The first fix released the active id at the top of the frame, before any panel is submitted, which is necessary and **was not sufficient** -- and the picture that was supposed to prove it was misread: the row it showed was HOVER-highlighted, not selected, and the old selection was still sitting two rows down in the same shot. **The second half was found by instrumenting the real window** and printing what ImGui thought on the click frame: the row was hovered, the active id was zero, the mouse key had no owner -- and no press happened anyway. The reason is one line in `ItemHoverable`: an item carrying `ImGuiItemFlags_AllowOverlap`, which every Explorer row does, is hoverable only if it was ALREADY the hovered id on the PREVIOUS frame. While the caret lived in the pane no row could hover, so on the frame the block was lifted no row had the history it needed either. **The cost was never one frame, it was one click.** **Fixed 2026-08-25** by declaring what is actually true -- `ActiveIdAllowOverlap`, "this widget is active and another may take that from it" -- so hover goes on being resolved underneath the caret and the row is already hovered when the press arrives. The top-of-frame release stays: it is what hands the caret back when a click lands somewhere no item takes it. **The general shape**: a fix whose only evidence is that the source reads correctly is D101's lesson arriving a second time, and the screenshot that seemed to confirm it is worse than no screenshot |
| D110 | human, 2026-08-25 | Autocomplete knows nothing about Luau itself: `typeof`, `math.floor` and `string.format` are not offered | fixed | The completion had the engine's reflection and the project's tree and not one thing from the language it is an editor for -- so `math.` offered nothing and `typeof` was a word you had to remember. **Fixed 2026-08-25** with a list in `engine/script/include/luaug/script/stdlib.h`: Luau's free globals and its ten library tables, every name carrying what `type()` answers for it. **Beside the sandbox rather than beside the editor**, because what a script can name is a fact about the VM -- the script module owns the sandbox, a test there can boot one, and `engine/app` already depends on it. **And written down, which needs an argument**: `api/defs/libraries.api.luau` refuses to declare Luau's libraries because "re-declaring them would make this file a second source of truth for something the pin already fixes", and that is right -- the difference is that the editor has to OFFER these names and cannot boot a VM to answer a keystroke. So the drift is caught instead of prevented: `sandbox_tests.cpp` stands a real sandboxed VM up and checks the list **in both directions**, so a Luau bump that gains a function fails a test rather than quietly leaving the editor a version behind. **It caught two mistakes on the first run**, which is the argument in miniature: the list was transcribed from `third_party/luau/VM/src/l*lib.cpp` rather than remembered, and that missed `math.e`, `math.nan`, `math.phi`, `math.sqrt2` and `math.tau` -- set with `lua_setfield` rather than listed in the registration table -- while offering `buffer.readinteger` and `buffer.writeinteger`, which are in that file inside a block this pin does not build. **The surface is this engine's, not stock Luau's**: `os` carries three names because `removeUnsafeGlobals` takes `difftime` off, and `getfenv`, `setfenv`, `newproxy` and `loadstring` are not offered because they are not there |
| D111 | human, 2026-08-25 | A ModuleScript nested inside another module cannot reach its own children: `require` of it errors | fixed | Reported as "I cannot require a ModuleScript that is a child of a Script", and reproducing it split the report in two. **Dot-access to a child is not the defect**: `script.Nested` is divergence #26 in api-design.md, decided deliberately -- an `__index` fallback to children is untypeable under a 100%-strict surface (ADR 0018) and is the habit that made `WaitForChild` load-bearing -- and the supported `script:FindFirstChild("Nested")` worked for a module under a Script and for one inside a Folder. **What was really broken is a module inside a module.** `evaluateModule` runs a module on a fresh sandboxed coroutine, and `luaL_sandboxthread` gives that thread a globals table whose `__index` is the creating thread's -- so a module inherited the `script` of whatever REQUIRED it. A nested module asking for its own children asked the outer Script's and found none, which from the outside reads exactly like "this module cannot be required". **Fixed 2026-08-25**: `script` inside a module is the module's own instance, and nil rather than inherited when a module has no instance at all (`@luaug/…` is not a script). Set BEFORE the load, which is not a preference -- `luaL_sandboxthread` marks the globals safeenv, so the compiler resolves globals at load time and anything assigned afterwards is invisible to every `script.X` in the chunk; the same ordering `startScripts` already documents. A file required by path gets the instance the mount made for it, so the same module body means the same thing whichever way it was reached. Three conformance cases cover the three nestings a person actually builds: under a Script, under a Folder, and under another module |
| D112 | human, 2026-08-25 | Deleting a script in the Explorer leaves its editor tab open | fixed | The tab went on drawing a document that could no longer be saved anywhere -- there is no instance to write `Source` to and no file behind a scene script either. **`ScriptEditor::forgetDestroyed` had existed and been tested since the editor was built and nothing ever called it**, which is the shape D087 recorded: code that is right and unreachable fails the moment something finally reaches for it. **Fixed 2026-08-25** by asking it once a frame at the safe point, after every command that can destroy an instance -- rather than wiring it to the delete, because an instance also goes when a scene is loaded, when a stamp session opens, and when an undo takes a create back. One question in one place answers all of them; a list of the ways would have missed the next one somebody adds. The close is said out loud in the status line, because a tab vanishing is something that happened TO a person and the text went with the instance it belonged to |
| D113 | human, 2026-08-25 | A script created inside an open stamp shows the wrong name in its tab and sometimes does not save | fixed | Both symptoms, and a third nobody had hit yet, are one cause: **an `InstanceId` is a handle into ONE world's slotmap and this editor has two**. A stamp is edited in a world of its own (ADR 0049), and the two allocate independently -- the first instance in each has the same handle. `openScript` read `host->world()` unconditionally, so a row double-clicked in the stamp's Explorer was resolved against the scene's world and answered about whatever instance happened to share the slot: a name from the wrong instance, a `Source` from the wrong instance, and -- the third one -- **every keystroke written into that wrong instance**, silently, in the project's own scene. Ctrl+S then took the `tab->file.empty()` branch and saved the SCENE, which is why the stamp "sometimes did not save": it never did, and the scene got the write. **Fixed 2026-08-25** by making a tab record which world it came from -- `ScriptOrigin`, an enum and not a `World*`, because a stage's world is destroyed when the stamp closes and a stale pointer compared against a fresh allocation that reused the address is a worse version of the same bug. Opening reads `authored()`, typing writes into the tab's own world, Ctrl+S on a stamp's script saves the stamp, `indexOf` is keyed on the pair so opening a stamp's script does not focus a scene tab that shares the id, and a stamp tab closes when its session does -- there is nowhere left to edit it. A stamp script's chunk name is its place in the stamp's tree, which needed `script::treePathOf` to take a world rather than a `lua_State`: it is a fact about a world, and the stamp's has never been seen by a VM. **The case is now a test rather than a reading**: two fixture worlds are asserted to hand out equal ids, and the tabs are kept apart anyway |
| D114 | human, 2026-08-25 | The script editor is not intelligent enough: it never says a name was used without being declared, nor that one was declared and never used | fixed | Asked for with Roblox Studio as the bar, and with the standard the request itself set: **responsive, and not in the developer's way.** **Fixed 2026-08-25** with two lints over the AST the parser already builds for the syntax errors -- `Luau.Analysis` carries a linter and is deliberately not built (35% of a cold build; ADR 0018, ADR 0057), and neither of these needs it. The PARSER already resolves scope, which is the hard half: a name it could bind to a local comes out as `AstExprLocal` and one it could not comes out as `AstExprGlobal`. **An unknown global here is a fact rather than a style note**, which is what makes it worth drawing: `sealGlobals` freezes the globals table (R4), so a name outside the sandbox's surface will be nil at runtime -- and the set it is checked against is the same one the completion offers, so it can never underline a name the editor itself just suggested. **What it deliberately does not report** is the whole of "not in the way": a file that does not parse is not linted at all, because a half-typed one would put a warning under every name somebody is mid-word on; a function's parameters and a loop variable are never reported, because both are unused constantly and on purpose; and a leading underscore is honoured, because that is how a person says "I know". A warning is drawn in the warning colour under the NAME rather than in red to the end of the line -- an error means this will not compile and a warning means it will, and drawing them the same is how a panel teaches somebody to ignore both. Costs nothing per keystroke: it runs in the parse that already happened one frame after the text came to rest |
| D115 | human, 2026-08-25 | A material being edited draws with none of its maps: the preview sphere ignores a diffuse, a normal and a roughness map all set on it | fixed | Reported as "the stamp preview is not showing the texture", and it was two walls one behind the other. **The first is this row.** The frame loop ran `syncPrimitives`, `syncTextures` and `sync` over `host->world()` and nothing else, while `extract` has known about the stamp stage since a stamp began opening onto a world of its own -- so a `Material` that existed only on the stage had no texture loaded, and every `MeshPart` on a stage had no mesh uploaded either. **Fixed 2026-08-25** by loading for every world that can be drawn rather than for the game's: both, not whichever is on screen, because the libraries are keyed by content and shared, and keeping the game's meshes resident is what makes closing a stamp instant instead of a frame of missing geometry. The second wall is D116, and fixing this one alone changed nothing anybody could see -- which is worth recording, because it is exactly the case where a correct fix reads as a wrong diagnosis |
| D116 | agent, 2026-08-25 | Every `Material` instance in the engine ignores every map it is given: the texture is bound, sampled, and multiplied by zero | fixed | Found while looking for why D115's fix changed nothing. The shader never branches on whether a map is present -- every slot always has a texture, a 1x1 stand-in when the material has none -- so `textureFlags` is what decides whether a sample counts: it multiplies the difference between the map and the factor. `materialOf` assigned the four handles and left the four flags at their zero default. `fillEntry`, forty lines away in the same module, wrote both, which is why a mesh out of a glTF has always looked right and made the difference impossible to see from the symptom: a surface drawing its factor alone is indistinguishable from a surface whose map was never set. **Fixed 2026-08-25** by making the maps and the flags one verb, `RenderMaterial::setMaps`, used by both paths -- stating a fact twice is what let one copy go stale, and no amount of remembering to write the second line is a fix. **Why it shipped**: every material case in `render_world_tests.cpp` checks a NUMBER, and a number arrives through the uniforms whether or not the flags say anything. Nothing had ever handed a `Material` a texture. Two cases now do, break-verified: deleting the four flag writes fails both and nothing else in 49,369 assertions notices |
| D117 | agent, 2026-08-25 | Dragging a panel edge in the editor stalls the whole GPU, once per frame, for as long as the drag lasts | fixed | Found while auditing for what makes the editor feel heavy, and the code said so itself: `ViewportTarget::resize` called `device.waitIdle()` before freeing the texture it was replacing, and its own comment noted that "the stall is real and it is paid while somebody drags a splitter, which is the one moment nobody is measuring frame time". It is also the one moment somebody is looking hardest at how the tool feels. **Fixed 2026-08-25** with the retirement queue `MeshCache::beginFrame` already uses, down to the two frames of slack: a replaced target is put aside and freed once no command list still in flight can name it. `destroy()` keeps the wait, because shutting down is not a frame and there is no next one to drain the queue. Asserted through the CAPTURE device, which records every call -- the null device forgets and a real one cannot be interrogated -- so "was this freed yet" is a question a test can actually ask. Break-verified. **What this is not**: it is not the reported "everything reloads when I change a property", which is still unreproduced. Measurement ruled out the two obvious suspects -- an undo snapshot of a 30,000-instance world costs 0.81 ms, and a headless frame of an idle editor costs 0.037 ms -- and the windowed numbers that looked bad were vsync |
| D118 | human, 2026-08-25 | "Anything I do in the engine looks like everything reloads -- I change a property on something and it seems to reload" | fixed | **Measured rather than guessed at, after two other suspects were ruled out the same way.** An undo snapshot of a 30,000-instance world costs 0.81 ms and a headless idle editor frame costs 0.037 ms, so neither the snapshot nor the frame loop was it; the windowed numbers that looked bad were vsync. What it is: `MeshLoader::syncTextures` reads, decodes and uploads every missing map it finds **in one frame, with no budget at all**, on the frame thread. An ordinary 1024-square PNG out of a texture pack costs **14 to 36 ms to decode** -- measured on the reporter's own textures -- so pointing a part at a three-map material froze the frame after the write for a tenth of a second. A 4K source is sixteen times worse. On a 240 Hz display that is eight to seventy dropped frames on exactly the frame somebody changed something, which is how a tool whose frame times all look fine comes to feel like it reloads the world whenever you touch it. **Fixed 2026-08-25** with the three-stage shape `StreamingHost::pump` already uses: `platform::readFileAsync` reads, the job pool decodes and resamples as `Domain::AssetIo`, and only the upload happens on the frame, because only the frame has a command list. Bounded at four in flight, and a queue that cannot be joined falls through to the synchronous read rather than failing -- deferring is a way of doing the work, not a permission to skip it. **Off by default**, and that default is what keeps every golden byte-identical: a capture records the frame it was told to, and a texture arriving two frames later is a different picture. The editor turns it on and says why. **Confirmed end to end by A/B on the reporter's own project**: the same scene, the same three maps, 269 ms on the load frame with the synchronous path and 179 ms with the deferred one -- the 90 ms difference being exactly the three decodes. **What is still open**: meshes take the same synchronous path (`MeshLoader::sync` parses a 3 MB glTF on the frame thread), and the content browser's thumbnails were given this shape in the same session for the same measurement |
