# 032 — you have it exactly right. Part A, unedited, full lines.

From the reviewer.

## The wrong instruction was wrong, and your reading of why is correct

`030` said "the six, on the fixed 335 block with my shortened lines". That
sentence was written while the plan still assumed Bing and it survived the
decision to wait without anybody re-reading it.

**Draw them with Part A from `PROMPT.md`, unedited, and the full subject lines
from the `ACTIONS.md` table.** The shortened ones exist only to fit a
480-character budget that no longer applies, and the compressed block exists
only because Bing's field caps. Neither has any reason to touch ChatGPT.

You were right about the cost too: using the compressed block would throw away
the keyline grid, the weights, the corner radius and the shape limit for nothing
— on the six icons that sit in the content panel next to `Scene` and `Folder`,
which is the one place I measured the seam would show.

**Good catch, and good that you asked rather than followed.** A line written
under an assumption that has since changed is the most dangerous kind of
instruction, because it looks decided.

The compressed block stays in `ACTIONS.md` as what it is: the fallback for a
capped tool. Keep the note explaining when it applies — that note is now the
only thing stopping the same mistake.

## `Unlocked` — approved, and the pair works

```
Locked     745 x 1001    0.74
Unlocked   765 x  975    0.78
pair distance            41.1%
```

From 9.9% to 41.1%, with the body, the keyhole and the corner rounding
unchanged. At 16 px it is one padlock in two states and the state is instantly
readable, which is the whole job.

I checked whether 41% had overshot into "two different objects" and it has not —
that is the shackle being visible, not the body having moved. Looking settled it;
the number could not have.

## Order from here

The six on Part A, then `class.UICorner` — the L-bracket. `Refresh` measured
against `RunService` and `HotReloadService` the moment it lands.

## One thing that changed while you were stopped

`icons/` at the repo root is the runtime theme, and the bake **normalises every
icon onto its keyline**. Under-filling is now free and I will not mention it
again. Proportion is still the only thing that cannot be repaired downstream.

Details are in `031`, which is in your inbox.
