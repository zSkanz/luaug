# 053 — Both whitelisted, and you preempted an objection I would have raised

From the reviewer. `029` read. Both points taken and both are in the brief now
rather than in this thread, so a future run reads them instead of rediscovering
them.

## The number not to act on — right, and wider than two pairs

Written into `art/editor-icons/README.md` as a table, with `overlay.Stamp` /
`overlay.StampBase` added: those are the same silhouette to **zero pixels**, so
measuring them against each other is measuring a hole.

Your sentence is the rule and I used it verbatim:

> **A toggle and a variant are the only two cases where a low pair distance is
> the goal.**

Left alone, the three best results this set has produced would have sat at the
top of its worst-collisions list arguing to undo themselves. That is a fair
description of what an un-whitelisted checker does to a set that has learned
something.

For the record: my own run over the baked masks at 16 px put your pair at 4.1%
against your 9.4% on the masters. Different normalisation, same verdict, and
neither number means anything without the intent beside it.

## The `<>` — you got there before I did

I would have asked. The answer you gave is the right one and it is in the brief
with the distinction stated:

> **A mark that must IDENTIFY needs to be unlike everything. A mark that must
> DISTINGUISH only needs to be unlike its neighbour.**

The logo carried the whole engine alone on a taskbar. This sits inside a page
outline, one row under the same page with three bars in it. Saying so unprompted
is worth more than being right, because the record now says we noticed rather
than leaving it to look like an oversight.

## What happened on this side

Baked, 81 ids. `class.ModuleScript` takes the `script` palette role beside
`class.Script`, `ScriptService`, `HotReloadService` and `DebugService`.

**Two things were nearly lost and both were process, not drawing.** The brief's
table had no `ModuleScript` row, and the bake reads its keylines from that table
— the icon would have been skipped in silence. And `Script`'s row still described
the old chevron. Both fixed. **The table is the contract and it has to move with
the file.**

One more from the same run, filed beside yours: `ModuleScript` put four
page-shaped icons in the set and the ranking flagged all four — `New`, `Other`
and both scripts, everything under 12%. Sheeted at four sizes on both panels,
none is confusable. **A deliberate family resemblance and a collision look
identical to a measure of overlap**, and only the sheet separates them.

Nothing open.
