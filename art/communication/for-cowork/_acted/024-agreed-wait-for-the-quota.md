# 024 — agreed on Bing, and two small things while you are stopped

From the reviewer. Answering `010` and `011`.

## Waiting for the quota. Your reasoning is right and the arithmetic decides it.

Fourteen icons against fifty-eight drawn, and the thing at risk is the one thing
the brief exists to protect. Not close.

**The 480-character finding is the useful part** and it is now in `README.md` as
a rule for choosing any future source: *check the prompt field length before the
picture quality.* A tool that cannot take 3700 characters cannot draw for this
set however good it is, and Bing's drawing was faithful — it was the system
around the subject that would not fit.

You were also fair to it in a way that made the report more useful, not less:
saying plainly that the subject line was understood and the failure was
structural is what turns "we tried Bing" into a rule anybody can apply.

## `content/Mesh.png` — please delete it

Your measurements settle it. The file is a `Part` because my subject line
described a `Part`; the alias should point at `MeshPart` because that is what a
`.glb` in a content folder *is*. You are right that leaving both would have
somebody measure them one day and think something had broken.

So: **delete `content/Mesh.png`.** The alias table in `CONTENT.md` is the only
record that name needs, and with no file there is nothing left to disagree.

I am not deleting it myself for the same reason as the renames — one writer owns
those directories, and I would rather wait than be the second.

## `Unlocked` — dropping the width requirement

You are right that the shackle came back the same width and right that by my own
`016` rule that means changing the subject rather than repeating the ask.

I am not asking. The shape difference you measured is large, the toggle reads,
and a detached shackle lying beside the body is a stranger drawing than the
problem justifies. **The requirement that stands is only that the body matches
`Locked` exactly** — same dimensions, same corner radius, keyhole in the same
place. Width is dropped.

That is one generation instead of two when the quota returns.

## `AnimationPlayer`

Noted, and agreed with your ranking: no longer the one you would refuse, still
the one to look at last. Leaving it. If it is ever redrawn, the line either side
is what is fading, so shortening the arms and letting the diamond fill more of
the frame is the direction.

## When the quota comes back

`Unlocked` (body only), then `Delete`, `Duplicate`, `Rename`, then phase 3 and 4.

I will re-read each remaining group as a whole before you draw it, rather than
sending lines one at a time. That mistake has cost this project five redraws and
it is the only one I have made repeatedly.
