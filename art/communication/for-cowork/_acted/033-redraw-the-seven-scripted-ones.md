# 033 — draw the seven scripted ones on Part A, and we will compare

From the reviewer. Direct from our human: replace the scripted icons with
ChatGPT versions now that the quota is back.

## The seven

`Stop`, `Pause`, `Duplicate`, `Grid`, `Back`, `Forward`, `Up`.

**Part A unedited, full subject lines from `ACTIONS.md`** — same as the six.
These have never been tried on ChatGPT. Only Bing saw them, on the compressed
block, and what killed them there was precisely the absence of the keyline
system that Part A carries.

## Deliver them into `proposals/`, not `actions/`

Not because I doubt them. Because we have something to compare against for the
first time, and it would be a waste not to:

```
                        scripted            drawn
radius                  58 exactly          measured on arrival
keyline fill            exactly             89-112% historically
midtone                 boundary only       ~0.4%
proportion              1.00 / 1.03         measured on arrival
```

The scripted seven are the most accurate icons in the project. If the drawn ones
land close, they win on being one hand with the other sixty-five. If they land
loose, we keep what we have and nothing is lost.

**I will measure both and put the sheet in front of our human**, because this is
exactly the kind of thing the numbers cannot decide alone — the earlier argument
was that a rounded square has no *hand*, so precision may simply beat sameness
here, and that is a judgement rather than a measurement.

Whichever wins, one set gets promoted into `actions/` and the other is deleted.
No pair survives.

## `UICorner` — approved

The L-bracket cleared it. `UICorner` / `Stop` was **2.0%**, the lowest number
this project produced; it is now well out of the closest pairs, and the
`Model` / `UICorner` pair went with it.

That was the collision that proved the general rule — *a difference that lives
in a fraction of an outline does not survive* — and the fix was to stop
distinguishing by a corner and start distinguishing by a shape.

## Order

1. The six that were never drawn: `Delete`, `Save`, `Open`, `New`, `Rename`,
   `Refresh` — into `actions/` as real deliveries.
2. The seven above — into `proposals/`.

The six first. They are the ones the set actually lacks; the seven already have
a working answer.
