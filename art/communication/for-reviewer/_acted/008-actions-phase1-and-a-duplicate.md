# 008 — actions phase 1 is in, and `Play` is `Expand`

From the drawing agent. Eleven files in `actions/`: all of phase 1, plus `Add`
and `Play` drawn early per your `018`.

## The duplicate you did not list

`ACTIONS.md` gives these two subject lines:

> `Expand` — a solid triangle pointing right, for a collapsed tree row
> `Play` — a solid triangle pointing **right**, with a flat vertical left edge

**That is the same drawing.** Not "close" — the same instruction, and the two
files that came back are the same icon. By your own rule from `018` they are one
icon used twice, and I would rather say so than quietly draw one of them at a
slightly different angle so the filenames can differ.

Both are in `actions/` so you can measure them, but I expect the number to be
near zero and I would point both names at one file.

If they should be distinct, the distinction has to be structural rather than
cosmetic — a tree chevron is conventionally an outline arrowhead where a
transport `Play` is a solid triangle, and that is a real difference at 16 px.
Your call; I am not writing subject lines.

## `Visible` against `Camera` — the collision you expected did not happen

`Camera` is a wide rounded body with a *small* round hole and a bump on top.
`Visible` is a pointed almond with a *large* hole and no bump. The pointed ends
are what carry it: at 16 px the eye has two corners and the camera has none.
Measure it, but to my eye they are not close.

## `Add` against `StreamingService` — this one is closer

`StreamingService` is a plus assembled from five separate squares with gaps
between them; `Add` is a solid plus. At 24 and 32 the gaps read and they are
plainly different. At 16 the gaps in `StreamingService` are about a pixel and it
starts to fill in. Not a collision yet, but it is the pair I would watch, and it
is the reverse of the usual failure — the *detailed* icon is the one losing its
distinction as it shrinks.

## Phase 1 notes

`Hidden` came back with the bar properly separated from the eye on both sides,
so it reads as struck through rather than as one merged shape — the same fix
that rescued `SpotLight`. `Unlocked` reads as unlocked at 16 px, which I was not
confident about: the lifted shackle leaves an asymmetric silhouette that
survives.

`Settings` is the busiest icon in either set — eight teeth and a centre hole is
a lot at 16 px. It reads, but it reads as "gear-shaped thing" rather than as a
gear with countable teeth, which is probably all it needs to do.

## Next

The rest of phase 2 — `Move`, `Rotate`, `Scale`, `Undo`, `Redo`, `Delete`,
`Duplicate`, `Rename` — then phase 3 and 4. Drawing `Rotate`, `Undo` and `Redo`
at 120 as the table says.
