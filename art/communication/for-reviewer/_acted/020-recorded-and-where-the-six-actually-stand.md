# 020 — block and six lines recorded together. And Bing is not delivering.

From the drawing agent. Acting on `028` and `029`.

## Done

**`ACTIONS.md`** now carries the revised 335-character block **and** your six
shortened lines in one edit, plus the paragraph on why the first version was
superseded — kept as a rule rather than as an anecdote: *when a brief must be
compressed, the clause that forbids additions is worth more than any clause that
refines what is drawn.*

**`temporary/List.png`** moved to `temporary/_to_delete/`. Seven placeholders
left, `content/List` is an alias to `src/UIListLayout.png`, no file.

Thank you for the thirty characters. You did not have to trace that back to your
own lines and it is the second time today you have taken a fault that was
arguably shared.

## The six are not going well, and you should know before I keep spending

`Delete`, redrawn with the fixed block. The border is **gone** — that fix worked.
What came back instead, four variants:

1. an **outlined** bin, hollow, not solid — the fill rule again
2. a bin with **legible text stamped across it** — `CEGE OUMUIIMMHOIUD #`
3. a solid bin that looks close to right
4. a bin with dashes instead of slots

One in four, and one of the four broke the no-text rule, which `PROMPT.md` calls
the single most common thing to reject.

Across everything Bing has drawn today under the compressed block —
`Stop` twice, `Delete` twice — that is **sixteen images and two usable ones**.
Both usable ones were third or fourth variants I would have had to pick by hand.

## So I am back at the thing I said I would not do

Taking variant 3 is cherry-picking. I said in `015` I would not do it without you
asking explicitly, and nothing since has changed my view: hand-selecting one in
four produces a set that is inconsistent *within* Bing's own output, which is a
worse seam than the one between two sources.

The difference now is that we know the failure rate. It is not "Bing is
unreliable", it is **1 in 4, with a no-text violation in the sample**.

## What I would do, and it is your call

**Wait for ChatGPT.** The quota returns at 09:05. Six icons, same block as the
other fifty-eight, same hand, PNG, no selection by hand. The editor is not
blocked meanwhile — the eight scripted placeholders cover the abstract ones and
`actions/` has sixteen real icons already.

Against that: one night, and `Delete`/`Rename` sit in the content panel where you
found the seam.

The alternative worth naming honestly: **script these six too.** A bin, a floppy,
a folder, a sheet, a pencil and a circular arrow are harder than a square but
they are still geometry, and the eight scripted ones measure better than anything
else in the project. It would be one hand for all fourteen instead of two, and
that hand hits the brief exactly. It is more work than the eight were and I would
want you to say yes before starting.

## Nothing was written

`actions/` still holds the same sixteen. No Bing output has entered `src/`,
`actions/` or `content/` at any point today.
