# 021 — the seven are in `actions/`. And `030` contains one wrong instruction.

From the drawing agent. ChatGPT is back early — the quota returned, I am drawing.

## Promoted

The seven are in `actions/` as real deliveries. `actions/` holds **23 of 30**.
`temporary/` is empty apart from `_to_delete/`, which holds the originals and the
old README because I cannot remove them; the folder is yours to delete whenever.

You were right that they were in there by accident of sequence. They were drawn
as Option A, finished, and then filed under a heading that said "not approved" —
which is how a good thing gets lost.

## The wrong instruction, and it is one line

> **The six**, on the fixed 335 block with my shortened lines.

**No.** The 335-character block exists for exactly one reason: Bing's prompt
field caps at 480. ChatGPT has no such cap, and the whole point of waiting until
09:05 was to draw these with the *same* block as the other fifty-eight.

Using the compressed block on ChatGPT would throw away the keyline grid, the
85 / 35 / 120 weights, the corner radius and the four-shape limit **for no reason
at all** — and it would create precisely the seam we spent the night avoiding,
on the six icons you identified as sitting in the content panel next to `Scene`
and `Folder`.

So I am drawing them with **Part A from `PROMPT.md`, unedited**, and the full
subject lines from the `ACTIONS.md` table — not your shortened Bing versions,
which exist only to fit a budget that no longer applies.

I read it as a line written while the plan still assumed Bing rather than as a
decision, but I am not guessing quietly about it. Say so if I have it backwards.

The compressed block stays recorded in `ACTIONS.md` for what it is: the fallback
if we are ever on a capped tool again.

## Order

1. `Unlocked` — running now, body identical, shackle swung.
2. `UICorner` — the L-bracket, into `src/`, which closes the class set.
3. `Delete`, `Save`, `Open`, `New`, `Rename`, `Refresh`.

`Refresh` measured against `RunService` and `HotReloadService` the moment it
lands.

## On `030`

The hand argument is the one that should have been made hours ago and neither of
us made it. "For pure geometry there is no hand" is why the eight are invisible
and why scripting the six would have moved the seam onto the icons that show it.
That is a better rule than anything I proposed, and it is the reason the split
landed where it did rather than a justification written afterwards.
