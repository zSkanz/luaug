// GENERATED by api/generator/gen_cpp.luau from api/defs/*.api.luau.
//
// Do not edit. The Luau gate regenerates this file and fails on a diff, so a
// hand edit is a change that survives exactly until the next build
// (architecture.md section 4).
//
// Every `docKey` below is empty on purpose. The doc text lives in the IDL and
// nothing in the engine formats a doc key yet: turning it into a catalog entry
// and a hover string is the M3 documentation pipeline (gen_docs and the docs
// JSON, docs/roadmap.md). An empty key here is that decision, not an omission.

#include "class_descriptors.gen.h"

#include <array>

#include "luaug/core/name_atom.h"
#include "luaug/core/text_key.h"
#include "luaug/scene/class_registry.h"
#include "luaug/scene/value.h"

namespace luaug::input::generated
{

// The descriptor arrays have static storage duration because
// `ClassDescriptor` holds `std::span`s into them and the registry outlives
// this call -- a local array would leave every descriptor pointing at a dead
// frame. They are ASSIGNED on each call rather than initialised on the first,
// so a second registry built over a second `AtomTable` gets ids from its own
// table instead of inheriting the first one's alphabet. The two registries
// still share this storage and so cannot be live at once.
//
// A `ClassId` is bound to a name only where a later class extends that class;
// binding one nothing reads would be an unused local, and warnings are errors
// on `engine/` (architecture.md section 8).
void registerClasses(scene::ClassRegistry& classes, core::AtomTable& atoms)
{
    // Registered by a lower module (architecture.md section 2, rule 3), so
    // the id is a lookup rather than a local: the module that owns a class is
    // the module that holds its `ClassId`. That module's `registerClasses`
    // must already have run -- engine/app/src/world_host.cpp orders them.
    const scene::ClassId instanceClass = classes.findId(atoms.intern("Instance"));

    // --- InputContext ---
    static std::array<scene::PropertyDesc, 4> inputContextProperties;
    inputContextProperties = {{
        scene::PropertyDesc{
            .name = atoms.intern("Enabled"),
            .type = scene::ValueType::Bool,
            .threadSafety = scene::ThreadSafety::Unsafe,
            .readOnly = false,
            .inert = false,
            .docKey = {},
            .errKeyOnInvalidSet = LUAUG_TR("scene.err.expected_boolean"),
            .get = native::getInputContextEnabled,
            .set = native::setInputContextEnabled,
        },
        scene::PropertyDesc{
            .name = atoms.intern("Priority"),
            .type = scene::ValueType::Number,
            .threadSafety = scene::ThreadSafety::Unsafe,
            .readOnly = false,
            .inert = false,
            .docKey = {},
            .errKeyOnInvalidSet = LUAUG_TR("scene.err.expected_number"),
            .get = native::getInputContextPriority,
            .set = native::setInputContextPriority,
        },
        scene::PropertyDesc{
            .name = atoms.intern("Sink"),
            .type = scene::ValueType::Bool,
            .threadSafety = scene::ThreadSafety::Unsafe,
            .readOnly = false,
            .inert = false,
            .docKey = {},
            .errKeyOnInvalidSet = LUAUG_TR("scene.err.expected_boolean"),
            .get = native::getInputContextSink,
            .set = native::setInputContextSink,
        },
        scene::PropertyDesc{
            .name = atoms.intern("Rate"),
            .type = scene::ValueType::EnumItem,
            .threadSafety = scene::ThreadSafety::Unsafe,
            .readOnly = false,
            .inert = false,
            .docKey = {},
            .errKeyOnInvalidSet = LUAUG_TR("scene.err.expected_enum_item"),
            .get = native::getInputContextRate,
            .set = native::setInputContextRate,
        },
    }};
    scene::ClassDescriptor inputContextDesc;
    inputContextDesc.name = atoms.intern("InputContext");
    inputContextDesc.super = instanceClass;
    inputContextDesc.flags = scene::ClassFlags::None;
    inputContextDesc.defaultName = atoms.intern("InputContext");
    inputContextDesc.docKey = {};
    inputContextDesc.properties = inputContextProperties;
    inputContextDesc.attachComponents = native::attachInputContextComponents;
    inputContextDesc.detachComponents = native::detachInputContextComponents;
    classes.registerClass(inputContextDesc);

    // --- InputAction ---
    static std::array<scene::PropertyDesc, 2> inputActionProperties;
    inputActionProperties = {{
        scene::PropertyDesc{
            .name = atoms.intern("Type"),
            .type = scene::ValueType::EnumItem,
            .threadSafety = scene::ThreadSafety::Unsafe,
            .readOnly = false,
            .inert = false,
            .docKey = {},
            .errKeyOnInvalidSet = LUAUG_TR("scene.err.expected_enum_item"),
            .get = native::getInputActionType,
            .set = native::setInputActionType,
        },
        scene::PropertyDesc{
            .name = atoms.intern("Enabled"),
            .type = scene::ValueType::Bool,
            .threadSafety = scene::ThreadSafety::Unsafe,
            .readOnly = false,
            .inert = false,
            .docKey = {},
            .errKeyOnInvalidSet = LUAUG_TR("scene.err.expected_boolean"),
            .get = native::getInputActionEnabled,
            .set = native::setInputActionEnabled,
        },
    }};
    static std::array<scene::MethodDesc, 2> inputActionMethods;
    inputActionMethods = {{
        scene::MethodDesc{
            .name = atoms.intern("GetState"),
            .yields = false,
            .threadSafety = scene::ThreadSafety::Unsafe,
            .docKey = {},
        },
        scene::MethodDesc{
            .name = atoms.intern("GetPreferredBinding"),
            .yields = false,
            .threadSafety = scene::ThreadSafety::Unsafe,
            .docKey = {},
        },
    }};
    static std::array<scene::EventDesc, 3> inputActionEvents;
    inputActionEvents = {{
        scene::EventDesc{
            .name = atoms.intern("Pressed"),
            .slot = 7,
            .docKey = {},
        },
        scene::EventDesc{
            .name = atoms.intern("Released"),
            .slot = 8,
            .docKey = {},
        },
        scene::EventDesc{
            .name = atoms.intern("StateChanged"),
            .slot = 9,
            .docKey = {},
        },
    }};
    scene::ClassDescriptor inputActionDesc;
    inputActionDesc.name = atoms.intern("InputAction");
    inputActionDesc.super = instanceClass;
    inputActionDesc.flags = scene::ClassFlags::None;
    inputActionDesc.defaultName = atoms.intern("InputAction");
    inputActionDesc.docKey = {};
    inputActionDesc.properties = inputActionProperties;
    inputActionDesc.methods = inputActionMethods;
    inputActionDesc.events = inputActionEvents;
    inputActionDesc.attachComponents = native::attachInputActionComponents;
    inputActionDesc.detachComponents = native::detachInputActionComponents;
    classes.registerClass(inputActionDesc);

    // --- InputBinding ---
    static std::array<scene::PropertyDesc, 9> inputBindingProperties;
    inputBindingProperties = {{
        scene::PropertyDesc{
            .name = atoms.intern("KeyCode"),
            .type = scene::ValueType::EnumItem,
            .threadSafety = scene::ThreadSafety::Unsafe,
            .readOnly = false,
            .inert = false,
            .docKey = {},
            .errKeyOnInvalidSet = LUAUG_TR("scene.err.expected_enum_item"),
            .get = native::getInputBindingKeyCode,
            .set = native::setInputBindingKeyCode,
        },
        scene::PropertyDesc{
            .name = atoms.intern("Up"),
            .type = scene::ValueType::EnumItem,
            .threadSafety = scene::ThreadSafety::Unsafe,
            .readOnly = false,
            .inert = false,
            .docKey = {},
            .errKeyOnInvalidSet = LUAUG_TR("scene.err.expected_enum_item"),
            .get = native::getInputBindingUp,
            .set = native::setInputBindingUp,
        },
        scene::PropertyDesc{
            .name = atoms.intern("Down"),
            .type = scene::ValueType::EnumItem,
            .threadSafety = scene::ThreadSafety::Unsafe,
            .readOnly = false,
            .inert = false,
            .docKey = {},
            .errKeyOnInvalidSet = LUAUG_TR("scene.err.expected_enum_item"),
            .get = native::getInputBindingDown,
            .set = native::setInputBindingDown,
        },
        scene::PropertyDesc{
            .name = atoms.intern("Left"),
            .type = scene::ValueType::EnumItem,
            .threadSafety = scene::ThreadSafety::Unsafe,
            .readOnly = false,
            .inert = false,
            .docKey = {},
            .errKeyOnInvalidSet = LUAUG_TR("scene.err.expected_enum_item"),
            .get = native::getInputBindingLeft,
            .set = native::setInputBindingLeft,
        },
        scene::PropertyDesc{
            .name = atoms.intern("Right"),
            .type = scene::ValueType::EnumItem,
            .threadSafety = scene::ThreadSafety::Unsafe,
            .readOnly = false,
            .inert = false,
            .docKey = {},
            .errKeyOnInvalidSet = LUAUG_TR("scene.err.expected_enum_item"),
            .get = native::getInputBindingRight,
            .set = native::setInputBindingRight,
        },
        scene::PropertyDesc{
            .name = atoms.intern("Scale"),
            .type = scene::ValueType::Number,
            .threadSafety = scene::ThreadSafety::Unsafe,
            .readOnly = false,
            .inert = false,
            .docKey = {},
            .errKeyOnInvalidSet = LUAUG_TR("scene.err.expected_number"),
            .get = native::getInputBindingScale,
            .set = native::setInputBindingScale,
        },
        scene::PropertyDesc{
            .name = atoms.intern("DisplayName"),
            .type = scene::ValueType::String,
            .threadSafety = scene::ThreadSafety::Unsafe,
            .readOnly = false,
            .inert = false,
            .docKey = {},
            .errKeyOnInvalidSet = LUAUG_TR("scene.err.expected_string"),
            .get = native::getInputBindingDisplayName,
            .set = native::setInputBindingDisplayName,
        },
        scene::PropertyDesc{
            .name = atoms.intern("Image"),
            .type = scene::ValueType::String,
            .threadSafety = scene::ThreadSafety::Unsafe,
            .readOnly = false,
            .inert = false,
            .docKey = {},
            .errKeyOnInvalidSet = LUAUG_TR("scene.err.expected_string"),
            .get = native::getInputBindingImage,
            .set = native::setInputBindingImage,
        },
        scene::PropertyDesc{
            .name = atoms.intern("DeviceType"),
            .type = scene::ValueType::EnumItem,
            .threadSafety = scene::ThreadSafety::Unsafe,
            .readOnly = true,
            .inert = false,
            .docKey = {},
            .errKeyOnInvalidSet = LUAUG_TR("scene.err.expected_enum_item"),
            .get = native::getInputBindingDeviceType,
            .set = nullptr,
        },
    }};
    scene::ClassDescriptor inputBindingDesc;
    inputBindingDesc.name = atoms.intern("InputBinding");
    inputBindingDesc.super = instanceClass;
    inputBindingDesc.flags = scene::ClassFlags::None;
    inputBindingDesc.defaultName = atoms.intern("InputBinding");
    inputBindingDesc.docKey = {};
    inputBindingDesc.properties = inputBindingProperties;
    inputBindingDesc.attachComponents = native::attachInputBindingComponents;
    inputBindingDesc.detachComponents = native::detachInputBindingComponents;
    classes.registerClass(inputBindingDesc);

    // --- InputService ---
    static std::array<scene::PropertyDesc, 3> inputServiceProperties;
    inputServiceProperties = {{
        scene::PropertyDesc{
            .name = atoms.intern("PointerLocked"),
            .type = scene::ValueType::Bool,
            .threadSafety = scene::ThreadSafety::Unsafe,
            .readOnly = false,
            .inert = false,
            .docKey = {},
            .errKeyOnInvalidSet = LUAUG_TR("scene.err.expected_boolean"),
            .get = native::getInputServicePointerLocked,
            .set = native::setInputServicePointerLocked,
        },
        scene::PropertyDesc{
            .name = atoms.intern("PointerVisible"),
            .type = scene::ValueType::Bool,
            .threadSafety = scene::ThreadSafety::Unsafe,
            .readOnly = false,
            .inert = false,
            .docKey = {},
            .errKeyOnInvalidSet = LUAUG_TR("scene.err.expected_boolean"),
            .get = native::getInputServicePointerVisible,
            .set = native::setInputServicePointerVisible,
        },
        scene::PropertyDesc{
            .name = atoms.intern("LastInputDeviceType"),
            .type = scene::ValueType::EnumItem,
            .threadSafety = scene::ThreadSafety::Unsafe,
            .readOnly = true,
            .inert = false,
            .docKey = {},
            .errKeyOnInvalidSet = LUAUG_TR("scene.err.expected_enum_item"),
            .get = native::getInputServiceLastInputDeviceType,
            .set = nullptr,
        },
    }};
    static std::array<scene::MethodDesc, 1> inputServiceMethods;
    inputServiceMethods = {{
        scene::MethodDesc{
            .name = atoms.intern("GetPointerPosition"),
            .yields = false,
            .threadSafety = scene::ThreadSafety::Unsafe,
            .docKey = {},
        },
    }};
    static std::array<scene::EventDesc, 2> inputServiceEvents;
    inputServiceEvents = {{
        scene::EventDesc{
            .name = atoms.intern("InputDeviceChanged"),
            .slot = 7,
            .docKey = {},
        },
        scene::EventDesc{
            .name = atoms.intern("WindowFocusChanged"),
            .slot = 8,
            .docKey = {},
        },
    }};
    scene::ClassDescriptor inputServiceDesc;
    inputServiceDesc.name = atoms.intern("InputService");
    inputServiceDesc.super = instanceClass;
    inputServiceDesc.flags = scene::ClassFlags::Service | scene::ClassFlags::NotCreatable;
    inputServiceDesc.defaultName = atoms.intern("InputService");
    inputServiceDesc.docKey = {};
    inputServiceDesc.properties = inputServiceProperties;
    inputServiceDesc.methods = inputServiceMethods;
    inputServiceDesc.events = inputServiceEvents;
    classes.registerClass(inputServiceDesc);
}

} // namespace luaug::input::generated
