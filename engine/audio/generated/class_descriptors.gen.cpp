// GENERATED by api/generator/gen_cpp.luau from api/defs/*.api.luau.
//
// Do not edit. The Luau gate regenerates this file and fails on a diff, so a
// hand edit is a change that survives exactly until the next build
// (architecture.md section 4).
//
// Every `.doc` below is the IDL's own English prose, not a catalog key: the
// audience for a reflection tooltip is whoever is building a game, and a
// `TextKey` is a hash with nothing on the other end of it unless the catalog is
// loaded. See `class_registry.h` for the whole of that reasoning.
//
// Anything outside printable ASCII is written as a three-digit octal escape, so
// these files stay pure ASCII whatever a compiler believes their source charset
// to be, and the bytes reaching the program are the IDL's own UTF-8.

#include "class_descriptors.gen.h"

#include <array>

#include "luaug/core/name_atom.h"
#include "luaug/core/text_key.h"
#include "luaug/scene/class_registry.h"
#include "luaug/scene/value.h"

namespace luaug::audio::generated
{

// The descriptor arrays have static storage duration because
// `ClassDescriptor` holds `std::span`s into them and the registry outlives
// this call -- a local array would leave every descriptor pointing at a dead
// frame. They are ASSIGNED on each call rather than initialised on the first,
// so a second registry built over a second `AtomTable` gets ids from its own
// table instead of inheriting the first one's alphabet. The two registries
// still share this storage and so cannot be live at once.
//
// A `ClassId` is bound to a name only where a later class extends that class;
// binding one nothing reads would be an unused local, and warnings are errors
// on `engine/` (architecture.md section 8).
void registerClasses(scene::ClassRegistry& classes, core::AtomTable& atoms)
{
    // Registered by a lower module (architecture.md section 2, rule 3), so
    // the id is a lookup rather than a local: the module that owns a class is
    // the module that holds its `ClassId`. That module's `registerClasses`
    // must already have run -- engine/app/src/world_host.cpp orders them.
    const scene::ClassId instanceClass = classes.findId(atoms.intern("Instance"));

    // --- AudioGroup ---
    static std::array<scene::PropertyDesc, 1> audioGroupProperties;
    audioGroupProperties = {{
        scene::PropertyDesc{
            .name = atoms.intern("Volume"),
            .type = scene::ValueType::Number,
            .threadSafety = scene::ThreadSafety::Unsafe,
            .readOnly = false,
            .inert = false,
            .doc = "A multiplier, not a level in decibels: 0 is silent, 1 is unchanged, and above 1 is louder and will clip if the material was already near full scale. Nesting is not supported -- a group parented to a group is just an instance parented to an instance, and only a `Sound`'s own `Group` is read.",
            .errKeyOnInvalidSet = LUAUG_TR("scene.err.number_at_least_zero"),
            .get = native::getAudioGroupVolume,
            .set = native::setAudioGroupVolume,
        },
    }};
    scene::ClassDescriptor audioGroupDesc;
    audioGroupDesc.name = atoms.intern("AudioGroup");
    audioGroupDesc.super = instanceClass;
    audioGroupDesc.flags = scene::ClassFlags::None;
    audioGroupDesc.defaultName = atoms.intern("AudioGroup");
    audioGroupDesc.doc = "A mixing bus (\302\247" "2.1, \302\247" "2.2). A `Sound` whose `Group` names one has its volume multiplied by the group's, which is how \"turn the music down\" is one property rather than a loop over every music track.\012\012Named `AudioGroup` rather than `SoundGroup` because the service is `AudioService`, and one prefix across a family beats two.";
    audioGroupDesc.properties = audioGroupProperties;
    audioGroupDesc.attachComponents = native::attachAudioGroupComponents;
    audioGroupDesc.detachComponents = native::detachAudioGroupComponents;
    classes.registerClass(audioGroupDesc);

    // --- Sound ---
    static std::array<scene::PropertyDesc, 9> soundProperties;
    soundProperties = {{
        scene::PropertyDesc{
            .name = atoms.intern("Content"),
            .type = scene::ValueType::String,
            .contentKind = atoms.intern("Audio"),
            .threadSafety = scene::ThreadSafety::Unsafe,
            .readOnly = false,
            .inert = false,
            .doc = "The audio, as an `asset://` URI. WAV, MP3, FLAC and Ogg Vorbis, decoded once and cached: a sound does not re-read its file sixty times a second.\012\012A URI that names nothing still PLAYS, as a generated tone whose pitch comes from a hash of the id. A sound that went silent because a file was missing is a bug report about the sound; a placeholder that is audibly a placeholder is one about the file. `DebugService:GetStat(\"AudioClipsMissing\")` is how a script asks which it got.\012\012**The file's length is the sound's length.** `Ended` fires when `TimePosition` reaches it, so a two-minute track plays for two minutes; a content that names nothing is one second long, because that is how long the placeholder tone is.\012\012The mixer keeps its own cursor and takes `TimePosition` whenever the two part company -- a seek, a rewind, a sound stopped and started. It has to: the timeline moves in whole ticks and a device moves in buffers, so a cursor re-read every frame stutters between them. Nothing in the simulation reads that cursor, which is what keeps a replay exact (R10).",
            .errKeyOnInvalidSet = LUAUG_TR("scene.err.expected_string"),
            .get = native::getSoundContent,
            .set = native::setSoundContent,
        },
        scene::PropertyDesc{
            .name = atoms.intern("Playing"),
            .type = scene::ValueType::Bool,
            .threadSafety = scene::ThreadSafety::Unsafe,
            .readOnly = false,
            .inert = false,
            .doc = "Whether the timeline is advancing. Writing it is the same as calling `Play` or `Stop`, and reading it is how a script asks without keeping its own flag.",
            .errKeyOnInvalidSet = LUAUG_TR("scene.err.expected_boolean"),
            .get = native::getSoundPlaying,
            .set = native::setSoundPlaying,
        },
        scene::PropertyDesc{
            .name = atoms.intern("Looped"),
            .type = scene::ValueType::Bool,
            .threadSafety = scene::ThreadSafety::Unsafe,
            .readOnly = false,
            .inert = false,
            .doc = "Whether reaching the end wraps to the start instead of stopping. A looped sound never fires `Ended`, because it never does.",
            .errKeyOnInvalidSet = LUAUG_TR("scene.err.expected_boolean"),
            .get = native::getSoundLooped,
            .set = native::setSoundLooped,
        },
        scene::PropertyDesc{
            .name = atoms.intern("Volume"),
            .type = scene::ValueType::Number,
            .threadSafety = scene::ThreadSafety::Unsafe,
            .readOnly = false,
            .inert = false,
            .doc = "A multiplier, combined with the group's and with the listener distance. The default is half rather than full because a game with several sounds at once and every one at 1 is a game that clips, and the first thing anybody does is turn them all down.",
            .errKeyOnInvalidSet = LUAUG_TR("scene.err.number_at_least_zero"),
            .get = native::getSoundVolume,
            .set = native::setSoundVolume,
        },
        scene::PropertyDesc{
            .name = atoms.intern("PlaybackSpeed"),
            .type = scene::ValueType::Number,
            .threadSafety = scene::ThreadSafety::Unsafe,
            .readOnly = false,
            .inert = false,
            .doc = "How fast the timeline runs, and therefore the pitch. 2 is an octave up and half the duration. Zero is refused rather than treated as a pause -- `Playing` is what pauses, and a speed of zero would be a sound that never ends.",
            .errKeyOnInvalidSet = LUAUG_TR("scene.err.number_above_zero"),
            .get = native::getSoundPlaybackSpeed,
            .set = native::setSoundPlaybackSpeed,
        },
        scene::PropertyDesc{
            .name = atoms.intern("TimePosition"),
            .type = scene::ValueType::Number,
            .threadSafety = scene::ThreadSafety::Unsafe,
            .readOnly = false,
            .inert = false,
            .doc = "Where the timeline is, in seconds. Writable, which is how a script seeks; the write lands on the next tick like every other, and past the end it stops or wraps exactly as arriving there would.",
            .errKeyOnInvalidSet = LUAUG_TR("scene.err.number_at_least_zero"),
            .get = native::getSoundTimePosition,
            .set = native::setSoundTimePosition,
        },
        scene::PropertyDesc{
            .name = atoms.intern("RollOffMinDistance"),
            .type = scene::ValueType::Number,
            .threadSafety = scene::ThreadSafety::Unsafe,
            .readOnly = false,
            .inert = false,
            .doc = "How far the listener can be before a positional sound starts getting quieter. Inside it the sound is at full `Volume`; ignored entirely for a 2D sound.",
            .errKeyOnInvalidSet = LUAUG_TR("scene.err.number_at_least_zero"),
            .get = native::getSoundRollOffMinDistance,
            .set = native::setSoundRollOffMinDistance,
        },
        scene::PropertyDesc{
            .name = atoms.intern("RollOffMaxDistance"),
            .type = scene::ValueType::Number,
            .threadSafety = scene::ThreadSafety::Unsafe,
            .readOnly = false,
            .inert = false,
            .doc = "How far the listener can be before a positional sound is silent. Between the two distances it fades linearly -- linear rather than inverse-square because a game's audible range is a design decision rather than a physical one, and an inverse square makes the far half of it inaudible.",
            .errKeyOnInvalidSet = LUAUG_TR("scene.err.number_at_least_zero"),
            .get = native::getSoundRollOffMaxDistance,
            .set = native::setSoundRollOffMaxDistance,
        },
        scene::PropertyDesc{
            .name = atoms.intern("Group"),
            .type = scene::ValueType::Instance,
            .instanceClass = atoms.intern("AudioGroup"),
            .threadSafety = scene::ThreadSafety::Unsafe,
            .readOnly = false,
            .inert = false,
            .doc = "The bus this sound's volume is multiplied by, or nil for none.",
            .errKeyOnInvalidSet = LUAUG_TR("scene.err.expected_instance"),
            .get = native::getSoundGroup,
            .set = native::setSoundGroup,
        },
    }};
    static std::array<scene::MethodDesc, 3> soundMethods;
    soundMethods = {{
        scene::MethodDesc{
            .name = atoms.intern("Play"),
            .yields = false,
            .threadSafety = scene::ThreadSafety::Unsafe,
            .doc = "Starts it from `TimePosition`, which is 0 for a sound that has never played and wherever `Stop` left it otherwise. Playing one that is already playing is a no-op rather than a restart -- restarting is `TimePosition = 0` and then this.",
        },
        scene::MethodDesc{
            .name = atoms.intern("Pause"),
            .yields = false,
            .threadSafety = scene::ThreadSafety::Unsafe,
            .doc = "Stops the timeline where it is. `Play` resumes from there.",
        },
        scene::MethodDesc{
            .name = atoms.intern("Stop"),
            .yields = false,
            .threadSafety = scene::ThreadSafety::Unsafe,
            .doc = "Stops it and rewinds to the start. `Ended` does NOT fire: it is a past-tense fact about reaching the end, and code that awards something when a jingle finishes must not be fooled by one that was cut off.",
        },
    }};
    static std::array<scene::EventDesc, 2> soundEvents;
    soundEvents = {{
        scene::EventDesc{
            .name = atoms.intern("Ended"),
            .slot = 7,
            .doc = "Fired on the tick the timeline reaches the end of a sound that is not `Looped`. From the SIM timeline rather than from the mixer, so it lands on the same tick in a replay, in a headless run, and on a machine whose audio buffer is four times the size.",
        },
        scene::EventDesc{
            .name = atoms.intern("Loaded"),
            .slot = 8,
            .doc = "Fired when this sound's `Content` has been decoded and is ready to play. **Fires immediately in v1**, because a sound has nothing to wait for yet -- declared now so that code written today does not have to change when it does.",
        },
    }};
    scene::ClassDescriptor soundDesc;
    soundDesc.name = atoms.intern("Sound");
    soundDesc.super = instanceClass;
    soundDesc.flags = scene::ClassFlags::None;
    soundDesc.defaultName = atoms.intern("Sound");
    soundDesc.doc = "One sound, playing or not (\302\247" "2.2). **Parent it to a `BasePart` and it is positional**; parent it anywhere else and it is 2D. That is the whole of the 3D switch, and it is a property of where the instance sits rather than a flag that could disagree with it.\012\012**Positional means two things**: it gets quieter with distance, and it comes out of the side it is on. The side is an azimuth in the listener's own frame, so turning around swaps it -- elevation is not expressed, because two channels cannot, and a source directly ahead and one directly behind are both centre for the same reason. A positional sound is folded to mono before it is placed; a 2D one keeps its own stereo image, which is all it has.\012\012**Its timeline is the SimClock's, not the mixer's.** `TimePosition` advances by `FixedTimestep * PlaybackSpeed` per tick and `Ended` fires from that timeline, so a replay reproduces both exactly and a headless run with no audio device produces the same `Ended` on the same tick as a run with speakers. What the speakers do is downstream of the simulation and never an input to it -- otherwise a script reading `TimePosition` would be reading the wall clock through a side door (R10).\012\012**How long it is, is the FILE's.** `TimePosition` advances on the simulation's clock and stops at the length of what `Content` decoded to, so the two facts a sound is made of come from the two places that own them.";
    soundDesc.properties = soundProperties;
    soundDesc.methods = soundMethods;
    soundDesc.events = soundEvents;
    soundDesc.attachComponents = native::attachSoundComponents;
    soundDesc.detachComponents = native::detachSoundComponents;
    classes.registerClass(soundDesc);

    // --- AudioService ---
    static std::array<scene::PropertyDesc, 1> audioServiceProperties;
    audioServiceProperties = {{
        scene::PropertyDesc{
            .name = atoms.intern("MasterVolume"),
            .type = scene::ValueType::Number,
            .threadSafety = scene::ThreadSafety::Unsafe,
            .readOnly = false,
            .inert = false,
            .doc = "Multiplied into every sound, after its own volume and its group's. The one a settings screen writes.",
            .errKeyOnInvalidSet = LUAUG_TR("scene.err.number_at_least_zero"),
            .get = native::getAudioServiceMasterVolume,
            .set = native::setAudioServiceMasterVolume,
        },
    }};
    static std::array<scene::MethodDesc, 1> audioServiceMethods;
    audioServiceMethods = {{
        scene::MethodDesc{
            .name = atoms.intern("PlayLocal"),
            .yields = false,
            .threadSafety = scene::ThreadSafety::Unsafe,
            .doc = "Creates a 2D `Sound` parented to this service and plays it. For the fire-and-forget case -- a click, a pickup -- where naming an instance is all ceremony.\012\012**It does not clean up after itself in this release**: the `Sound` stays a child of the service once it has ended, so a caller firing one per frame accumulates them. Keep the returned handle and `Destroy` it on `Ended` where that matters. The handle is also what a caller sets the volume through, since this takes only the content.",
        },
    }};
    scene::ClassDescriptor audioServiceDesc;
    audioServiceDesc.name = atoms.intern("AudioService");
    audioServiceDesc.super = instanceClass;
    audioServiceDesc.flags = scene::ClassFlags::Service | scene::ClassFlags::NotCreatable;
    audioServiceDesc.defaultName = atoms.intern("AudioService");
    audioServiceDesc.doc = "Mixing and the listener (\302\247" "2.1). **The listener is `Workspace.CurrentCamera`** and is not settable: a game with two ideas about where the player is hearing from is a game with a bug, and the camera is already the one thing that answers that question.";
    audioServiceDesc.properties = audioServiceProperties;
    audioServiceDesc.methods = audioServiceMethods;
    classes.registerClass(audioServiceDesc);
}

} // namespace luaug::audio::generated
